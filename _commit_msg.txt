fix(lsp): 链式字段访问悬停解析

对 root.children.add 这类链式访问，当方法名包含点号时：
1. 先从变量类型解析字段（如 children → Array[HtmlNode]）
2. 提取字段基础类型（如 Array）
3. 在字段类型上查找方法（如 Array.add）

避免在 struct 上误查 "children.add" 方法名导致 fallback 提示
