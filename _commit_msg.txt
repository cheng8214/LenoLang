fix(lsp): 悬停格式改为前置类型语法

LenoC 使用前置类型声明（如 int a, Array[string] ab, var x），
悬停提示从错误的 "名字: 类型" 统一改为 "类型 名字"：
- 变量: Array[string] ab（原来 ab: Array[string]）
- 字段: int x（原来 x: int）
- 闭包参数: Renderer r（原来 r: Renderer）
- enum 值: ColorName red（原来 red: ColorName）

共修改 10 处 snprintf 格式
