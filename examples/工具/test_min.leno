import dirs

main() {
    print("start")
    var w = dirs.walk("d:\\CLeno\\LenoC\\src\\semantic")
    for w to entry {
        var files = entry[2]
        print("dir: " + entry[0] + " files: " + files.len())
        break
    }
}
