// wrapper.leno — 尝试重新导出 types.leno 的别名

import "types.leno" as t

// ❌ 方式1: export alias — 语法报错 "期望表达式"
// export alias MySize = t.MySize

// ❌ 方式2: use — 报错 "模块中没有 struct/clib/face 类型"
// use t.MySize

// ✅ 方式3: 本地重定义 — 只能重复写 Dict[string, int]
// alias MySize = Dict[string, int]  // 这样写会覆盖掉

// 测试用函数
export func getSize(): t.MySize {
    return {w: 100, h: 200}
}
