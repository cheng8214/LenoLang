// 测试 1: 导入不存在的模块

main() {
    print("=== 测试 1: 导入不存在的模块 ===")
    print("尝试导入不存在的模块...")

    import "nonexistent_module.leno" as nonexistent

    // 如果导入失败，下面的代码应该不会执行
    print("如果看到这行，说明导入没有正确报错")
}
