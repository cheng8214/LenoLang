// 简化形状数学模块

import "../数学模块链/geometry.leno" as geo
import "../类型定义/types.leno" as ty

// 计算圆形面积
export func circle_area(any radius)
{
    return geo.circle_area(radius)
}

// 获取颜色名称
export func get_color_name(any color)
{
    return ty.color_name(color)
}
