// 几何计算模块 - 依赖 math_utils 模块

import "math_utils.leno" as math

export func circle_area(var radius)
{
    // 使用导入模块的变量和函数
    return math.multiply(math.PI, math.square(radius))
}

export func rectangle_area(var width, var height)
{
    return math.multiply(width, height)
}
