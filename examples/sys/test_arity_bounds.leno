main() {
    // 正常调用
    assert(true)
    assert(true, "msg")

    assert_eq(1, 1)
    assert_eq(1, 1, "msg")

    _gc()
    _gc(true)

    input()
    input("prompt")

    print("hello")
    print("a", "b", "c")

    format("hello %s", "world")

    print("全部正常调用通过")
}
