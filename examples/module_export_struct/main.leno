// 测试导入包含 struct 的模块

import "math.leno"

main()
{
    print("测试导入包含 struct 的模块")
    
    // 尝试使用导入的函数（函数内部使用 Point）
    var p2 = math.create_point(30, 40)
    print($"Point2: x={p2.x}, y={p2.y}")
    
    print("测试完成!")
}
