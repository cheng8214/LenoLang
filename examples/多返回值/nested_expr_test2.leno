// 测试：多返回值在算术和条件表达式中
func get_info(): [int, string, float] {
    return 42, "hello", 3.14
}

// 情况1: 多返回值函数 + 算术运算
var y = get_info() + 1
print("y = " + y)

// 情况2: 多返回值在条件表达式中
if get_info() > 0 {
    print("positive")
}

print("=== 测试结束 ===")
