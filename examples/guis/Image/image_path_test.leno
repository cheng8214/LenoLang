import guis

main() {
    // 测试1：相对路径（已知能工作）
    print("=== 测试1: 相对路径 ===")
    Image img1 = guis.load_image("icons/leno_logo.png")
    if img1 == null {
        print("  失败!")
    } else {
        print("  成功:", img1.width(), "x", img1.height())
    }

    // 测试2：绝对路径，单反斜杠
    print("=== 测试2: 绝对路径（单反斜杠） ===")
    Image img2 = guis.load_image("D:\CLeno\LenoC\icons\leno_logo.png")
    if img2 == null {
        print("  失败!")
    } else {
        print("  成功:", img2.width(), "x", img2.height())
    }

    // 测试3：绝对路径，正斜杠
    print("=== 测试3: 绝对路径（正斜杠） ===")
    Image img3 = guis.load_image("D:/CLeno/LenoC/icons/leno_logo.png")
    if img3 == null {
        print("  失败!")
    } else {
        print("  成功:", img3.width(), "x", img3.height())
    }
}
