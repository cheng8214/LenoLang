// Bug复现1: import 链式访问 struct（不用 use）
// import "repro_struct_def.leno" as m → new m.Info() 能否创建？

import "../结构体GC/repro_struct_def.leno" as m

// ⚠️ 关键测试: 不 use，直接用 m.XXX 访问
var info = new m.Info(name = "Tom", age = 30)
var pt   = new m.Point(x = 1.0, y = 2.0)

main() {
    // struct 创建
    print("info.name =", info.name)
    print("info.age  =", info.age)
    print("info.greet =", info.greet())
    print("pt = (", pt.x, ",", pt.y, ")")
    print("=== PASS ===")
}
