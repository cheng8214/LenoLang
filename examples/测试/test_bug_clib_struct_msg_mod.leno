// clib 调用的模块，缺少 as Ptr[u8] 转换
import ffi

clib kernel32 {
    Ptr GetCurrentProcess()
}

kernel32 _lib = ffi.load("kernel32.dll")

export func getHandle(): Ptr[u8] {
    return _lib.GetCurrentProcess()     // 缺少 as Ptr[u8]
}
