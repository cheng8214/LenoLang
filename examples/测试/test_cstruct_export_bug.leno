// 复现：A→B→C→main，export struct + 跨模块 cstruct 数组索引 → 模块加载崩溃
// 关键条件：export struct 有多个方法 + 多个 cstruct use + 一个方法用 v[i].x = xxx
import "test_bug_mod_c.leno" as c
use c.RendererLike

main() {
    print("开始 — 如果看到这行，模块加载成功")
    RendererLike r = new RendererLike()
    r.test_array()
    print("完成 — 如果看到这行，bug 未复现")
}
