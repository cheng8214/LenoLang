// 跨模块调用带约束泛型函数
import "constraint_module.leno" as cm

main() {
    var a = new cm.OrdInt(value=10)
    var b = new cm.OrdInt(value=20)
    var r = cm.maxBy(a, b)
    var ri = r as cm.OrdInt
    print("max = " + ri.value)
    assert(ri.value == 20)
    print("test_cross_constraint: OK")
}
