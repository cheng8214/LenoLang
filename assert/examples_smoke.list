# 示例冒烟门禁 —— **只编译**（`-c`）的清单
# 一行一个路径（仓库相对）；`#` 开头是注释。
# 扩这份清单 = 承诺「它以后不许坏」⇒ 只放**本来就绿**的；发现坏示例先修、再进清单。
# 背景（为什么要有这个门禁）：`examples/jsons/` 曾三个示例**全部编译不过**（47 / 87 / 87 个
#   「不能在 any 类型上直接访问字段」）却长期没人发现 —— 它们不在任何门禁里
#   （套件只自动发现 `assert/test_*.leno`）。见 docs/待办_易用性痛点（TraeSign 移植实录）.md 的 P7。
examples/jsons/json_any.leno
examples/min_multi.leno
examples/multi_ret_closure_test.leno
examples/nested_expr_test.leno
examples/nested_expr_test2.leno
examples/test_type_check.leno
examples/array/比较.leno
examples/array/比较2.leno
examples/array/多层嵌套.leno
examples/array/基本 clear.leno
examples/array/交换.leno
examples/array/深拷贝功能.leno
