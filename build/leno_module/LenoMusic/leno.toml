[package]
name = "LenoMusic"
version = "2.0.0"
description = "Leno audio module (streaming playback) based on miniaudio"
authors = ""
license = "MIT"

[modules]
root = "lib"

# 原生库：只声明**随包实际分发**的文件（打包时按当前平台键复制到 exe 同目录）。
# 声明了而文件不存在 ⇒ 打包直接报错，故本包未随附的平台不列键。
[native-libs.miniaudio]
win   = "lib/miniaudio.dll"
