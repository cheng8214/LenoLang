// 回边 GC 轮询的每轮成本测量（§8.39）
//
// 为什么要 10 亿次：times.ms() 的刻度是系统计时器 tick ≈ 15.625ms —— 实测拿到的
// 只有 187 / 203 / 219 这类「16ms 的倍数」。1 亿次 arr[0] 约 190ms（12 个刻度），
// 而轮询成本约 15ms（1 个刻度）⇒ **1 亿次下分辨率不足以分辨**，很容易把刻度跳动
// 当成真实效应（§8.39 记录了这个教训：最初报的「203 vs 188」其实只有 1 个刻度）。
// 10 亿次下同样成本约 150ms ≈ 9 个刻度，才有分辨力。
//
// 测法：同一台机器上跑「有轮询」与「无轮询（临时把 EMIT_CALLOUT_BEGIN 里的
// body_has_callout 置 0 重新编译）」两个二进制，各跑 N 轮取**最小**值。
// 参考值（i5-3450，JIT 全开，10 亿次）：
//   无轮询 1906ms / 有轮询（2 条指令）2015ms ⇒ +0.11ns/轮 ≈ +5.7%
//
// 注意这是「循环体只有一个内联数组读」的极紧循环，是本机制成本的**下界形态**：
// 真实分配循环每轮本来要付一次 callout（~1ns 起），轮询占比远小于 1%。
// 纯算术循环（i++）根本不发射轮询（body_has_callout 为假），零开销。
import times

var arr = []
for 1000 { arr.add(1) }

var n = 1000000000
var t = times.ms()
for n { arr[0] }
print("arr[0] x " + n + ": " + (times.ms() - t) + "ms")
