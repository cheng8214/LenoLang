# 待办与路线图

> **用途**：把"已知还没做"的事集中记在这里 —— 一件事一条，带**判据**和**跑法**，
> 以后可以一条条捡起来做，不必重新摸上下文。
>
> **与差异清单的分工**（别混）：
> - `docs/寄存式与栈式的差异清单.md` 记**事实**：哪一侧对/错、根因、修过的历史、既存差异。
> - 本文档记**待做事项**：优先级、判据、成本、依赖。
> - 做完一项 → 差异清单补"事实"，这里标 ✅ 并写 commit。
>
> 最后更新：2026-09-24。基线：套件 **373 passed / 0 failed**。
>
> ⚠ **性能数字的测量机**：本仓库所有耗时数据都在 **Intel Core i5-14400F**（10 核 / 16 线程）
> 上测得。换机器绝对值不可比；跨轮比较也会被频率漂移 + 构建布局漂移污染
> （见 ⑤-m 末尾的测量方法学）⇒ 只信**同机同轮**的 A/B。
>
> ⚠ **回归方式（已定）**：日常回归**只跑断言套件**：
> `.\build\leno.exe assert\run_tests.leno build\leno.exe assert`。
> **不再与栈式逐行对比**（2026-09-23 起）：寄存器式已经在语义与性能上跑到栈式前面，栈式计划放弃，
> `tools/` 里的对照脚本已删除 —— 这条流程整个去掉，别再写"跑差分"的判据。

---

## 一、现状快照

| 指标 | 值 |
|---|---|
| 断言套件 | `373 passed / 0 failed`（2026-09-24 合并远端 13 笔后，367 → 373） |
| `examples/` 规模 | **993** 个 .leno |
| 已逐条验证 | async await 23 + func 23 + import 211 + 测试 140 + threads 14 + 若干单文件 ≈ **420** |
| 已知性能项 | `examples/async await/gc .leno`：寄存器式 25s 只跑完 ~1785/2000+ |

> **不再与栈式对比**（2026-09-23）：寄存器式已是唯一实现，栈式放弃。`tools/` 下的对照脚本已删除，
> 涉及"栈式跑得完/栈式不同"的历史记录只当**归档**看，不再驱动任何工作。

---

## 二、待办总表

| 编号 | 优先级 | 事项 | 成本 | 风险 | 依赖 | 状态 |
|---|---|---|---|---|---|---|
| T2 | P0 | 输出快照基线（golden）+ 一键校验 | 半天 | 低 | 挑"结果确定"的样例 | ⬜ |
| T3 | P1 | async 类型推断三连：C3 + C6 + C5 | 半天–1 天 | 中（动语义层推断） | — | ⬜ |
| T4 | P2 | `asyncs.all` 语义：C8 + C10 | 2–4 h | 中（改语义） | **需先拍板方向** | ⛔ |
| T5 | P3 | 性能：~~5 个死 IC 全部接线~~（`a40f0fa`/`8642aeb`/`759d45a`）／~~数组 `add` 融合~~（`b209f07`）／~~零拷贝原地帧~~（`0cab715`，PBKDF2 −12%）／~~位运算裸位运算~~（`e91ed86`，−25%）／~~for 计数器原始减一~~（−9~14%）／~~返回路径专精（照 Lua OP_RETURN0/1）~~（空调用 −6.1%、pbkdf2 −2.1%，见 ⑤-y）／**调用路径（仍 2.5x，无单点肥肉，见 ⑤-g/⑤-v）**／~~共享寄存器池（帧 664B→152B，收益未量出 ⇒ 已回退，见 ⑤-x）~~／~~绑定对象缓存 arity/is_async（收益在测量地板下 ⇒ 已回退，见 ⑤-aa）~~／~~struct 方法调用窥孔（face 路径 −47.4%，见 ⑤-ab）~~／~~比较+跳转融合放宽到下标等非变量操作数（原地快排 20000 档 −10.7%，见 ⑤-ad）~~／~~`and` 链条件的逐侧融合（优化快排 −8~−10%，见 ⑤-ae）~~／~~`while true` 专精（中性/负收益 ⇒ 已回退，见 ⑤-af）~~／~~一元运算直取变量寄存器（隔离探针 −13.4%，见 ⑤-ag）~~／`gc .leno` | 1–2 天 | 中 | — | ◐ |
| T10 | P1 | 特化/融合指令对账：补「栈式有、我们缺」的指令（清单见 §T10） | 逐条 0.5–2 h | 低–中 | F1 已完成（`b209f07`）；① 比较+跳转融合已完成（⑤-ad 放宽到下标/字段/算术操作数、⑤-ae 覆盖 `and` 链，见 §T10① 末两条） | ◐ |
| T6 | — | ~~差异清单：C3 升级为"已实测"~~ ⇒ **取消**（不再与栈式对比） | — | — | — | ⛔ |
| T7 | — | ~~复核 B1 / D1~~ ⇒ **取消**（不再与栈式对比） | — | — | — | ⛔ |
| T9 | P3 | ~~解释器：抽 6 个巨型 handler 成 `static` 函数（方案 B）~~ ⇒ **已实测并回退：不是杠杆，是"代码布局抽奖"**（抽 5KB 冷代码 ⇒ 空调用 **+8.5%**、广覆盖探针 +3.34%，单循环级 ±10% 抖动方向随机；`vm_run` 机器码 120,256 B = 3.75× L1I 的前提是真的）。见 §T9 末节 | 已做 | 高（布局不可控） | 分块已完成（`bbb95c2`） | ⛔ |
| T11 | P2 | ~~`int` 特化算术把 **null 静默当作 0**~~ ⇒ **按作者决定"一律报错"**：特化算术/位运算的守卫改成「两侧都是 int48」，null 走冷路径报错；编译期**能确定**的（`var a = null` / `int? a = null` 且此后未赋值）提到**编译错误**（`Symbol.is_null_value`，判据「宁漏勿误报」+ 误报守卫用例）。见 ⑤-u | 完成 | 中（动语义／特化路径） | 已拍板：一律报错 | ✅ |
| T12 | P2 | ~~`patch_common` 对超范围跳转偏移**静默截断**~~ ⇒ 已改成**编译期报错**（见 ⑤-r）：`OP_JMP`/`CMPJMP`/`iAsBx`/`sBx` 四条编码各加范围检查，溢出时报错并中止编译（退出码 -1） | 0.5 h | 低 | — | ✅ |
| T15 | P0 | ~~模块 chunk 的动态 locals 被**双重释放**~~ ⇒ 已修（见 ⑤-u）：`REG_FINISH_RETURN` 三支已各自 free，`vm_run_chunk` 又 free 一次 ⇒ 0xC0000374 堆损坏。触发条件 = 模块 chunk 槽位 > `INLINE_LOCALS_MAX` | 2 h | 中 | — | ✅ |
| T16 | **P0** | ~~被调函数里的空体 `for {}` 会踩掉调用方的活寄存器~~ ⇒ **已修**（见 ⑤-ac）：根因是全局函数直呼的 `dst_safe` 只校验"实参区空闲"（0 参时该区间为空 ⇒ 恒真）⇒ 拿活变量槽当基址，而原地帧窗口比实参区长、伸到调用方活寄存器上。改成"dst 之上没有活寄存器"的健全判据，**零性能代价**；新增 `assert\test_for_call_alias.leno` | 2 h | 高（调用约定） | — | ✅ |
| T17 | P2 | **.lenb 体积继续瘦身**（行号表 RLE 已落地 = `4c14ab1`）：① 字符串 interning（实测上限 pvz −28% / minilang −13%）／② 整包 LZ（当前产物 deflate 2.8×，见 ③-补）／③ 元数据 u32→varint（从未实测）。构成实测与判据见 §T17 | ①中 ②高 ③低 | ①低–中 ②中 ③低 | **裁剪侧已收工（2026-09-25）：无死代码可删，433~505 KB 已达标；压缩侧仍开放** | ⬜ |

---

## 三、逐项详情

### ~~T1 全量差分扫描~~ ⇒ **已取消**（2026-09-23）

> 不再与栈式对比、`tools/diff_vs_stack.ps1` 已删除 ⇒ 本条整个作废。
> 需要"跑一批样例看有没有回归"时走 **T2 的输出快照**（对自身基线，不依赖任何第二实现）。

---

### T2（P0）输出快照基线（golden files）

**为什么**：断言套件只有 373 个点（覆盖不到全语料），需要一张**对自身基线**的回归网 ——
尤其以后改热路径/语义时，想"跑一批样例看有没有炸"就有现成手段（不再有第二实现可对照）。

**做法**：在 `tools/` 加 `snapshot.ps1`：
- 默认模式：对选定的"结果确定"样例跑一遍，把**归一化后**的输出写成基线（`tools/baselines/*.txt`）；
- `-Check` 模式：逐条对比，输出"与基线不一致"清单，退出码 = 不一致个数。
- 归一化规则：指针地址 / 耗时 / 时间戳 / 可执行路径 / 空行（这些在样例里天然会漂，
  实测 `examples` 里踩过的噪声类型见 ⑤-ab 的样例扫描说明）。

**挑样例的目录规模参考（约数）**：`cstruct` 41 ｜ `struct\struct test` 36 ｜ `性能测试` 32 ｜
`验证` 27 ｜ `clib` 24 ｜ `type类型` 22 ｜ `crypto` 21 ｜ `enum` 21 ｜ `ffi` 21 ｜ `struct\face` 18 ｜
`array` 16 ｜ `bigint` 16 …

**判据**：一条命令给出不一致清单；基线纳入版本管理；至少覆盖 `async await`、`func`、
`import` 里的确定性子集。

---

### T3（P1）async 类型推断三连：C3 + C6 + C5

**为什么**：这三条是**同一个子系统** —— "`Future` 出现在某个位置时，静态类型推不出来"，
现状是**合法 async 代码在编译期就过不去**。合起来做最省。

| 项 | 现象 | 现状 |
|---|---|---|
| C3 | `a?.b()`（安全访问调 async 方法）静态类型是 `int?` | 运行时**是对的**、类型错（已实测，见下） |
| C6 | `await` 作用于**声明为 `Future` 的形参 / 字段** | 推不出类型 |
| C5 | `func` 形参里 `await cb(x)` | 推成 `func()` |

**已实测证据（C3）**：

```leno
struct W {
    int v = 7
    async func go(): int {
        await asyncs.sleep(5)
        return self.v
    }
}

async func driver() {
    var w = new W()
    var b = w?.go()        // 运行时正确：实测方法体里 current() = 3 ⇒ 确实建了协程、在协程里跑
    io.print(type(b))      // object（运行时是 Future）
    // 但**静态类型**是 `int?` ⇒ 下面这条编译期就失败：
    //   asyncs.is_done(b)  →  「参数 1 类型错误: 期望 'Future'，但传入 'int?'」
}
```

**判据**：`w?.go()` 的静态类型是 Future（可选包装则为"可选 Future"）⇒
`await w?.go()` / `is_done(w?.go())` / `all([w?.go()])` 都能编译通过；
`await` 声明为 `Future` 的形参/字段、`await cb(x)`（`func` 形参）同样能编译；**补断言**。

---

### T4（P2）`asyncs.all` 的语义：C8 + C10 —— ⛔ 需要先拍板

**现状**（不是纯 bug，是"文档这么写、但直觉相反"）：
- 文档原话：`### all(futures)` 「收集多个**已完成** Future 的结果」；
- 未完成的成员 ⇒ 结果里给 `null`（C10）；
- 失败的 future ⇒ 也给 `null`，错误**延后到 `run()`** 才炸（C8）。

**两个方向**：

| 方向 | 内容 | 影响 |
|---|---|---|
| (a) | 保持文档语义，把"调用 `all` 前必须 await"写成显式警示 | 不改行为，只改文档 |
| (b) | 改成"等齐全部 + 汇总错误" | 更符合 `all` 的名字，但**与现有文档相反**（新 A 类差异） |

**我的倾向：(b)**。理由是这几轮一贯的优先级 —— **静默错值 > 编译期报错 > 性能** ——
而"`all([失败future])` 返回 `[null]`、错误跑到别处才炸"属于最该消灭的那一类。

**判据（若选 b）**：`all` 等待所有成员完成；有失败 ⇒ 抛**汇总错误**（与 `run()` 的
「N 个协程失败: …」同口径）；`docs/module_asyncs.md` 同步改写；补断言。

---

### T5（P3）性能

#### ✅ 已修：`OP_MODULE_CALL` 的内联缓存是死字段（`a40f0fa`）

用户实测 `examples/性能测试/函数调用数学计算.leno`：栈式 4528ms / 寄存器式 7903ms（**慢 1.68x**）。

分解实验（关键：先把"哪一段慢"隔离出来）：

| 探针 | 栈式 | 寄存器式 | 结论 |
|---|---|---|---|
| 纯浮点循环 2000 万次 | 236ms | 148ms | 0.63x，**寄存器式更快** |
| 整数循环 2000 万次 | 1153ms | 643ms | 0.56x，**寄存器式更快** |
| `maths.sqrt` 1000 万次 | 199ms | 310ms | 1.55x 慢 |
| `sin*cos` 2000 万轮 | 802ms | 1465ms | 1.83x 慢 |

⇒ 差距**全在原生模块调用**这一条路上（≈22ns/次），与 VM 模型/算术/循环无关。

根因：`OP_MODULE_CALL` 每次调用都 `native_find_module_method()`（哈希 + 两次 `strcmp`），
而 `vm.ic_module_cache`（`InlineModuleCallCacheEntry` + `IC_MODULE_CACHE_SIZE 2048`）
**定义了、初始化了，handler 里从没读写过** —— 死字段（注释还写着"避免每次调用都做哈希表查找"）。
接上后：**7903ms → 3878ms（0.85x，反超 18%）**；`maths.sqrt` 探针 310→162ms。

#### ✅ 已修：另外两个死 IC（`8642aeb`）—— struct 方法查找 / struct 定义查找

| 缓存 | 用在哪 | 接线后的键 |
|---|---|---|
| `ic_method_cache` | `OP_GET_METHOD`（struct 分支） | 调用点 + `def` 指针 + 方法名哈希 + `struct_def_generation()` ⇒ 缓存**预创建闭包** |
| `ic_structdef_cache` | `OP_STRUCT_INIT` | 调用点 + 名字哈希 + 定义表代数 + `mod_owner` ⇒ 缓存 `def` |

键与判据都照栈式 `op_struct.inc` 的同名实现（它这两处是接上的）。
**A/B 实测**（`git stash` 回退前后各跑一次）：`光线追踪对象版` 5546ms → **5108ms（−8%）**；
另两个样例噪声级 ⇒ 有效但不大，**这两个不是当前最大的瓶颈**。

#### ✅ 已修：`ic_cache`（死字段）+ 内建方法调用的窥孔优化（`759d45a`）

`vm.ic_cache` 同样是死字段 ⇒ `OP_GET_METHOD` 的内建类型分支（`arr.len()` / `str.len()` /
`d.has(x)`）每次调用都查一遍方法表。按（调用点、receiver 类型、方法名哈希）缓存
`ObjNative*` 接上；**更大的头**是照栈式 `op_property.inc` 加了窥孔：

> 下一条就是 `OP_CALL` 且调用的正是本寄存器 ⇒ **不构造 `ObjBoundMethod`**，
> 直接把 receiver 当第 0 个实参调 `method->function()`，并就地吃掉那条 `OP_CALL`。

实测 `examples/性能测试/内联缓存 (Inline Caching) 性能对比测试.leno`：
**708ms → 671ms（只接缓存）→ 188ms（再加窥孔）**，对栈式 92ms 即 **7.64x → 2.04x**。
行为探针与栈式**输出完全一致**（0 参/1 参取值、热路径循环、循环内 try/catch）。

> 至此 **5 个内联缓存全部接上**：native / module / method / structdef / ic_cache。

#### ✅ 部分已修：立即数加减（`4f4cc86`）—— 递归基准 fib 1.45x → 1.33x

用户实测 `examples/性能测试/经典斐波那契数列测试_int.leno`：栈式 ~70ms / 寄存器式 ~101ms。
反汇编对齐后，**每次递归**的指令数：栈式 **11** 条、寄存器式 **23** 条，差距三处：

| 写法 | 栈式 | 寄存器式（改前） |
|---|---|---|
| `n - 2` / `n - 1` | `SUB_INT_IMM`（1 条） | `MOV + LOADI + SUB_INT`（3 条）✗ |
| `n <= 1` | `CMPJMP_LI_INT`（比较+跳转融合，1 条） | `MOV + LOADI + LE_INT + JMP_IF_FALSE`（4 条）✗ |
| 递归调用 `fib(x)` | `CALL_GLOBAL_FUNC_TYPED`（1 条） | `GETGLOBALFUNC + MOV + CALL + MOV`（4 条）✗ |

**已完成**：立即数加减（`OP_ADD_INT_IMM` / `OP_SUB_INT_IMM`，8 位有符号立即数）
⇒ fib 每次递归 23 → 19 条指令，中位数 1.45x → **1.33x**；`assert/test_int_imm.leno` 锁住。

> 实现期间踩到的三个坑（都写进了 codegen 的注释，别重犯）：
> ① 跳过右值求值的条件必须与"真的会用立即数指令"完全一致 —— 否则 `n <= 1` 会被编成
>    `OP_LE_INT A=1 B=1 C=0`＝`n <= n` 恒真（fib 返回 30 而非 832040）；
> ② `gen_binop` 收尾是**无条件** `reg_free(gen, r)` ⇒ 立即数路径必须补一个占位
>    `reg_alloc`，否则 free 掉寄存器 0、分配器全乱（runner 里 351 个用例挂）；
> ③ 左操作数必须是 int：`big1 + 100`（左为 bigint）只有通用 ADD 能算。

#### ✅ 又修两处：立即数比较（`efde937`）+ 免掉多余 CAST

| 改动 | 省下的指令 |
|---|---|
| `OP_LE_INT_IMM` 等 4 个立即数比较（`efde937`） | `n <= 1` 少一条 `LOADI` |
| `assign_cast_needed`：int/float 的**算术/一元**结果与目标类型一致时免 CAST | `return <int 运算>` 少一条 `CAST_INT` |

fib 每次递归：**23 → 22（加减立即数 `4f4cc86`）→ 21（比较立即数）→ 20（免 CAST）**，
耗时 **1.45x → 1.33x → 1.29x**（各 5 次取中位数；`函数调用数学计算` 仍 0.83x 反超，
`光线追踪对象版` 1.17x → 1.12x）。
> ⚠ 更正：`4f4cc86` 提交信息里"23 → 19"的 19 是**带 bug 的中间版本**上数的，
> 修好后实际是 22（已在 `efde937` 的提交信息里更正）。

#### ⬜ 下一步（fib 打平的账，20 → 11）

| 还差的 | 条数 | 做法 |
|---|---|---|
| **全局函数直呼** | ~6 | 递归调用现在是 `GETGLOBALFUNC + MOV + CALL + MOV` ×2 ⇒ `CALL_GLOBAL_FUNC`（函数索引 + 实参个数），并把实参**直接算进**实参寄存器（省掉那次搬运） |
| **LHS 搬进 dst** | ~2 | `MOV R1,R0` 只是把 `n` 搬到比较的临时寄存器；立即数比较读 `B` 寄存器，本来可以直接读 `n` 所在寄存器 |
| **比较+跳转融合** | 1 | `CMPJMP_*`：现在 `LE_INT_IMM + JMP_IF_FALSE` 两条 ⇒ 一条（受定长 4 字节指令限制，可能要短跳变体） |
| 基例的 `CAST_INT` | 1 | `return n`（局部变量）现在仍保守保留 CAST（变量运行期类型可能不符声明 —— 要动得先确认规范化策略） |

其中**全局函数直呼是最大的一块**（~6 条，占了剩余差距的一半），
也是唯一需要动"函数索引编码 + 调用发射"的，建议下一轮先做它。

#### ⬜ 仍待做：内建方法调用还差 2x ⇒ 需要**融合调用指令**

剩余差距来自寄存器式每次调用多两条 `OP_MOV`（receiver 搬进实参寄存器、结果搬出）
+ 逐参 `vm_stack_push` 的容量检查。要再压下去得加一条融合指令
（类似栈式的 `OP_INVOKE_METHOD_TYPED`，或让 codegen 直接生成"方法自调用"形态），
属于 **opcode + codegen** 的改动，比接缓存大一圈。

#### ⬜ 仍待做：`gc .leno` 的协程 / GC 吞吐

**事实**：寄存器式 25s 只跑完 ~1785/2000+，栈式跑完 ⇒ 差一个量级（**不是结果错误**）。
**做法**：先量再改（协程创建 / 事件循环轮询 / GC / 调度各占多少），别凭直觉改。
**判据**：能在 25s 内跑完，或至少把瓶颈定位到具体环节并记录在案。

---

### T9（P3）解释器：抽巨型 handler 成 `static` 函数（方案 B）

**已完成的前置（方案 A，`bbb95c2`）**：`src/vm/vminc/vm_run.inc` 从 4249 行单文件拆成
`src/vm/vminc/run/01..14_*.inc`（14 块、合计仍 4249 行），`vm_run.inc` 退化为 31 行的聚合文件。
拆分是**纯文本搬运**（拼回去与原文件逐字节一致），切点一律在 `LABEL(...)` 之前。

**为什么不能"拆成 .c"**：它本质是**一个函数体** —— `vm.c` 的 `vm_run()` 与
`vm_run_with_vm()` 各 include 一次（后者用 `#define vm (*vm_ptr)`），内部是
`LABEL(OP_x)` + 计算跳转。所以再往下只能做**函数级抽取**。

**只动这 6 个（实测 >100 行）**：

| handler | 行数 |
|---|---|
| `OP_STRUCT_INIT` | 285 |
| `OP_INDEX` | 256 |
| `OP_CALL` | 205 |
| `OP_STRUCT_DEF` | 171 |
| `OP_GET_METHOD` | 118 |
| `OP_INDEX_SET` | 107 |

其余 **116 个 handler 中位数只有 15 行，不要动**（动了只增加间接层）。建议顺序：
`INDEX`（纯值计算最多）→ `CALL` 的 native 分支 → `STRUCT_INIT`（最独立）。

**模式**：照 `vminc/vm_helpers.inc` 里的 `read_ext_const` —— 抽成 `static` /
`static inline`，**显式传 VM 和参数**（宏能靠 `#define vm (*vm_ptr)` 生效，函数不行）。

**两个坑**：
1. handler 里的错误路径现在是"报错 + `DISPATCH()`"，抽出去的函数**不能跳分发** ⇒
   必须用**返回值/状态码**表达失败，由 handler 决定怎么跳（别把控制流一起搬走）；
2. 分块文件里不得出现**文件级 `static`**（同一 TU include 两次会重复定义）。

**判据**：抽完后 `.\build.bat` + **断言套件 353/353**；每个被抽的 handler 体在
原分块文件里显著变短（理想 < 60 行）。（差分对照按需，不作日常判据。）

---

#### ⛔ T9 实测结论（2026-09-24）：**已实施并回退 —— 它不是杠杆，是"代码布局抽奖"**

> 用户要求"实施 T9 看看有提升没，有提升就推送"。**实测没有提升，反而在调用探针上稳定更慢**，
> 故**整体回退**（代码已还原，断言 367/367）。补丁留在 `build\_t9_reverted.patch` 备查。

**① 前提核实（T9 的动机本身站得住）** —— 用 `nm` 量 `vm_run` 的**机器码尺寸**：

| | 机器码 | 与 L1I(32KB) 之比 |
|---|---|---|
| `vm_run`（基线） | **120,256 B** | **3.75×** |
| `vm_run_with_vm` | 124,512 B | 3.8× |

⇒ 巨型单函数确实**远超 L1I**，"热派发路径与冷代码纠缠"这个嫌疑是真实的，不是臆测。

**② 实施（机械上完全可行，一次做通）**：
新增 `src/vm/vminc/vm_slow_handlers.inc`（**只 include 一次**，放在 vm.c 所有 `static inline`
辅助之后、`vm_run` 之前 —— 见该文件开头的三条约束），把
`OP_INDEX` 的 6 支冷类型（module/enum/struct/cstruct/cstruct_array/cstruct_array_view）
+ `OP_INDEX_SET` 的 3 支冷类型整体搬成 `static` **noinline** 函数。
**返回值协议**（关键：抽出的代码不能跳分发、不能碰 vm_run 的局部 `frame`）：
`0` = 成功（写进调用方给的 dst 指针）/ `1` = 已抛出可捕获异常（调用方重取 frame 后 DISPATCH）
/ `-1` = 致命（调用方 `return -1`）。参数**显式传 `VM*`**（该位置 `vm` 不是宏）。

结果：`vm_run` **120,256 → 115,168 B（−5,088 B，−4.23%）**，抽出的两个函数合计 6,384 B。
断言 **367/367** 通过 —— **功能上完全等价，机械上完全可行**。

**③ 但实测是负收益**（双向交替 A/B，两侧都 `--no-cache`；baseline = `build\leno_base_t9.exe`）：

| 探针 | 前 | 后 | Δ |
|---|---|---|---|
| **空调用隔离探针**（`空调用与自增隔离微基准`，各 6 次原始值） | **164.99 ~ 169.36** | **177.85 ~ 184.27** | **+8.5%**（分布**完全不重叠**） |
| 同探针 1 参调用 / `i++` | 145.48 / 39.59 | 143.15 / 40.15 | −1.6% / +1.4%（中性） |
| 指令覆盖面基准（8 项合计，5 轮取 min） | 1,021,427 | 1,055,586 | **+3.34%**（更差） |
| 光线追踪对象版 B2 / B / C | 321.1 / 536.8 / 1741.5 | 297.7 / 503.2 / 1757.2 | −7.3% / −6.3% / +0.9%（**方向互相抵消**） |
| 光线追踪标量版 C / A | 1271.8 / 78.3 | 1300.3 / 75.3 | +2.2% / −3.8%（同上） |

**④ 机理（这才是本轮真正的收获）**：抽走 5KB 冷代码把 `vm_run` 的**绝对地址整体挪了 ~11KB**
（`140017a40` → `140018560`）⇒ **I-cache 组相联的 set 映射全变了**。
极短循环体（空调用）对这一点最敏感（命中/冲突的 set 全换了）⇒ **±10% 的单循环级抖动**，
方向由运气决定：同一次改动里"空调用 +8.5%""B2 −7.3%"并存，合计≈0。
⇒ **T9 的收益不是"少几条指令/少几 KB 代码"能决定的，而是地址对齐的抽奖结果。**
这也解释了为什么 ⑤-c 把它列为"剩余嫌疑"却**从来量不出收益** —— 它本来就没有稳定收益。

**⑤ 教训（写给下一次的自己）**：
- **不要再试 T9**（含抽 `OP_CALL`/`OP_STRUCT_INIT`/`OP_GET_METHOD`/`OP_STRUCT_DEF`）——
  单次抽 5KB 已经证明是布局抽奖，抽更多只会换一个抽奖结果，**没有可归因的杠杆**；
- 若要碰"代码布局"这条路，前提是先有**布局可控**的手段（例如把热 handler 集中到独立段、
  或按 profile 重排、或 `__attribute__((aligned))`/链接脚本），**否则任何"抽代码"的 A/B
  都不可信**（连"回退后恢复原值"这种验证都只能证明"这次抽奖抽到了坏签"）；
- **回退验证很有用**：回退后 `空调用` 回到 164.18~169.13（与基线同区间）⇒ 归因干净，
  排除了"机器变慢/热漂移"这类解释。以后凡是"改动让某项变慢"，都做一次回退复测再下结论。

---

### T10（P1）特化/融合指令对账：栈式有、我们缺

**做法**（10 分钟可复现，两条集合差即可）：

```powershell
$reg = (Select-String src\include\leno_vm.h -Pattern 'OP_[A-Z0-9_]+' -AllMatches).Matches.Value | Sort-Object -Unique
$stk = (Select-String D:\CLeno\Leno\src\include\leno_vm.h -Pattern 'OP_[A-Z0-9_]+' -AllMatches).Matches.Value | Sort-Object -Unique
Compare-Object $reg $stk
```

结果：寄存器式 **136** 条、栈式 **167** 条。差集里约 60 条是**栈式机器专有**
（`GET_LOCAL`/`SET_LOCAL_POP`/`CLEAR_LOCAL_RANGE`/`POP`/`DUP`/`NULL`/`TRUE`/`ONE`…对寄存器式无意义 ✗）
或**同义异名**（`ADD_FLOAT`≡我们 `ADD_F`、`LENGTH`≡`LEN`、`NULL`≡`LOADNIL`、`JUMP`≡`JMP`、
`ARRAY`≡`NEWARRAY`、`CLOSE_UPVALUE`≡`CLOSE`、`GET_UPVALUE`≡`GETUPVAL`、`JUMP_IF_FALSE`≡`JMP_IF_FALSE`…）。
**真正值得补的是这几类**（已逐条核实"栈式有、我们没有"）：

| 序 | 项 | 我们的现状 | 栈式 | 预期收益 | 成本 |
|---|---|---|---|---|---|
| **1** | **比较+跳转融合** ✅ **已完成** | `LE_INT_IMM` + `JMP_IF_FALSE`（**2 条**） | `OP_CMPJMP_LI_INT` / `_LG_INT` / `_LL_INT`（1 条） | **每个 `if`/`while`/`for` 条件少 1 条**；fib 递归路径 9 → 8（**实测 0.938x**） | 中 |
| 2 | 语句上下文 `*_NOPUSH` ✅ **已完成（只做 add）** | 无 | `ARRAY_APPEND_NOPUSH`、`INDEX_SET_NOPUSH`、`INC/DEC_LOCAL_NOPUSH` | 语句上下文不再写结果/搬 MOV（`a.add(x)` 4 条 → 3 条，**同轮 0.86x**） | 低 |
| 3 | 整数特化补全 ◐（`MUL_INT_IMM` 已完成；`LE_INT` 本来就有 ⇒ 清单原判断有误） | 有 `ADD/SUB/LT/LE/GT/GE_INT_IMM`、`MUL_INT`、`EQ_INT`、`LE_INT` | 另有 `MOD_INT`、`EQ/NE_INT_IMM`、`MUL_INT_IMM` | `x * 3` 省掉一条 LOADI（**同轮 0.50x**）；其余两项见下 | 低 |
| 4 | 浮点比较特化 ✅ **已完成** | 只有 `CAST_FLOAT` | `LT/GT/LE/GE_FLOAT` | 浮点比较**同轮 0.913x**（`OP_LT_F` 等 4 条，省掉 value_compare_stdlib 调用） | 低 |
| 5 | 存疑项 | — | ✅ `*_TYPED` **已完成**（**同轮 0.837×–0.937×**，调用密集代码 -12~16%）；`SHL·SHR_IMM` / `TAIL_CALL_NATIVE` 实测本就不慢（0.29–0.40×）⇒ 降优先级；**`OP_ACC_FIELDS` 已有实测证据 ⇒ 升级为待做**（见下） | 中 |

**证据（fib `--debug` dump 对照）**：

| | 递归路径指令序列 |
|---|---|
| 栈式 | `CMPJMP_LI_INT` / `GET_LOCAL` / `RETURN` / `JUMP` / `GET_LOCAL` / `SUB_INT_IMM` / `CALL_GLOBAL_FUNC_TYPED` / `GET_LOCAL` / `SUB_INT_IMM` / `CALL_GLOBAL_FUNC_TYPED` / `ADD_INT` / `RETURN` ≈ **12 条** |
| 我们 | `LE_INT_IMM` / `JMP_IF_FALSE` / `RETURN` / `SUB_INT_IMM` / `CALL_GLOBAL_FUNC` / `SUB_INT_IMM` / `CALL_GLOBAL_FUNC` / `ADD_INT` / `RETURN` = **9 条** |

⇒ 栈式执行**更多**指令却打平（66.2ms vs 66.5ms）：我们**单条更便宜**、栈式**条数多但每条更薄**。
补上 1 号即可在条数上反超（9 → 8），且它作用于**所有**条件判断。

**① 比较+跳转融合 —— ✅ 已完成**

- **编码**：新增 `OP_CMPJMP_LT/LE/GT/GE`，占 **8 字节**：第一个字
  `[op][A=左操作数寄存器][B=右操作数][C(最高位=1 表示立即数形式)]`，
  **第二个 4 字节字**的前 2 字节是跳转偏移（`offset + 32768`，与 iAsBx 同约定）。
- **发射条件**（与 `*_INT / *_INT_IMM` 完全一致 ⇒ 语义等价）：`if`/`while` 条件是有序比较
  （`< <= > >=`）、两侧静态类型都是 int、左操作数是普通局部变量/参数（`SYM_LOCAL`/`SYM_PARAM`
  ⇒ 就是 `R[index]`）、右操作数是 int8 字面量或另一个局部变量/参数；任一不满足即不融合。
- **⑤-ad 放宽（2026-09-24）**：上面「左操作数必须是变量」这条已放宽为「**两侧都落进一个寄存器**」——
  下标 `arr[j]`、字段、`right-left` 这类算术子表达式、upvalue/全局都改为 **`gen_expr` 求值到临时
  寄存器**后融合（判据 ① 不变 ⇒ 语义仍与 `*_INT[_IMM]` 逐条等价）。顺带把"超 int8 立即数"
  从回退三条变成 `LOADI + CMPJMP` 两条。收益：原地快排 20000 档 **−10.7%**、微基准比较分支 **−9.1%**（见 ⑤-ad）。
- **⑤-ae 再放宽（2026-09-24）**：条件位置的 **`and` 链**逐侧融合（`gen_and_cond_jumps`）——
  每侧各自一条 CMPJMP，链尾那条 JMP 与每侧自己的 JMP 一起消失；`if`/`while 入口`各侧"假则跳"、
  `while 回边`非末侧"假则跳出"末侧"真则跳回"（侧数上限 8，超长链先摊平后发指令 ⇒ 不半截发射）。
  收益：优化快排（插入排序内层）**−8~−10%**、探针 −20.8%。见 ⑤-ae。
- **实测**：fib 递归路径 **9 → 8 条**；**同一份二进制 A/B**（`LENO_NO_CMPJMP=1` 关闭融合）
  65.7ms → **61.6ms = 0.938x**；`assert/test_cmpjmp.leno` 在**开/关两种模式下都过**
  （语义等价的直接证据）；断言套件 **354/354**。
- **三个坑**（都已踩过，后人别重踩）：
  1. `patch_jmp` / `patch_jmp_to` 的偏移必须按**指令自身长度**扣（`len - pos - size`）：
     融合指令是 8 字节，沿用 `- 4` 会静默写出错误的跳距 ⇒ 加了 `instr_bytes_at()`；
  2. **反汇编器**（`decode_trailing`）与**自检**（`disasm_self_check`）都必须知道这 8 字节，
     否则 `--debug` 的清单从那条指令起整体错位（看着像"凭空多出一条指令"），自检也核对不了它的跳转；
  3. **A/B 不要用"两次构建 + git stash"**：慢、易被机器频率漂移误导，还有把改动留在 stash 里的
     风险（本次取消时正好停在那一步）。用 `LENO_NO_CMPJMP` 这类**同二进制环境变量开关**最稳
     —— 栈式本来就有这个开关，当时就该照抄。

**② 语句上下文 `*_NOPUSH` —— ✅ 已完成（只做 `arr.add`）**

- 做法：**不新增 opcode**，直接让 `OP_ARRAY_APPEND` 认 C 位（`need_result`）——
  `C == 0` 时跳过"写回新长度"。codegen 侧在 `AST_EXPR_STMT` 里识别语句位置的
  `arr.add(x)`（`AST_CALL` + `ast.add` / `arr["add"]` 两种 callee 形态、接收者静态类型是数组、
  实参恰 1 个）⇒ 走 `gen_array_add(..., need_result=0)`。
- 效果：`a.add(i)` 作为语句从 **4 条**（MOV/MOV/APPEND+搬回 MOV）降到 **3 条**；
  **同轮 A/B 0.86x**（3.80ms vs 栈式 4.40ms）。
- 为什么没做 `INDEX_SET_NOPUSH` / `INC/DEC_LOCAL_NOPUSH`：`OP_INC/OP_DEC` 的写回**就是
  自增本身**（局部变量原地自增），`OP_INDEX_SET` 也只有赋值一条路径 ⇒ 没有"结果是废的"可省；
  真要做得多加 opcode 且收益 <1 条指令 ⇒ 暂缓（有需求再回来看）。

**③ 整数特化补全 —— ◐ 只补了 `MUL_INT_IMM`（其余有意不做）**

- **已完成**：`OP_MUL_INT_IMM`（语义与 `OP_MUL_INT` 逐条一致，含 bigint 与溢出提升）；
  codegen 把 `TOK_STAR` 也纳入立即数路径 ⇒ `x * 3` 从 `LOADI + MUL_INT` **2 条降到 1 条**、
  **同轮 0.50x**（2.17ms vs 栈式 4.35ms）。
  ⚠ 立即数路径的前提是**左操作数静态类型为 int**（沿用原有判断）—— 左值是 bigint 的
  `big * 100` 必须走通用 MUL，否则通用路径会读到空寄存器（原有注释里的老坑）。
- **清单纠错**：`LE_INT`（双变量）**本来就有**（`leno_vm.h:82`），之前那条是 grep 漏看 ⇒ 无需补。
- **有意不做**：
  - `EQ_INT_IMM` / `NE_INT_IMM`：**语义有风险** —— `==`/`!=` 目前刻意走通用 `OP_EQ`（跨 int/bigint/float
    的相等语义与"按位比较"的 `*_INT` 家族不同），补特化版要先厘清 `1 == 1.0` 这类口径，
    收益却只有"省一次类型分支"⇒ 不值得冒险；
  - `MOD_INT`：通用 `OP_MOD` 的**第一个分支就是 int 快路径**（`val_is_int && val_is_int`），
    特化版只省两次类型判断（~1–2ns）⇒ 收益太小，不值得多一条指令的维护面。

**④ 浮点有序比较特化 —— ✅ 已完成（附带一个通用收益）**

- 新增 `OP_LT_F / LE_F / GT_F / GE_F`：两侧静态类型都是 float 时替代通用 `OP_LT/LE/GT/GE`，
  不再每次调用 `value_compare_stdlib()`。**语义与通用版逐条一致**：null ⇒ false；两边是数值
  （非 bigint）⇒ 按数值比；其余（字符串 / bigint / 其它）**回退**通用比较，连报错文案都用同一份。
- **实测**（`examples/性能测试/特化指令基准.leno`，同二进制 `LENO_NO_FLOAT_CMP` 开关、
  5 次取 min）：float 比较 **0.893x ~ 0.913x**；float 字面量比较 0.956x。
- **顺带发现的通用问题**：`gen_binop` 里**右操作数**即使是普通局部变量/参数也会先 `OP_MOV`
  搬进临时寄存器（左操作数早就优化过，右边漏了 ✗）。修掉后 `a < b` 少一条指令、
  `x = x + 1.0` 这类别名情形也安全（指令先读两个操作数再写结果）。
  ⚠ 收尾的 `reg_free` 必须按 `r_is_temp` 守卫 —— 与文档里记过的 `rl` 那次是**同一类坑**
  （误释放活着的变量槽 ⇒ 变量被就地覆盖、runner 全挂）。
- **方法论教训（重要）**：这类 ~10% 的改动**不能靠单两次运行定论** —— 实测单两次的噪声就能
  把 0.91x 淹成"看不出效果"。口径固定为：**同一份二进制 + 环境变量开关 + 5 次取 min**。
  基准文件 `examples/性能测试/特化指令基准.leno` 就是为这个建的（各段都在同一进程内计时）。

**⑤ typed 直呼（`OP_CALL_GLOBAL_FUNC_TYPED`）—— ✅ 已完成（调用路径的第一刀）**

- **动机**：建帧时每个参数都要走一遍类型转换检查（int→float / bigint→float / float→int），
  每参数 4–6 个判断 —— 栈式为此专门有 `OP_CALL_GLOBAL_FUNC_TYPED`。
- **做法**：新增同编码的 `OP_CALL_GLOBAL_FUNC_TYPED`，与 `OP_CALL_GLOBAL_FUNC` **共用同一个处理体**
  （用 `frame->ip[-4]` 区分 opcode），只是改调 `call_reg_typed()` ⇒ `call_reg_impl(..., no_convert=1)`
  跳过转换循环、直接复制。
- **安全门槛（关键）**：只有当"**每个实参的静态类型 == 形参声明类型**、且都限定在 `{int, float}`"
  时才发 typed 版 —— 这两种类型正好是那套转换**唯一**会动的类型 ⇒ 跳过是**可证明的等价**。
  形参表缺失、实参个数 ≠ 形参个数（默认值补齐）、any/null/其它类型 ⇒ 一律走非 typed 版。
- **实测**（同二进制 `LENO_NO_TYPED_CALL` 开关、5 次取 min）：

  | 调用形状 | typed | 非 typed | 比值 |
  |---|---|---|---|
  | 结果丢弃（1 参） | 62.7ms | 71.2ms | **0.880x** |
  | 用结果（2 参） | 69.1 | 81.3 | **0.850x** |
  | 5 参数 | 99.1 | 118.4 | **0.837x** |
  | 8 局部变量 | 144.0 | 153.7 | 0.937x |

  （相对本会话开始时：函数调用 89.1 → **69.1ms = -22%**；5 参数 119.0 → **99.1 = -17%**。）
- **顺手澄清一件事**：`call_reg` 里那条 `TYPE_INT && val_is_float` 运行期转换分支，在本语言里
  **走不到** —— 实测"浮点字面量传进 int 形参""`any` 传进 int 形参"都被**语义层直接拦成编译期错误**
  （「参数类型不匹配」）。所以那条分支只有显式 `_int()` 这类转换后的值才可能碰到。
- **验证**：`assert/test_typed_call.leno`（同类型 / int→float 加宽 / any 形参 / 默认参数 / 多参数混合 /
  递归），**typed 开与关两种模式都过**；**关掉 typed 跑全量套件也是 357/357** ⇒ 语义等价的强证据。

> 调用形状的**横向**数据（同文件基准，栈式为参照）：纯调用 0.63x、用结果 0.75x、5 参数 0.63x、
> 8 局部变量 0.50x —— 每种形状我们都比栈式快 25–37%，所以"调用是短板"只相对于我们自己的其它段而言；
> 真正还差的是 Lua 那种"不搬参数的寄存器窗口"结构（尚未做，风险高）。

**⑤-b `OP_ACC_FIELDS`（字段访问密集场景，尚未做）—— 从"存疑"升级为"有实测证据"**

- **现象**（用户实测 `examples/性能测试/光线追踪对象版.leno`）：Phase A（对象字段访问）
  我们 **1.31x 慢于栈式** ✗，而 Phase B/B2/C（方法调用/递归）我们快 5–12% ✓。
- **dump 对照**（`s.cx + s.cy + s.cz + s.r` 每轮）：

  | | 指令 |
  |---|---|
  | 栈式 | `GET_LOCAL sum` / `GET_LOCAL s` / **`OP_ACC_FIELDS count=4`** / `ADD_FLOAT` = **4 条** |
  | 我们（修前） | `MOV`+`GET_FIELD` ×4 + `ADD` ×3 = **11 条** ✗ |

  ⇒ 栈式的 `ACC_FIELDS` 把"连取 N 个字段并求和"合成一条指令。
- **已顺手修掉的一半（本次提交）**：我们每个字段访问前都多一条 `OP_MOV`（对象被 `gen_expr`
  一律搬进临时寄存器），`spheres[si]` 的下标同样被搬一次 —— 按"局部变量/参数直取其寄存器"
  的手法修掉（与 `rl_direct`/右值直取同一论证，`*_is_temp` 守卫 `reg_free`）。
  **Phase A：131.3ms(1.31x) → 118.3ms(1.10x)**，即快 ~16%；B/B2/C 也各快 3–8%。
- **剩下的差距就是 `OP_ACC_FIELDS` 本身**（2 条 vs 7 条）—— ✅ **已完成**，见下 ⑤-d。

**⑤-d `OP_ACC_FIELDS` + `OP_GET_FIELD_FAST` —— ✅ 已完成（借鉴栈式的两条字段访问指令）**

- **`OP_ACC_FIELDS A B C`**（指令后紧随 C 字节字段索引）：把「同一对象的 N 个字段连加」合成一条
  （栈式同名为 `OP_ACC_FIELDS`）。codegen 判据：整棵 `+` 子树的叶子**全部**是字段访问、
  对象是**同一个**局部变量/参数、字段静态类型**全是 float**（原路径必然是 `ADD_F` 连加 ⇒
  融合版「double 左到右累加 + `val_float`」与之**逐位等价**）。int 字段/混入其它项一律走原路径
  （否则结果会从 int 变 float，语义改变）。
- **`OP_GET_FIELD_FAST A B C`**（对齐栈式同名指令）：静态类型已确定是 struct（**非** cstruct）时的
  字段读，省掉 cstruct 分流与 `struct_get_field()` 的跨 TU 调用；tag/类型/越界三项轻检查与
  **报错口径保持不变**（「字段访问需要结构体对象」/「字段索引越界」照旧能报）。
- **实测**（同机同轮，`examples/性能测试/光线追踪对象版.leno`）：

  | Phase | 栈式 | 我们（改前） | 我们（改后） |
  |---|---|---|---|
  | A 字段密集 | 289.8ms | 368.0ms | **223.7 / 238.8 / 224.8ms** |
  | B 方法调用 | 2302.4ms | 1995.3ms | 1980.5ms |
  | B2 单方法 | 1988.4ms | 1414.3ms | 1390.4ms |
  | C 递归 trace | 8307.0ms | 6478.6ms | 6243.4 / 6294.7ms |

  ⇒ Phase A **368 → ~225ms（−39%）**：从「比栈式慢 1.27x」变成「比栈式快 ~1.25x」，
  四个 Phase **全部快于栈式**；校验和 `sum=50730000.000273108` **逐位相同**。
- **dump 证据**：`benchIndex` 内层 `GET_FIELD×4 + ADD_F×3`（7 条派发）→
  `OP_ACC_FIELDS A=10 B=8 C=4 | 字段个数=4 索引=[0,1,2,3]`（1 条），内层热路径 17 → 11 条指令；
  同文件 18 处字段读取全部走 `OP_GET_FIELD_FAST`（旧 `OP_GET_FIELD` 0 次）。
- **门禁**：断言套件 **358/358**；植物大战僵尸连续 75s 无错误输出。
  ⚠ 该崩溃的**根因**由 `cce4e7e` 正面修掉了（预留实参块时清掉落在保留区间里的陈旧空闲槽位，
  回归网 `assert/test_call_arg_reg_reuse.leno`）—— 与本提交无关，别把功劳记到这里。
  本次两遍 dump 核对：`spawnSkySun` 的实参已落在块内 `R5..R9`、临时寄存器在块外（`R10`）。

**⑤-e `OP_INVOKE_METHOD_TYPED` —— ✅ 已完成（struct 方法「取方法 + 调用」融合）**

- **做法**：8 字节定长（头 = op/A/B/C；第二个字 = 方法名常量(16) + 静态类型名常量(16)。
  VM 侧仍按**运行时实际类型**分发，类型名常量只读掉、不参与查找 —— 与栈式同口径，
  留给将来的编译期去虚拟化）。`obj.m(a, b)` 原先是 `OP_GET_METHOD`（查表 + **每次调用
  分配一个 ObjBoundMethod**）+ `OP_CALL`（还要判"receiver 是否已在实参首位"）两条派发，
  现在一条指令直接 `call_reg` 建帧。
  codegen 判据：接收者静态类型是 struct、`func_table` 里能查到 `"<Struct>::<method>"`
  （脚本方法）、非 async、实参个数 ≤ 255。`t.cb()` 这类**函数类型字段**调用没有 mdef，
  照旧走老路径（含其"退化为取字段"分支），不受影响。
- **实测**（同机同轮，`examples/性能测试/光线追踪对象版.leno`，各两遍）：

  | Phase | 栈式 | 我们（融合前） | 我们（融合后） |
  |---|---|---|---|
  | A 字段密集 | 289.8ms | 223.7–238.8ms | 235.4 / 243.2ms |
  | B 方法调用 | 2302.4ms | 1980.5ms | **1471.4 / 1520.8ms** |
  | B2 单方法 | 1988.4ms | 1390.4ms | **1057.8 / 1078.8ms** |
  | C 递归 trace | 8307.0ms | 6243.4ms | **4989.3 / 5035.7ms** |

  ⇒ 调用密集的三个 Phase 各快 **20–26%**，对栈式领先 **1.5x–1.9x**；校验和逐位不变
  （hits=900000 / 900000 / 585500，sum=50730000.000273108）。
- **⚠ 踩过的坑（别再踩）**：第一版按"R[A+1] 就是 self 副本"写成
  `call_reg(cl, has_self=1, receiver, a+2, argc-1, ...)` ⇒ **校验和直接变 hits=0**（实参整体
  错位）。真因：语义分析对 struct 方法调用**是否把 receiver 塞进实参首位并不统一**
  （MODULE_CALL 改写的调用**不含**、AST_CALL 形态**含**），而且"值相等"会被**自引用**骗到
  （`a.add(a)` 只传 1 个实参却被当成 [receiver]）。正解是**逐字沿用** `06_call.inc` 绑定方法
  分支那段"首实参是否就是 receiver + 形参个数佐证"的判定，不要自己推布局。
- **门禁**：断言套件 **358/358**；植物大战僵尸连续 60s 无错误输出。
- **差集剩余**：仅剩整数冷路径补洞（`OP_DIV_INT` / `OP_MOD_INT` / `OP_EQ_INT_IMM`）。

**⑤-f `i++` 竟比 `i = i + 1` 慢 —— ✅ 已修（`OP_INC`/`OP_DEC` int 快路径）**

- **发现**（`examples/性能测试/全部测试.leno` 规模从 1 亿降回 1 千万后逐项对拍）：
  `i++` 216.5ms 而 `i = i + 1` 167.8ms —— **同一件事、更短的写法反而慢 29%**（栈式两者
  195/315ms，`i++` 更快）。隔离微基准（各 1 千万次）：`i++` 208.4ms vs `i = i + 1` 156.4ms。
- **真因**：`OP_INC` 走 `do_inc_dec()` —— **跨 TU 调用 + 逐类型派发**（int/float/bigint/非法），
  而 `i = i + 1` 编成 `OP_ADD_INT_IMM`（内联 int48 + `VM_INT_RESULT` 溢出提升）。
- **修复**：`OP_INC`/`OP_DEC` 加 int48 快路径（内联、复用 `VM_INT_RESULT` 同一提升口径）；
  bigint / float / 非法类型仍原样走 `do_inc_dec`（`val_is_int` 对 bigint 为假 ⇒ 不会误当 int48）。
- **实测**：`i++` 208.4 → **177.5 / 184.1ms（−14%）**；断言套件 **358/358**。

**⑤-g 函数调用的固定开销（已量清：没有单点"肥肉"）**

- **⚠ 先修正一条**：`全部测试.leno` 的"空函数调用"我们 591.9ms、栈式 109.0ms，看着
  **慢 5.4x** —— **那是假象**。用栈式 exe 对同一份微基准 `--debug` 反汇编可见，它的循环体
  是**空的**（`OP_FOR_PREP` 与 `OP_FOR_LOOP` 相邻、**没有 CALL**）：它把无副作用空函数的
  调用整个消掉了。真实对照只有 **Lua（19ns/次 vs 我们 64ns ⇒ 慢 3.1x）**。
- **量法**：`examples/性能测试/空调用与自增隔离微基准.leno`（空调用 / `i++` / `i = i + 1` /
  1 参调用分开量，各 1 千万次）⇒ 空调用 ≈60ns/次（循环体只有 `OP_CALL_GLOBAL_FUNC` +
  `OP_FOR_LOOP`）、1 参调用 ≈47–50ns/次。
- **rdtsc 四段计数器结论（`LENO_CALLSTAT=1`，量完已删）**：前导段（`OP_CALL_GLOBAL_FUNC`
  的读操作数/查全局槽/类型与 async 检查）≈5–10、建帧段 ≈30、函数体段 ≈15、返回段 ≈13 周期
  （每次读数约 20–25 周期的 rdtsc 自身开销已同向扣减），四段合计 ≈60–68；而每轮总开销约
  120 周期 ⇒ **没有哪一段是"肥肉"**，剩余 ~50 周期落在"返回后重载 frame + 跳转表间接跳转
  （分支预测）"上。下面三条原"剩余嫌疑"由此被部分证实，但**没有可一举砍掉的大块**。
- **已排除（别再重复查）**：① **建帧字段** —— 栈式 `vm_call.inc:83-148` 与我们
  `call_reg_impl` 写的是同一批（我们只多 `ret_frame/ret_reg/ret_want`）；② **返回路径逻辑**
  —— 栈式 `OP_RETURN` 反而**多**做（GC 安全点计数 + 栈下溢检查，`op_call.inc:620-640`）；
  ③ **参数个数** —— 空调用与 1 参调用耗时接近，不是逐参拷贝。
- **下一步（追 Lua 的主战场）**：**每指令派发成本** —— 我们 ≈30 周期/条（与栈式持平，
  但 Lua ≈10–17）⇒ 方向是 **T9**（把 `OP_INDEX`/`OP_CALL`/`OP_STRUCT_INIT` 等 6 个巨型
  handler 的冷路径抽成 `static`，改善热派发循环的代码密度 / I-cache）。调用开销本身，
  在砍掉 ⑤-h 之后已无单点可砍，属"多项小口子"。

**⑤-h 取消每条指令的 `vm.current_line` 写回 —— ✅ 已完成**

- **发现**：`vm.current_line` 在本仓库**只有 3 处写入、0 处 VM 内读取** —— 原生函数取当前行
  走 `native_get_current_line()`（`native.c:1504`），它自己从 `frame->ip` / `chunk->lines`
  **重算**，只在"拿不到帧"时才回退到该字段。而 `DISPATCH` 原先每条指令都要写一次
  （2 次依赖加载：`frame->chunk` → `chunk->lines`，再 1 次写）。
- **做法**：删掉 `DISPATCH`（含 switch 变体）里的写回；字段保留给"帧不可用"的宿主/回调场景
  兜底（初值 0）。
- **实测**（同轮、交替 3 轮取 min；`examples/性能测试/空调用与自增隔离微基准.leno` 与
  `光线追踪对象版.leno`）：

  | 项 | 有写回 | 无写回 | 收益 |
  |---|---|---|---|
  | 空调用 | 609.3ms | **545.3ms** | **−20%** |
  | 1 参调用 | 508.5ms | **464.2ms** | **−9%** |
  | `i++` | 154.5ms | 148.4ms | −4% |
  | `i = i + 1` | 154.5ms | 146.7ms | −5% |

- **门禁**：断言套件 **358/358**；光线追踪 Phase A 199.7ms（改前 211–218ms）。

**⑤-i 数值 for 改计数制（借鉴 Lua 5.5 的 `forprep`）—— ✅ 已完成**

- **动机**：`全部测试.leno` 的空体 for 我们 159.2ms、栈式 113.9ms、Lua 38ms ⇒ 落后栈式 1.40x、
  落后 Lua 4.2x。原因是三寄存器比较制：每轮 3 次寄存器读 + 加 + 比较 + `inclusive`/符号两支，
  外加每轮 4 次槽位越界检查。
- **Lua 做法**（`lvm.c:214-265` 的 `forprep` + `1831-1848` 的 `OP_FORLOOP`）：入口算好
  **"首轮之外的额外迭代次数"** 存进寄存器，循环内只做「减一 + 判零 + 回绕加」——
  **不读上界、不判 step 正负**（边界/float 由入口一次处理）。
- **移植做法（不动指令编码、不动 codegen）**：`OP_FOR_PREP` 运行时若 start/end/step 全是 int48，
  就把 count 写进 **end 槽**（该槽是 codegen 给本循环分配的临时槽、循环内只有 `OP_FOR_LOOP`
  读它 ⇒ 复用安全；Lua 同样复用 `ra`）；非全 int 时把 end 槽**规范成非 int**（float / bigint，
  int48→double 无精度损失）⇒ `OP_FOR_LOOP` 见到 end 槽是 int 即走计数制，否则原样退回通用
  double/bigint 路径（语义不变）。排他式折成含上界：整数下 `x < end` ⟺ `x <= end-1`
  （下行 `x > end` ⟺ `x >= end+1`）。同时删掉循环内重复的槽位越界检查（`OP_FOR_PREP`
  已就同一批槽位号查过，槽位号全程不变）。
- **⚠ 口径坑（第一稿踩了）**：count 必须是"**首轮之外**的额外次数"（Lua 口径）。写成"总次数"
  ⇒ 每个循环多跑一轮（首稿实测 `i++` 结果 `10000001`、断言套件大面积 FAIL）。
- **实测**（同会话；before = 本改动前的二进制 `660b7ba`）：

  | 项 | before | after | 收益 |
  |---|---|---|---|
  | 空体 for | 159.2ms | 105.6 / 108.0ms | **−33%** |
  | for + `arr.add` | 362.6ms | 276.9 / 281.0ms | **−23%** |
  | 嵌套 for | 16.1ms | 13.3 / 13.5ms | −17% |
  | for + `i++` | 153.6ms | 136.6ms | −11% |
  | for `to var` | 138.5ms | 124.3ms | −10% |
  | while（对照，未改） | 187.9ms | 188.5ms | — |

  ⇒ 空体 for **105.6ms 已快于栈式 113.9ms**、数组添加 for **276.9ms 已快于栈式 344.4ms**
  （这两项此前分别落后 1.40x / 1.05x，是本轮"只剩 4 项落后栈式"里的两项）；
  对 Lua 空体 for 仍差 2.8x（38ms）。
- **语义变化（登记在案）**：计数制下**循环体给控制变量赋值不再影响迭代次数**（与 Lua 一致）；
  原先比较制下会提前/延后终止。断言套件 **358/358** 通过。
- **门禁**：断言套件 **358/358**；新增回归网 `examples/性能测试/for循环隔离微基准.leno`
  （空体 / 含上界 / 带数组添加 / 带自增 / 步长2 / 嵌套 / 负步长倒序）。

**⑤-j 结论（负）：K 常量操作数族**不划算** —— 静态计数会骗人**

- **动机**：Lua 5.5 有一整套 K 型常量操作数（`OP_ADDK/MULK/DIVK/IDIVK/MODK`、`OP_EQK/EQI`、
  `OP_SHLI/SHRI`，`lopcodes.h:261-315`），右操作数**直接从常量表读**（`lvm.c:1013` 的 `KC(i)`），
  不必先 `LOADK` 进寄存器。我们只有 int8 立即数。
- **先量后否**：对两个热基准 dump 统计「常量装载指令紧跟算术指令」的对数：
  光线追踪 **18** 对 / 全部测试 **59** 对（看着有 18–39% 的算术都能省一条）。
- **但分类后真相相反**：全部测试那 59 对里 **51 对是 `LOADK -> ADD`** —— 是 `print("..." + x)`
  这类**字符串拼接**（冷路径，且在打印行里）；真正热的只有光线追踪的 `LOADI -> MOD` **5 处**
  （`i % 3` 这类）加零星 `MUL_F/DIV_F`。
- **结论**：为 K 型新增 opcode（8 字节编码 + codegen 发射 + 反汇编）换不到 5–10 个热点，
  **不值得**；int8 立即数族（`*_INT_IMM`）已经覆盖 `x-1`/`x*3` 这类真正高频的写法。
  **别再用静态计数判断 K 型收益**——它会把你引向冷路径。

**⑤-k 字典写路径加调用点内联缓存（第 6 张 IC 表）—— ✅ 已完成**

- **动机**：`全部测试.leno` 里 `dict[key]=value` 我们 517ms、Lua 110ms（**4.7x**）。读代码定位到
  一次 `d[k]=v` 要穿 **5~8 层跨 TU 调用**：`dict_set` → `key_to_array_index` →
  `dict_find_slot` → `dict_hash_value` / `dict_key_equals`（→ `value_shallow_equal`）→
  `gc_write_barrier_obj` → `gc_write_barrier` ⇒ 实测 51.7ns/次（≈150 周期）。
- **做法**：VM 加第 6 张 IC 表 `InlineDictCacheEntry`（`dict` 指针 + `key` + `capacity` + `slot`）。
  命中前**必须**校验 `entries[slot].key == key` ⇒（a）槽位确实还是这个键（rehash / 键被删 /
  地址被复用都挡得住），（b）不必依赖 GC 保活缓存里的裸指针。写路径保留 GC 写屏障
  （与 `dict_set` 的"更新现有键"分支逐字一致）。
- **实测**（同轮、交替 3 轮取 min；`build/t_dict.leno`）：

  | 项 | 旧 | 新 | |
  |---|---|---|---|
  | `d[k]=v` 单键字面量 | 359.5ms | **264.3ms** | **−26%** |
  | `d[k]` 单键字面量 | 373.8ms | 370.1ms | 0.99x（未接，见下） |
  | `d[k]=v` 4 键轮换 | 547.6ms | 571.2ms | 1.04x |

- **⚠ 两个坑（都别再踩）**：
  1. **IC 挂错了 opcode**：第一版接在 `OP_DICT_GET`/`OP_DICT_SET` 上，但实测 dump 里
     `d[k]` / `d[k]=v` 走的是 **`OP_INDEX` / `OP_INDEX_SET` 的字典分支**（那两条 opcode
     的计数是 0）⇒ 白做。**改 VM 前先 dump 确认 codegen 到底发哪条。**
  2. **键轮换的调用点**：每次未命中都回填的话要白付一次 `dict_slot_for` 探测（实测 +13%）。
     加了"连续 3 次未命中就放弃"仍不够 —— 轮换键**每 N 次会命中一次**把计数清零，于是又继续
     回填 ✗。正解是 `dead` 标志**不可逆**（放弃后只多一次 load+compare，实测收敛到 +4%）。
- **⑤-l 读路径 IC —— ✅ 已完成（真因：编译期字面量不在内化表里）**：
  现象是给读路径接上同款 IC 后**反而慢 52%**（373.8 → 576.6ms）。
  **真因**：命中校验原先用 `entries[slot].key == idx` **逐位比较** —— 而**编译期字符串字面量
  根本没进内化表**：`intern_table_init()` 只在 `vm_init` 里调用（`src/vm/vminc/vm_init.inc:30`），
  而**编译发生在 VM 启动之前** ⇒ 源码里两处 `"k"` 是**两个不同对象** ⇒ 逐位比较永远为假
  ⇒ 一次都没命中，却每次 miss 都多付一次 `dict_slot_for` 探测（+52% 正是这个量级）。
  写侧之所以看着有效，是因为**同一个调用点**传进去的就是它自己写进去的那个对象 ✓。
  **修法**：新增 `dict_ic_key_match()`（`vm_helpers.inc`）—— 指针相等直接过（内化后最常见），
  两侧都是字符串时才比 `len/hash/memcmp`，其它键类型保守不命中；读侧 `OP_INDEX` 与写侧
  `OP_INDEX_SET` 统一用它 ⇒ **不依赖内化也能命中**，将来字面量若也内化，它自动退化成指针比较。
  （没有直接用 `value_shallow_equal`：那会把一大坨类型分支内联进最热的 `OP_INDEX` ✗。）
  **实测**（同轮、交替 3 轮取 min，`build/t_dict.leno`）：

  | 项 | 读侧 IC 之前 | 之后 | |
  |---|---|---|---|
  | `d[k]` 单键字面量 | 371.1ms | **300.6ms** | **−19%** |
  | `d[k]=v` 单键字面量 | 264.7ms | 270.7ms | 1.02x（不变） |
  | `d[k]=v` 4 键轮换 | 570.2ms | 554.3ms | 0.97x |

- **⑤-m 编译期字面量内化 —— ✅ 已完成（与 ⑤-l 同一根因）**：
  **为什么值得做**：⑤-l 的 IC 只在**命中**时省掉探测；一旦 IC 未命中（键在轮换、调用点多、
  或抖动出槽位）仍要付全价探测，而探测里 `dict_key_equals` 是 **memcmp**（字面量不在表里
  ⇒ 与任何运行时字符串都不是同一对象）✗。内化后探测本身也退化成指针比较 ✓。
  **现状证据**：
  - `intern_table_init()` 只在 `vm_init` 里调用（`src/vm/vminc/vm_init.inc:30`），而**运行路径
    的编译发生在 `vm_init` 之前**：`main.c:536` parse → `main.c:662-663` 才
    `gc_init()+vm_init_with_scope()` ⇒ 源码字面量**全部没进表** ✗（`-c` 路径恰好相反，
    `main.c:763-764` 先 `vm_init` 再 parse ⇒ 那条路径的字面量是内化的）。
  - ⇒ ① 同一字面量出现两次是**两个对象**（⑤-l 读侧 IC 逐位比较永不命中就是被它坑的）；
    ② 字面量与运行时字符串比较、`dict[字面量键]` 的**每次探测**都要 memcmp。
    Lua 是 **parse 期就内化短串**（`lstring.c:240-263` `internshrstr`）⇒ 字符串相等 = 指针比较。
  **施工步骤**：
  1. `src/string_table.c: intern_table_init()` 加**幂等**护栏（已就绪即 return）——
     否则提前初始化后 `vm_init` 再调一次会重建表，把已内化对象从表里**孤立**出去 ✗。
  2. `src/object/object_string.c: str_new()` 里**懒初始化**兜底（表未就绪则 `intern_table_init()`）
     ⇒ parse / codegen / `.lenb` 反序列化 / REPL 所有入口一处覆盖，不必逐处改 main；
     长字符串走 `str_new_nointern`，不受影响。
  3. 跨"编译-运行"轮次的悬垂条目：已确认 `gc_free_all()` 会清表（`gc.c:2107` 调
     `intern_table_free()`）✓；free 后 `capacity=0`，懒初始化可再次生效 ✓。
  **预算的验证与度量**：断言 358/358；字典差分脚本逐行一致；微基准取"探测变便宜"的两处
  —— `d[k]=v` **4 键轮换**（IC 已永久放弃 ⇒ 全靠探测，当前 554.3ms）与**字面量对运行时串
  比较**新循环；光线追踪三 Phase 校验和 + 耗时；植物大战僵尸回归。
  **风险**：只改变"同内容字符串是否同一对象"，语义上应无可观察差异（有断言全套 + 差分脚本
  兜底）；参考实现（Lua）就是 parse 期内化的口径。

  **实测（同轮交替多轮取 min，`build/t_str_intern.leno`，各 1 千万次）**：

  | 项 | 内化前 | 内化后 | |
  |---|---|---|---|
  | `str_eq`（字面量 vs 字面量） | 313.3ms | 304.3ms | −3% |
  | **`d[k]` 4 键轮换读**（读侧 IC 已永久放弃 ⇒ 全靠探测） | 568.5ms | **487.9ms** | **−14%** |
  | `d[k]=v` 4 键轮换写 | 567.6ms | 562.0ms | −1% |

  4 键轮换读三轮不重叠（568/570/579 → 488/488/494）✓ 这是本项的**主要收益**：探测里的
  `dict_key_equals` 由 memcmp 变指针比较。**全量测试**（`examples/性能测试/全部测试.leno`
  36 行计时）无回退：最差 1.06x、多数 1.00x。
  **门禁**：断言套件 358/358；字典差分脚本逐行一致；光线追踪校验和逐位不变
  （`sum=50730000.000273108`、`hits=900000/900000/585500`）。

- **⚠ 测量方法学（本次踩到的坑，务必沿用）**：光线追踪 Phase B（3M 次「方法 + 每次构造一个
  `Hit`」）在新旧二进制之间**稳定差 +6~7%** ✗，而**不分配**的 Phase B2 反而 −5%、Phase A 持平。
  查证结论：这**不是**内化的算法成本 ——
  - Phase B 热循环里**零字符串操作**（`OP_STRUCT_INIT` 的类型解析走的是按名字哈希的 IC，
    不是字符串比较；字段名→索引在编译期已定），内化只改变"字符串是否同一对象"，无从影响它；
  - **同一个二进制**跑 14 次，Phase B 自己就横跨 1369–1540（10%）✗；B2 单轮可差 19%（775→925）✗；
  - 方向还不一致（B 慢、B2 快）✗ —— 这是"构建布局/地址对齐彩票"的典型特征（新增代码改变了
    函数与数据的摆放），与算法无关。
  ⇒ **这类"分配型 / 大堆操作型"基准，单轮或少量轮次的 A/B 不可信**；要用**多轮交替 + min 或
  中位数**，并且要意识到"换一次构建"本身就能带来 ±5~10% 的漂移。相对稳固的判据是
  **微基准里量级更大、多轮不重叠的差**（如上面 −14%）与**机制上说得通的差**。
- **门禁**：断言套件 **358/358**；字典差分脚本 `build/t_dict_correct.leno`
  （单键反复写读 / 多键触发 rehash / 读回 / 重写 / 同一个调用点操作两个 dict / 整数键走数组部分 /
  对象值考写屏障）与旧二进制**逐行一致**。

**⑤-n 两项指令数削减（无返回值函数末尾的 `LOADNIL`；Array 立即数下标）—— ✅ 已完成**

两项都是「**减少指令条数**」而不是改结构 —— 与 ⑤-g 的结论一致（调用路径没有单点"肥肉"，
可动的是条数）。

1. **无返回值函数末尾不再发 `LOADNIL`**（`codegen_func.c` 的补 return、`codegen_stmt.c` 的裸 `return`）：
   `OP_RETURN` 在 `nresults == 0`（B 字段 = 1）时本就返回 null，与
   「`LOADNIL` + `RETURN(nresults=1)`」**结果完全相同**，但空体函数从 2 条降到 1 条
   （`func f() {}` 现在只有一条 `RETURN A=0 B=1`）。
   ⚠ `reg_alloc/reg_free` **保留** —— `max_reg`（→ `local_count` → locals 数组大小与 GC
   扫描范围）的记账必须与改动前逐位一致，不能顺手把槽位也省掉。
   **实测**（`全部测试` 5 次取 min）：空函数调用 **170.0 → 156.8ms（−7.8%）**，双向 A/B 同向。

2. **新增 `OP_INDEX_ARRAY_IMM`（iABC，C = 无符号 0..255 立即数下标）**：
   `arr[0]` 原是「`LOADI tmp,0` + `OP_INDEX_ARRAY_INT`」两条派发 —— 那个临时寄存器纯为凑一个
   寄存器操作数（Lua 的 `GETI` 把小下标直接编在指令里）。现在一条：
   `OP_INDEX_ARRAY_IMM A=7 B=1 C=0`（循环体 3 条 → 2 条）。
   与 `OP_INDEX_ARRAY_INT/_FLOAT` 是**同一份逻辑**（那两个 handler 本来就逐字相同）⇒ 一条指令
   同时服务 `Array[int]` 与 `Array[float]`；越界处理与寄存器版**完全一致**
   （同一个无符号比较 + 同一句报错文案）。opcode 追加在枚举**末尾**
   ⇒ 既有编号不动、旧 `.lenb` 仍可执行；4 字节定长、无附加数据。
   **★ 下标宽度：为什么取无符号 0..255 而不是 int8（口径对齐 Lua）** —— 第一版写的是
   `int8_t`（−128..127），复核 Lua 源码后改掉：
   Lua 5.5 的 `OP_GETI` 用 `int c = GETARG_C(i)`（`SIZE_C = 8`、`MAXARG_C = 255`，**无符号**），
   而发射判据 `lcode.c:isCint()` 是 `l_castS2U(ival) <= l_castS2U(MAXARG_C)`
   —— 先转无符号再比 ⇒ **负字面量下标直接落回寄存器路径**（`VINDEXED` → `OP_GETTABLE`），
   `GETI` 从不确定表示负数。对 Leno 而言负半边更是纯浪费：`arr[-1]` 在本语言里**必然越界**
   （handler 里同一个无符号比较恒假）⇒ 用立即数只是把"注定报错"的路径省一条指令，
   却把 **128..255** 这段真会用到的（查找表 / 定长数组）挤回两条指令。
   改成 [0,255] 后边界实测：`arr[0]/[2]/[127]/[128]/[255]` 全部一条 `OP_INDEX_ARRAY_IMM`
   （C = 0/2/127/128/255），`arr[256]`、`arr[-1]`、变量下标照旧回退 `LOADI + INDEX_ARRAY_INT`
   —— **回退路径行为逐位不变**，只是 128..255 那一段从"两条"变"一条"。
   ⚠ 与 ⑤-j 的"K 常量操作数族"负结论**不矛盾**：那条讲的是"把常量编进指令以省一条 `LOADI`"
   在**字典/字符串键**上不划算（要过常量表 + 判型）；这里是「**静态已特化的数组 + 整数下标**」，
   省掉的是纯 `LOADI` 且保留了跳过判型的快路径 ⇒ 两头都赚。
   **实测**（同轮双向 A/B，`全部测试` 5 次取 min）：`arr[index]` **50.7 → 35.8ms（−29.4%）**，
   反向复测 −25.9%，两次同向不重叠。

- **⑤-n 的负结论：handler 序言里的 `GET_CURRENT_LINE()` 不是瓶颈（别再改）**。
  各算术 handler 开头都无条件算一次 `int line = GET_CURRENT_LINE();`，而
  `frame->chunk->lines[…]` 是 **3 次依赖加载**，看起来该下沉到错误分支。做法：把
  `GET_CURRENT_LINE()` 宏**临时**改成常量 `(0)` 出一个探针二进制，与正常构建做 36 项全量 A/B
  —— **差异方向随测量顺序翻转**（正常构建先跑：探针慢 1~6%；探针先跑：探针快 1~7%）⇒ **中性**。
  机理与 ⑤-c 已排除项 3（缓存 `frame->locals` 为局部变量、中性）同源：这段在**关键路径之外**，
  `-O2` + 乱序执行已经吃掉它 ⇒ **已回退，不做**。
  **连带影响**：⑤-c「剩余嫌疑 3」里的"`frame->ip` 经结构体字段访问"据此**进一步降权** ——
  同族手法（把指针从结构体搬进局部变量）已被**两次实测**判为中性。

- **顺带观察（按本文档的测量方法学，判为布局彩票，勿据此下结论）**：本轮 `fib 经典递归` 与
  `while 空` 在两个方向的 A/B 里都稳定快 ~5%，但这两项与本次改动**无逻辑关联**（无数组下标、
  无空体函数）⇒ 属于 ⑤-m 末尾记的"换一次构建 = ±5~10% 地址对齐漂移"，**不计作收益**。

- **门禁**：断言套件 **358/358**；`--debug` 可见 `arr[0]` 已走 `OP_INDEX_ARRAY_IMM`、
  空体函数只剩一条 `RETURN`。

**⑤-o 调用路径第一步：`INLINE_LOCALS_MAX` 32 → 64 —— ✅ 已完成**

调用路径是唯一 2x 级缺口（微基准 2.27x）。**先量再改**：写探针量「每次调用净成本」随
**帧规模**的变化（同一条循环、同一份计数，只换被调函数的槽位数；基线 `for 空体` 已扣）：

| 被调函数 | 槽位 | 帧内指令 | 每次调用净成本 |
|---|---|---|---|
| `loc0`（空体） | 1 | 1 | **13.0ns** ← 纯 call+return 固定成本 |
| `arg5`（空体，5 参） | 6 | 6 | 14.3ns |
| `loc4` | 6 | 8 | 24.1ns |
| `loc16` | 18 | 32 | 70.3ns |
| `loc32` | **34（>32 ⇒ malloc）** | 64 | **160ns** |

- **纯 call+return 固定成本 13.0ns**，与 `全部测试` 里算出的 12.98ns **完全吻合**
  （Lua 侧 4.9ns ⇒ 差 8.1ns）。与 **⑤-g** 的 rdtsc 分段结论一致：这 13ns **没有单点"肥肉"**。
- **关键发现**：`func->local_count` 是**寄存器高水位**（`gen->max_reg`，含表达式临时寄存器），
  不是"声明的局部变量个数" ⇒ **比直觉更容易越过 32**。实测语料：
  `光线追踪对象版.leno` 的 **`trace` = 37 槽**、`全部测试` 的 **`main` = 44 槽** ——
  恰好落在"32 不够、64 够"区间，**每次调用 malloc+free（约 30ns）**。
  这正是当年把 8 提到 32 的那类函数（`trace` 就是光追的主递归），只是门槛又涨过去了。

**动作**：`INLINE_LOCALS_MAX` 32 → 64（`src/include/leno_vm.h`）。**实测（同机同轮、双向 A/B）**：

| 场景 | 改前 | 改后 | 变化 |
|---|---|---|---|
| 大帧深递归（34 槽 × 40 层 × 20 万轮） | 1515ms | 1024ms | **−32.4%** |
| `loc32` 微探针（34 槽平调用 300 万次） | 487ms | 425ms | −12.7%（每次 −20.7ns）|
| 光线追踪 Phase C（`trace` 37 槽） | 1691.4ms | 1665.6ms | −1.5% |
| 小帧深递归（对照） | 187.8ms | 188.9ms | +0.6%（中性）|
| 光追 Phase A / B / B2 | — | — | ±2%（中性）|
| `全部测试` 36 项 | — | — | 全在 ±4.6% 内（多数 ±2%）|

**代价**：`CallFrame` 384 → 640 字节。帧池是 realloc 增长、按**历史最大深度**算（100 层 = 38→64KB），
且只有**被触到的**帧影响缓存 ⇒ 上表未测到回退。
**门禁**：断言套件 358/358；示例冒烟 15/15。

- **顺带量到、但登记不做的两件事**（都低于或接近本机分辨率上限，且动之风险不成比例）：
  1. **整帧 null 初始化**：`call_reg_impl` 每次都把 `locals[k+arg_count .. local_count)` 清成 null
     （Lua 不做，靠 codegen 自己 init）。消融实测 **≈0.32ns/槽**：`loc32`（34 槽）省下
     **11ns/次（约调用成本的 7%）**，`loc16` 约 2~5ns。**为什么不能直接删**：帧槽是**复用**的，
     上一次调用的残留值可能是指向**已回收对象**的指针，GC 扫描到就是 use-after-free ⇒
     要删必须先有 codegen 的「**确定赋值**」证明（保守分析，任何存疑就不置标志），
     风险与收益不成比例 ⇒ **登记备查，暂不做**。
  2. **`arg1`/`arg5` 那两项测得极不稳**（arg1 三次 56/39/42ms）⇒ 探针里这类小项
     **必须多轮取 min**，单次读数不能采信。

**⑤-p 写路径直取变量寄存器（`arr[i] = v` / `obj.f = v`）—— ✅ 已完成**

**动机**：按 ⑤-n 的结论继续做"**整条指令削减**"（唯一被证明有效的杠杆），方法是
**反汇编真实负载的热循环、逐条找可以整条删掉的**。这次挑了排序基准的 `partition`
（原地快排的热点）。

**发现**：`arr[i] = v` 每次要付 **2~3 条 `OP_MOV`** —— 因为 `codegen_stmt.c` 的写路径
（`gen_index_assign`、`gen_assign` 的 AST_INDEX/AST_INDEX_ASSIGN/AST_FIELD_ACCESS 分支）
一律 `gen_expr(接收者)` / `gen_expr(下标)` / `gen_expr(值)`，各产生一条搬运。
而**读路径早就做了"直取变量自己的寄存器"**（`AST_INDEX` 里那段 `obj_is_temp`）——
写路径只是**漏了同一个手法**。`INDEX_SET A B C` 的 A/B/C 本来就是任意寄存器 ⇒ 可省。

`partition` 内层循环（`if arr[j] <= pivot { 交换 arr[i],arr[j]; i++ }`）：

```
改前（14 条/轮）                          改后（10 条/轮）
INDEX_ARRAY_INT  ; arr[j]                 INDEX_ARRAY_INT  ; arr[j]
LE_INT                                    LE_INT
JMP_IF_FALSE                              JMP_IF_FALSE
INDEX_ARRAY_INT  ; temp = arr[i]          INDEX_ARRAY_INT  ; temp = arr[i]
MOV  arr → 临时                            INDEX_ARRAY_INT  ; arr[j]
MOV  i   → 临时                            INDEX_SET        ; arr[i] = arr[j]   ← 原来 4 条
INDEX_ARRAY_INT  ; arr[j]                  INDEX_SET        ; arr[j] = temp     ← 原来 4 条
INDEX_SET                                  INC
MOV  arr → 临时                            FOR_LOOP
MOV  j   → 临时
MOV  temp→ 临时
INDEX_SET
INC
FOR_LOOP
```
⇒ 函数代码 176 → **120 字节**，内层 **14 → 10 条**（−29%）。

**⚠ 安全性：写路径比读路径多一条约束（这是本次的核心难点）**
- 读路径的论证照旧：`OP_INDEX_SET` 是"先读 R[A]/R[B]/R[C]，再写数组元素"，
  即使三者是同一个寄存器也安全（写的是数组内部，不是寄存器槽）。
- **新增约束**：值的求值发生在**派发 `INDEX_SET`/`SET_FIELD` 之前** ⇒ 若右值会改写
  「下标 / 接收者」所绑的局部量，直取就会读到**新值**：
  `arr[i] = f()`，而 f 内部把 i 改了 ⇒ 直取版写 `arr[新 i]`、快照版写 `arr[旧 i]`。
- 修法：新增 `ast_may_write_slot()`（`ast_may_read_slot()` 的写侧对偶）：认得的节点精确递归，
  **含调用/赋值一律返回 1**（闭包可能改写捕获的局部量，编译期看不出来）⇒ 回退到
  "先搬进临时寄存器"的老路径，只多占一个寄存器，语义不变。值那一侧只对**裸变量**
  （`AST_VAR`，无副作用）直取。

**实测（同机同轮双向 A/B，用 `git stash` 出基线 exe，避免跨构建漂移）**：

| 场景 | 改前 | 改后 | 变化 |
|---|---|---|---|
| 原地快排 500 / 5000 / 20000 / 100000 / 400000 | — | — | **−12.3% / −14.1% / −15.4% / −14.4% / −12.7%** |
| 优化快排 500 / 5000 / 20000 / 100000 / 400000 | — | — | −5.8% / −5.4% / **−20.1%** / −12.1% / −6.6% |
| 冒泡 500 / 5000 | 41,793 / 260,084µs | 37,551 / 251,666µs | −10.2% / −3.2% |
| 原始快排（分配密集，写数组不占主导） | — | — | ±5% |
| `全部测试` 36 项（回退面） | — | — | 全在 ±3.6% 内（多数 ±1.5%）|
| 光线追踪 Phase A/B/B2/C（回退面） | — | — | ±1%（Phase A 是字段**读**，写路径不热）|

**门禁**：断言套件 **359/359**（新增 `assert/test_write_path_direct_reg.leno`）+ 示例冒烟 15/15。
- ⚠ 新用例按本文档的规矩做了"**必须能红**"的验证：把 `ast_may_write_slot()` 临时改成
  恒返回 0（等于关掉 guard）后重建，该用例报 **6 处失败**（命中"右值改写接收者"两例）⇒
  不是空用例。
- 备注：用例里的「右值改写下标」那一例（case 4）在**关掉 guard 时也没红** ⇒ 说明该形态下
  codegen 本来就没走直取（大概率被别的判据挡住）。仍保留它作为正确性检查，但**不要**
  把它当成 guard 的守门用例。

**⑤-q `while` 回边融合（每轮省一条 `JMP`）+ 顺手修掉 `while` 里 `continue` 的静默错值 —— ✅ 已完成**

**起因**：查 ⑤-p 之后的剩余项时回到 `while` —— 它不是"每条指令更贵"（那点早被证伪），
而是**每轮多一条控制指令**：`CMPJMP_false(cond) → exit` + `<body>` + `JMP head`。

**① 顺手发现的 bug（静默错值，优先修）**：`gen_while` **收集了 `ctx.continue_jumps` 却从不
`patch_jmp_to` 回填**（`continue_target` 这字段写了没人读；`for`/`for_iter` 都会回填）⇒
那条 `OP_JMP` 的偏移停在 **0**，`frame->ip += 0` 等于**不跳** —— `continue` 之后的语句照样执行。
- 实测：`while i < 10 { i++; if i == 5 { continue }; s += i }` 得 **s=55（应为 50）**。
- **为什么一直没被发现**：套件当时 359 项全绿 —— `continue` 的用例（`test_control_flow`、
  `test_for`）**全在 `for` 里**，`while` 这一格零覆盖。
- 修法：在 `gen_while` 里回填（目标 = 回边那次条件重测）。新增用例
  `assert/test_while_continue.leno`（5 组判据：融合路径 / **复合条件走回退路径** / 嵌套只作用于
  内层 / break 与 continue 混用 / continue 在体末）。
- **"用例必须能红"的零构建验证**：直接拿仓库里改动前的 `build/leno_old.exe`（`b49ada9`）跑新用例
  ⇒ 报 `断言失败: 期望 50, 实际 55` ✓ 不是空用例（**比临时回退再重建省一次构建**，值得沿用）。

**② 回边融合（本次的性能改动）**：把条件的重测挪到循环体之后，复用「比较 + 条件跳转」指令：

```
原形态（每轮 2 条控制指令）              现形态（每轮 1 条）
head: CMPJMP_false(cond) → exit          entry: CMPJMP_false(cond) → exit
      <body>                             body:  <body>
      JMP head        ← 白付一次派发           CMPJMP_**true**(cond) → body
exit:                                    exit:  （fall through 即退出）
```
- **`CMPJMP` 一族原本只有"为假则跳"**（4 条指令全是），回边要的是"真则跳"。
  **没有新增 opcode**：`C` 字段本来就只当标志用（bit7 = 立即数），而 **bit6 在寄存器模式与
  立即数模式下都是空闲的** ⇒ 用它当极性位（`0x40`）。既有字节码语义完全不变。
- **语义等价性**：条件求值次数不变（入口 1 次 + 每轮 1 次）；`break` → exit 不变；
  `continue` → 回边那次重测（与"跳回循环头重测"等价，只是少走一条 `JMP`）；
  融合不成立时（复合条件、非 int 比较）回退成 `cond + JMP_IF_TRUE body`，仍比原形态省一条。
- **反汇编同步**：`debug.c` 现在按极性照实打印「为真则跳 / 为假则跳」——
  否则会照旧印成"为假则跳"，让人按反汇编反推错语义（文档里记过"按名字查错指令、白折腾"的坑）。
- **⚠ 自己挡掉了一个"改动引入的静默错值"**：`CMPJMP` 的偏移是 **16 位带偏置**
  （`patch_common` 对超范围是**截断到边界**而非报错），而原形态的回边是 `OP_JMP`（24 位、8MB 内安全）
  ⇒ 换成融合回边后，**>32KB 的循环体会静默跳错**。已加尺寸闸门：偏移不够时就回退到
  `cond; JMP_IF_FALSE → 跳过; JMP body`（24 位）—— 宁可多一条也不静默跳错。

**实测（同机同轮双向 A/B，`git stash` 出 `3db6a69` 基线；两个方向都跑了）**：

| 场景 | 改前 | 改后 | 变化 |
|---|---|---|---|
| `while` 空 / i++ / #3（各 1 千万次） | 62.5 / 63.0 / 62.6ms | 52.4 / 52.5 / 52.0ms | **−16.2% / −16.6% / −16.9%** |
| `while` + arr.add | 124.3ms | 119.0ms | −4.3% |
| 嵌套 `while` + `while` | 7.27ms | 6.20ms | −14.7% |
| **排序基准·优化快排**（内层是 `while` 的插入排序）5 档规模 | — | — | **−17.2% / −22.0% / −23.1% / −17.9% / −17.3%** |
| 排序基准·原地快排 / 冒泡（**都没有 while**） | — | — | 0 ~ ±2%（漂移；500 那档是 Lua 量化过的噪声）|

⚠ **回退面判读的方法学（本轮又踩到一次，务必沿用）**：第一遍 A/B 里 `a = b + c`、`i++`、
`for 空体` 这些**完全不含 `CMPJMP`** 的项也一起"慢 2~6%"，乍看像是极性位拖累了共用指令；
**对调 A/B 顺序后那些"回退"全部反向**（变成 −3~−7%）⇒ 是**整批统一漂移**，不是改动引起的。
**怎么一眼看穿**：拿一个"机制上不可能受本改动影响"的项当**对照**（这里是 `a = b + c`，
它连 CMPJMP 都不执行）—— 对照项动了，就说明这一轮不能用。

**门禁**：断言套件 **360/360**（新增 `test_while_continue.leno`）+ 示例冒烟 15/15。

> **登记（未修，留给后面）：`patch_common` 对超范围跳转偏移是静默截断**（`bx` 夹到 [0,0xFFFF]）。
> 这意味着**既有的** `if` / `while` 入口跳转在"单个分支体 ≥32KB 字节码"时会**静默跳错**
> （约 8000 条指令的体量才会触发，未见实际样例，但属于"静默错值"类）。正解是让
> `patch_common` 在溢出时报**编译期错误**而不是夹断 —— 见待办总表 **T12**。

**⑤-u 修掉一个堆损坏崩溃：模块 chunk 的动态 locals 被双重释放（T15）—— ✅ 已完成**

**现象**：`leno_gui/应用/Trae签到/trae_gui.leno` 崩溃 —— 退出码 **`-1073740940`（0xC0000374
STATUS_HEAP_CORRUPTION，堆损坏）**、**零输出**、100% 可复现。
⚠ **第一步永远是先排除回归**：拿改动前的 exe（`b49ada9`）跑同一文件 ⇒ **同样崩溃**
⇒ **不是当天改动引入的**，是既有问题（省下了整轮 bisect）。

**定位（顺序值得复用）**：
1. `--debug` 能打出 98213 行反汇编 ⇒ **编译没问题，崩在运行期**。
2. 最小 SDL3 例（`leno_gui/控件/画布/test_canvas_tab.leno`）跑得起来 ⇒ 不是环境/SDL3 模块。
3. **最小 import 探针**（不用重建、最省力的缩小手段）：`import "SDL3"` 正常，
   `import "trae_core.leno"` **单独就崩** → 再拆 `web_net` / 内置模块 / `trae_crypto.leno`
   ⇒ 锁到 **`trae_crypto.leno`**（42 行）。
4. 该模块初始化只做两件事：`import "Crypto"` + 两个 64 元素数组字面量。造 `c1/c2/c9/c10` 二分：

   | 用例 | 模块 chunk 槽位 | 动态 locals？ | 结果 |
   |---|---|---|---|
   | c1：只 import（无数组） | 小 | 否 | 正常 |
   | c9：62 元素数组 | **64** | 否（内联） | 正常 |
   | c10：63 元素数组 | **65** | **是（malloc）** | **崩** |

   ⇒ 与"**走 malloc 的动态 locals**"绑定，**与数组大小无关**。
5. 交叉排除"动态 locals 路径整体坏了"：普通函数 71 槽（`p_bigfunc`）、函数内 100 元素数组
   （`p_farr100`）**都正常** ⇒ 只有**模块初始化帧**中招。
6. **哨兵诊断**（动态 locals 尾部多留 8 槽写标记、free 前检查）：**标记完好**
   ⇒ 否掉"写越界到数组尾"，转向**释放路径**。
7. **gdb + `-g -O0` 拿崩溃栈**（MinGW 无 libasan，实测 `cannot find -lasan`）：
   ```
   #6 ucrtbase!_free_base
   #7 vm_run_chunk (...) at src/vm/vminc/vm_init.inc:302   ← free 时撞出堆损坏
   #8 vm_run at src/vm/vminc/run/10_vars_module.inc:250    ← 模块导入
   ```

**根因：双重释放。**
- `REG_FINISH_RETURN`（`run/01_prologue.inc`）的**三个分支各自都 free 了**动态 locals
  （236 / 246 / 265 行）—— 这就是"谁拥有这块内存"的答案：**RETURN 路径负责释放**。
- 而 `vm_run_chunk`（模块初始化帧的收尾）返回后又 `if (dyn) free(locals);`
  ⇒ **第二次释放** ⇒ 堆损坏，崩点正是这一行 ✓
- **为什么长期没暴露**：只有「模块 chunk 槽位 > `INLINE_LOCALS_MAX`」才走 malloc：
  内联帧 `locals_is_dynamic = 0` ⇒ 两条路径都不 free（不崩）；普通函数帧走 265 行那一支，
  只 free 一次（不崩）。真实触发者：`trae_crypto.leno` 模块级两个 64 元素数组 ⇒ 模块 chunk **66 槽** ✓

**修法**（`src/vm/vminc/vm_init.inc`，+9/−1）：宿主的释放改成"**只在这个帧还挂在栈上时才释放**"：
```c
if (dyn && vm.frame_cnt > prev_frame_cnt) {        /* 正常 return ⇒ RETURN 已释放，跳过 */
    CallFrame* own = &vm.frames[prev_frame_cnt];   /* 按索引重取：vm_run 期间 vm.frames 可能已 realloc */
    if (own->locals == locals) { free(locals); own->locals = NULL; own->locals_is_dynamic = 0; }
}
```
- 正常 return：`frame_cnt == prev_frame_cnt` ⇒ 跳过 ✓ 不双重释放；
- 错误/异常退出（帧还挂着）：释放 ✓ 不泄漏（异常展开路径 `09_exception.inc` 另负责它自己那些帧）。
- ⚠ **没有**改 `REG_FINISH_RETURN`：那一支同时服务 `vm_call_value`（原生→Leno 回调），
  那里的动态 locals **必须**由它释放 —— 动它会引入泄漏。判据宁可加在宿主侧。

**验证**：
- 原始应用：退出码 **0**，输出 `[gui] 首帧渲染 ok  日历=540.0x300.0  2026 年 9 月  账号=TRAE SOLO CN/cheng8214`、
  `[gui] frames=30  accounts=2 ... 已签到日=5  状态=离线模式…` ✓
- 六个复现用例（c9/c10/crypto/hist/core/all）全绿 ✓
- 新增回归用例 `assert/test_module_dyn_locals.leno` + `assert/helpers/big_chunk_module.leno`
  （故意用 **200** 元素而不是"刚过 64"：将来调大 `INLINE_LOCALS_MAX` 也不会绕过这条路径）。
  ⚠ 按规矩做了"必须能红"的**零构建验证**：改动前的 `leno_old.exe` 跑该用例 ⇒
  退出码 `-1073740940`、零输出 ✓ 判别力确认。
- 门禁 **362/362** + 冒烟 15/15。
- 顺手核查同类风险：协程挂起/恢复的 `saved_frames` 所有权链（`13_async_ffi.inc` 转出 /
  `vm.c:1488` 转回 / `gc.c:1585` 回收）**自洽**（注释明写"所有权转给/转回"）⇒ 不构成同类双重释放 ✓。

**⑤-t 位运算/移位的**常量化**（T14）—— ✅ 已完成**

**起因**：`examples/性能测试/bench_bitwise_loop.leno`（对齐 Java 的位运算跑分）实测
**Leno 慢 Lua 1.96x**（2 亿轮：Leno 3002ms / Lua 1532ms，结果两边都是 1198）。
- 该基准原先写 20 亿轮（Lua 侧要跑上百秒），已改成 **2 亿**并新增 `.lua` 对照
  （`.leno` 的 `for 0 : N to i` 里 `:` 是**闭区间**，Lua 的 `for i = 0, N` 同样闭区间 ⇒ 轮数严格一致）。

**根因（反汇编）**：每轮 **10 条**，其中 **4 条 `LOADI` 是纯搬运** ——
`inp & 7` 必须先 `LOADI r,7` 再 `OP_BITAND r`，而且**每轮重装同一个常量**：

| | 每轮字节码 |
|---|---|
| Leno | `LOADI 8` +SHR、`LOADI 7` +BITAND、`LOADI 1125` +BITXOR、`LOADI 1135` +BITAND、`ADD_INT_IMM`、`FOR_LOOP` = **10 条** |
| Lua 5.4+ | `SHRI`、`BANDK`、`BXORK`、`BANDK`、`ADDI`、`FORLOOP` = **6 条** |

算术家族早有 `*_IMM`（`ADD/SUB/MUL/LT/…_INT_IMM`），**位运算与移位家族一条都没有**。

**改法**：新增 6 条常量化指令 `OP_{BITAND,BITOR,BITXOR,SHL,SHR,USHR}_K`
（`A` = 结果寄存器，`B` = 左操作数寄存器，`C` = 常量索引；
`C` 为 0 或 >255 时紧随一条 `EXTRAARG` 携带 24 位索引 —— **与 `OP_CALL_NATIVE` 同一惯例**）。
- **为什么走常量表而不是 int8 立即数**：`x & 0xFFFF`、`x & 1135` 这类超 8 位常量同样常见
  （本例的 `1125`/`1135` 就在 int8 之外），常量表没有幅度限制。
- **不复制 handler**：把 6 个 op 的函数体抽成宏（`BITAND_BODY(CV)` …），
  **寄存器版与 K 版各实例化一次** ⇒"语义逐字同体"由编译期强制成立（唯一差异是右操作数
  取 `R[C]` 还是 `constants[C]`），bigint 免装箱快路径、移位量的边界判断等全部只有一份。
  仓库有《单一事实来源与重复实现收敛》待办 ⇒ 新指令不该再抄一份 handler。
- **codegen**（`gen_binary`）：右操作数是整数字面量 ⇒ 右值**不物化**，直接走 K 版。
  **可交换的 AND/OR/XOR 还支持字面量在左边**（`1125 ^ inp`）：常量放 `C`、变量放 `B`
  （交换律保证等价；被换的只有"字面量那一侧"，字面量无副作用、另一侧又限定为普通局部变量
  ⇒ 求值顺序不可观测）。位移类不可交换 ⇒ 只处理右操作数（移位量）。
- ⚠ **两处占位寄存器必须补**（`rl` 在"字面量在左边"时、`r` 在右值是字面量时）：
  与 `rhs_imm` 同一坑 —— 不补占位，收尾会去 `reg_free` 寄存器 0 ⇒ **整个分配器错乱**
  （文档里记过"单跑测试都过、runner 里 351 个用例全挂"）。
- opcode 追加在末尾 ⇒ 既有编号不动、旧 `.lenb` 仍可执行。

**实测（同机同轮 A/B，`git stash` 出基线，4 次取最小、交错）**：

| | 耗时 | 结果 |
|---|---|---|
| 改前 | 2782.4ms | 1198 |
| 改后 | **2270.3ms（−18.4%）** | 1198 |

- 指令数 **10 → 6 条/轮（−40%）**，与 Lua **持平**；与 Lua 的差距 **1.96x → 1.48x**。
- 结果 8 次全是 `1198`（与 Lua / Python 版一致）。

**⚠ 记一笔反直觉的账（供下一步参考）**：−40% 的指令只换来 −18.4% 的时间。
- 每删一条 `LOADI` 只省 **~0.64ns**（此前从 `a = 1` 反推的边际 ≈0.87ns）；
- 更关键：**K 版的单条均摊成本反而更高** —— 11.35ns/6 = **1.89ns/条**，而改前是 13.9ns/10 = 1.39ns/条。
- 怀疑是常量表访问链 `frame → chunk → constants → [idx] → Value`（4 级依赖加载）
  比 `LOADI` 的立即数写更贵。**候选**：像 Lua 那样把常量表基址在帧建立时缓存进局部
  （Lua 的 `k`）—— 但文档里"缓存 `frame->locals`"那次实测是**中性**（`-O2` 已缓存过一层），
  所以这条只是候选，**动前必须先量**。
- ✅ **2026-09-22 已量、结账（上面的猜测只占缺口约 1/4）**：新增隔离微基准
  `examples/性能测试/位运算常量表隔离微基准.leno` —— 用**同指令数**的两段循环把"取操作数"
  单独隔离：`x & 1135`（字面量）⇒ **1 条** `OP_BITAND_K`（取常量表）；
  `x & y`（局部变量）⇒ **1 条** `OP_BITAND`（取寄存器）。两者是同一宏体实例化，唯一差异是
  右操作数来源；`--debug` 反汇编核对过两条循环体都只有 1 条指令、寄存器布局一致、无 `EXTRAARG`。
  同机**交替 4 轮取 min**（偶数轮正序、奇数轮逆序，消冷启动与顺序偏置）：
  - 空体循环（纯循环开销）3.134 ns/轮；`x = 7`（`OP_LOADI`）**0.785 ns/条**；
    `OP_BITAND_K` **1.700 ns/条**、`OP_BITAND` **1.474 ns/条**。
  - ⇒ **常量表取操作数比寄存器贵 0.225 ns/条** —— 假设方向成立，但只占 K 版自身约 13%；
    每替换一组 `LOADI + 通用位运算`（→ 1 条 K 版）实际只省 **0.559 ns**
    （0.785 + 1.474 − 1.700，与上面 ~0.64ns 同量级），**不是**反推所用的平均值。
  - **缺口的真因是"按平均指令成本折算"这个模型本身**：以改前 3002ms/10 条 = 1.50ns/条 折算，
    4 条应省 ≈1200ms（3002 → 反推的 ~1800ms），实测只省 ≈447ms（→ 2565ms）。
    缺口 ≈764ms 拆开是：**~573ms（约 3/4）来自 `LOADI` 被按平均指令计价**（它本就是全 VM
    最便宜的指令之一，0.785ns），**~180ms（约 1/4）才是常量表访问链**。
  - **结论：不动代码**。缓存常量表基址的上限只值那 ~180ms（整个基准 ~7%），且要与 175 处
    帧重同步点挂钩 —— 与上面"缓存 `frame->locals` 在 `-O2` 下中性"属同一类低收益、
    高不变量风险的改动，按"动前必须先量、中性收益不引入新不变量"的口径**不做**。

**回退面**：36 项里唯一 ≥4% 的是 `for 空体`（机制上不涉及位运算）——**交错复测后降到 +1.8%**
⇒ 布局漂移（那次非交错测量的 +6.3% 是顺序偏置），不是回归。

**门禁**：断言套件 **361/361** + 示例冒烟 15/15。
bigint 位运算那批用例（`test_bigint_arith` 等）正是"免装箱快路径"的回归面，全过 ⇒ 宏化没动语义。

**⑤-t 运行期错误被残留 try handler 反复接住 ⇒ 挂死（死循环）—— ✅ 已修复（正确性优先）**

**怎么撞上的**：⑤-s 之后做 T11（null 不能参与运算）时，新写的运行期用例
`assert/test_null_arith_error.leno` **只有一条断言失败**，表现却是"跑几分钟没有任何输出" ——
不是用例慢，是**挂死**：`try { … } catch { … }` **之后**的运行期错误被那个 catch 反复接住，
控制流一次次回到 catch 段（每轮计数 +1），永不退出。

**最小复现**（除零就够；与 T11 **无关**，是**既有**缺陷）：

```leno
main() {
    var z = 0
    var c = 0
    print("A")
    try { var v = 1 / z } catch { c = c + 1 }
    print("B c=" + c)      // ← 只打印一次
    var boom = 1 / z       // ← try **之外**抛错
    print("C")
}
```
修复前：`A / B c=1 / B c=2 / B c=3 …` **永不退出**。

**根因：两处异常派发分叉**（`vm/vminc/vm_exception.inc` 的运行期错误派发 vs
`vm/vminc/run/09_exception.inc` 的 `OP_THROW`）：

| | `OP_THROW`（对） | 运行期错误（错，已修） |
|---|---|---|
| 找 handler | `catch_ip ?: prev_catch_ip`（认外层兜底） | 只看 `catch_ip`/`finally_ip` ✗ |
| 接住之后 | **注销所用 handler** ✓ | 不注销 ✗ |
| `finally` 相关 | `in_finally` 守卫 + `catch_finally_ip` 分支 | 都没有 ✗ |

第 ② 条是主凶：`} catch {`（**无变量**）根本不发 `OP_CATCH`（codegen 侧注释里记着同一件事），
所以"OP_CATCH 会清 `catch_ip`"这个假设对最常见的 `catch { … }` **从来不成立** ⇒ handler 永远挂着。

**为什么它比"一条用例慢"严重得多**：任何带 `try` 的用例**只要断言失败一次**，就会挂死而不是判红
⇒ 门禁退化成一堆 120s 超时，而**真凶（断言失败）完全看不到** —— 本次排查就被它误导了整整一轮。

**修法**：运行期错误派发与 `OP_THROW` 逐条对齐（`prev_*` 兜底 / 接住后注销 / `catch_finally_ip`
分支 / `in_finally` 守卫），并在两处都写明"必须同口径，别再一次分叉"。

**用例必须能红**：新增 `assert/test_runtime_error_after_try.leno`（3 判据：try 之后的运行期错误必须
报错退出 / catch 体内抛错要传给外层 catch / finally 体内抛错不得绕回自己）。拿改动前的
`build/leno_old.exe` 跑 ⇒ **31.4s 后判红**（输出停在 `B c=444…`）✓ 不是空用例。

**⚠ 顺带堵掉一个"把挂住伪装成通过"的坑**（`assert/helpers/compiler.leno`）：子进程 `_exec` 此前
**不传超时**（挂住就拖死整个套件），而 `expect_error` 的判据是 `exit != 0` —— **超时码 124 也是非 0**
⇒ 挂住会被当成"编译失败 ✓"**判绿**。已改成：带上界（30s）+ **124 单独判红**（报错文案带 tag，
直接点名是哪一支子程序）。⇒ 以后凡是"子进程挂住/超时"一律显式判红，不许冒充通过。

**门禁**：断言套件 **365/365**。

**⑤-u T11：`null` 不能参与算术运算 —— 编译期能定的提到编译期，其余运行期拦 —— ✅ 已完成**

**规则出处**：教程的运算符类型规则（「bool 和 null 不能参与算术运算」）与「值类型字段必须指定默认值」
一节（不写默认值的值类型字段初值是 **null**，参与算术会「**编译警告或运行时报错**」）。
此前只实现了一半：字面量报错、可空类型只给警告，而**特化指令把 null 静默读成 0**
（`val_as_int(NULL_VAL) == 0`，`TAG_NULL = 0`）⇒ 值类型字段那种最危险的写法反而**无声算 0**。

**按"编译期能确定就别拖到运行期"分两层**（作者拍板）：

| 形态 | 判定 | 结果 |
|---|---|---|
| `null + 1`（字面量） | 编译期 | **编译错误**（既有） |
| `var a = null; a + 1`（声明即 null、此后没被写过） | 编译期 | **编译错误**（新增） |
| `int? a = null; a + 1`（同上） | 编译期 | **编译错误**（新增，教程此前只承诺警告） |
| 未赋默认值的值类型字段 `s.x + 1` | ✗ 静态无解（静态类型就是 `int`，运行期才是 null） | **运行期错误**（新增） |
| nullable 被别处赋过值 / `s.x += 1` / `s.x & 3` … | 不确定 | **运行期错误**（新增） |

- 编译期那半：`Symbol.is_null_value`（声明时初始化为 `null` 字面量 ⇒ 置位；任何写操作
  （`a = v` / `a += v`）⇒ 清位），在 `semantic_type.c` 的二元 / 一元取负 / 复合赋值三处报错。
  **⚠ 判据是「宁漏勿误报」**：误报会让本来能跑的程序编译不过，比漏报严重得多 ⇒ 拿不准一律不报。
  专门的**误报守卫**在用例里（声明后赋过值、条件赋值、闭包内赋值、循环内赋值、同名遮蔽
  ⇒ 必须全部编译通过）。
- 运行期那半：特化算术/位运算指令的守卫从「非 bigint」改成「**两侧都是 int48**」（都是两次 tag 比较，
  快路径代价不变），null 走冷路径报错 —— 文案逐字取自通用路径与栈式，不另起炉灶。
- **兼容性影响（有意为之）**：仓库里两处用例的 `struct` 值类型字段原本没写默认值、恰好参与算术
  ⇒ 已补默认值（`test_call_arg_reg_reuse.leno` 的 `float aframe`、`test_cross_module_method_args.leno`
  的 `int x`）。这正是教程警告的那种写法，规则上线后它**必须**显式写默认值。
- 用例：`assert/test_null_arith_error.leno`（运行期那一半，14 组判据 + 对照组）、
  `assert/test_null_arith_compile_error.leno`（编译期那一半 + 误报守卫）。

**⑤-s `for 容器 to v` 迭代条件融合（T13）—— ✅ 已完成**

**起因**：⑤-q 收掉 `while` 之后，同类候选只剩 `for_iter` 的回边。一开始的疑虑是
"它的条件带内置 `len()` 调用"——**先查清再说**：反汇编显示条件不是原生调用，而是专有指令
`OP_LEN`（`for_iter` 的 codegen 自己发），所以"把原生调用换成专用指令"这条路**不存在**。

**真正的账**（同机实测，1e6 元素数组迭代 1e7 轮）：

| 循环形态 | 每轮固定开销 | 实测 |
|---|---|---|
| `for a to x {}`（空体） | **6 条**（LEN, LT, JMP_IF_FALSE, ITER_GET, INC, JMP） | **10.03 ns/轮** |
| `for M to i {}`（数值 for，对照） | 2 条（INC, FOR_LOOP） | **3.12 ns/轮** |

⇒ 容器迭代的每轮开销是数值 for 的 **3.2 倍**，这就是要收的东西。

**先否掉一个"看起来白赚"的改法：把长度提到循环外。**
- 先量现行为（改动前，`for a to x` 起于 `[1,2]`、每轮 `a.add(9)`）：
  **6 轮结束、最终 len=6** ⇒ 长度是**每轮重读**的，追加会把循环"撑长"。
- 栈式实现的 `gen_for_iter` 同样每轮 `GET_LOCAL obj; OP_LENGTH; OP_LT`
  ⇒ **两侧口径一致，这是有意语义，不是缺陷**。
- 提到循环外会静默把它变成 **2 轮** —— 属于**语义决策**（还牵扯"迭代中 `remove` 会不会变成越界"），
  不是代码生成优化，**不做**（要改必须两侧一起改 + 写进语言文档）。

**改法：新增一条融合指令 `OP_CMPJMP_ITER`**（语义等价，长度仍在每次测试时读）：
```
原先每轮: LEN, LT, JMP_IF_FALSE, ITER_GET, [体], INC, JMP
现在每轮: ITER_GET, [体], CMPJMP_ITER(先自增 + 比较 + 真则跳回)
```
- 编码：`A` = 索引寄存器，`B` = 容器寄存器，`C` 只当标志 —— **bit6 = 真则跳**
  （⑤-q 为 `while` 回边引入的那一位，这里直接复用）、**bit5 = 先自增索引再测试**；
  紧随 4 字节字的低 16 位 = 跳转偏移（与 `CMPJMP` 同约定）⇒ 8 字节。
- **opcode 追加在末尾** ⇒ 既有编号不动、旧 `.lenb` 仍可执行。
- 索引寄存器恒为 int（codegen 用 `LOADI idx,0` 初始化，此后**只有**这条指令写它，
  用户可见的索引变量是另一个槽）⇒ 无需类型检查；自增用 `VM_INT_RESULT`
  与 `OP_INC` 同口径（超 int48 提升 bigint）。
- **顺手收掉一处重复实现**：长度规则原先内联在 `OP_LEN` 里，现在抽成
  `vm_helpers.inc` 的 `static inline vm_container_len(Value)`，`OP_LEN` 与
  `OP_CMPJMP_ITER` 共用（仓库有专门的《单一事实来源与重复实现收敛》待办，
  新指令不该再抄一份；字典 `acount+count`、struct 字段数、enum 成员数这三支
  "缺了就会静默不进入循环"的坑，注释一并搬进 helper）。
- **尺寸闸门**：融合指令是 16 位偏移，循环体 >32KB 时回退成
  `INC + LEN + LT + JMP_IF_FALSE + 24 位 JMP`（与 ⑤-q/T12 同一手法，宁可多一条也不静默跳错）。

**实测（同机同轮 A/B，`git stash` 出基线；4 次取最小）**：

| 场景 | 改前 | 改后 | 变化 |
|---|---|---|---|
| `for a to x {}`（空体） | 98.60ms | **51.97ms** | **−47.3%** |
| `for a to x { s = s + x }` | 112.27ms | **75.13ms** | **−33.1%** |
| 数值 for（**对照**，机制上不受影响） | 30.52 / 63.75ms | 29.24 / 61.89ms | −4.2% / −2.9%（漂移）|

- 与指令数模型对得上：空体 6 条 → 3 条（理论 −50%，实测 −47%）。
- 每轮固定开销 **10.03ns → 5.20ns**；容器迭代相对最紧的数值循环（+下标读）
  差距由 **1.76x 收到 1.21x**。
- 对照项也"快了 3~4%"是熟悉的老毛病（布局/漂移），方向与本改动一致 ⇒ 只会让收益被低估。

**门禁**：断言套件 **361/361** + 示例冒烟 15/15。新增
`assert/test_for_iter_semantics.leno`（8 组判据：**迭代中追加撑长循环**、空容器不进体、
`break`/`continue` 绑定、带索引变量、字典键/值两路、枚举成员、嵌套内层 `continue`、
循环变量在体内被改写不影响下一轮）。
- ⚠ 与"修 bug"的用例如意不同：**这条用例在改动前、改动后的 exe 上都必须绿**
  （它锁的是**既有语义**，价值在于挡住将来"把长度提到循环外"那类静默语义漂移）。
  先在新旧两个 exe 上都跑过确认 ✓。

**⑤-r T12：跳转偏移溢出从「静默截断」改成「编译期报错」—— ✅ 已完成（正确性优先）**

**动机**：⑤-q 做 `while` 回边融合时自己踩到了这条 —— `patch_common` 对超范围偏移是
**截断到边界**（16 位编码把 `bx` 夹到 `[0,0xFFFF]`；`OP_JMP` 连夹都没有、直接按掩码写低 24 位），
于是生成一条"跳到别处"的指令，运行期行为错乱且**没有任何提示**。按本文档一贯判准，
"静默错值"比"报错"严重得多，所以整条清掉。

**前提验证（先证明它不是空想）**：造一个"单个 `if` 体 = 4 万字节"的程序
（`var x = 1; var y = 0; if x < 1 { y = y + 1 × 10000 }`，每条 4 字节 ⇒ 体正好 **40000 字节**）：

| exe | 现象 |
|---|---|
| 更早的 `b49ada9` | **直接崩溃**（exit `-1073740791` = fail-fast），零输出 |
| ⑤-q 那版（有截断、无检查） | 编译"成功"并执行 ⇒ 报运行期 `字段访问需要结构体对象`，**行号 271312790** |
| 反汇编器 | 自检印出 `[反汇编自检] OP_CMPJMP_LT 的跳转目标 32783 不在指令边界上` |

⇒ 三种表现都"没有任何有用诊断"（崩溃 / 垃圾错误 / 只有反汇编时才看得见）。

**实现**（`src/codegen/codegen_emit.c`）：
- 新增 `report_jump_overflow(gen, pos, offset, limit)`：从 `chunk->lines[pos]` 取行号
  （`lines` 是**按字节偏移索引**的，与 VM 的 `GET_CURRENT_LINE()` 同一张表），
  `error_add(ERR_SEMANTIC, line, msg)`。
- `patch_common` 三种编码各加一道范围检查 + 夹断兜底（32 位不溢出）：
  `OP_JMP` = 24 位有符号 ±8388607；`CMPJMP` 与 `iAsBx` = 16 位带偏置 ±32767。
- `patch_sbx_at`（`FOR_PREP` 前跳 / `FOR_LOOP` 回跳，同样是 16 位）同样检查。
- **报错能让编译真的停下**：`main.c:801` 在 `codegen()` 之后就有
  `if (error_has_any()) goto compile_fail;` ⇒ 不会去写 `.lenb`、不会执行，**退出码 -1**（已实测）。

**修后表现**（同一个 4 万字节程序）：
```
build\_big_if.leno(4,1): error: [语义错误] 跳转距离超出指令可达范围：需要 +40000 字节，
                                  该指令上限 ±32767 字节（单个分支或循环体的字节码过大，请拆成独立函数）
```
行号正指向 `if` 那一行、需要的字节数也是真实值 —— 照提示拆函数即可。

**⚠ 复现配方**（正则那条自检提示仍在，值得保留）：
```powershell
# 生成"单个 if 体 4 万字节"的探针（PowerShell，10_000 条 y = y + 1）
$sb = New-Object System.Text.StringBuilder
$sb.AppendLine("main() {"); $sb.AppendLine("    var x = 1"); $sb.AppendLine("    var y = 0")
$sb.AppendLine("    if x < 1 {")
for ($i=0; $i -lt 10000; $i++) { $sb.AppendLine("        y = y + 1") }
$sb.AppendLine("    }"); $sb.AppendLine('    print("y = " + y)'); $sb.AppendLine("}")
Set-Content build\_big_if.leno $sb.ToString() -Encoding utf8
```

**⚠ 本轮踩到的工具坑（值得记，直接浪费了一轮验证）**：
`.\build.bat 2>&1 | Select-String -Pattern "error|warning|Build successful" | Select-Object -First 8`
—— **`Select-Object -First N` 取够 N 项后会终止上游管线**，而那次正好前 8 行全是警告 ⇒
**构建被掐断**，我却把"没看到 Build failed"读成了"构建成功"，接着拿**旧的 exe** 去验证，
差点得出"T12 没生效"的错误结论。
- 判据：`Get-Item build\leno.exe` 的时间戳**早于**改动的源文件（21:09 vs 21:18）⇒ 立刻暴露。
- 规矩：**构建日志不许截断取前 N 行**；只看 `Select-String -Pattern "error:"`，
  并且验证前先比一次"exe 时间戳 ≥ 源文件时间戳"。

**门禁**：断言套件 **360/360** + 示例冒烟 15/15（正常程序体量到不了 32KB ⇒ 零误报）。

**⑤-c Leno vs Lua（新基准口径）—— 已排除项与剩余嫌疑**

> ⚠ **测量机（所有耗时数字的适用前提）**：**Intel Core i5-14400F**（10 核 / 16 线程）。
> 换机器绝对值不可比；**比值**（Leno/Lua）稳一些，但也只在"两边**同机同轮**"时成立 ——
> 跨轮比较会被频率漂移和构建布局漂移污染（见 ⑤-m 末尾的测量方法学）。

- **微基准**：`examples/性能测试/全部测试.leno` vs `.lua`（规模已对齐，各 5 次取 min）。
  ⚠ 两个 runner 的执行顺序**不同**（Leno：算术→fibtest→test1→try→whileandfor；
  Lua：算术→test1→try→whileandfor→fibtest）⇒ 对表必须按**位置索引**显式对齐，按名字对会错位。
- **真实负载**：`examples/排序测试/各种排序`（与 `.lua` 版同算法/同规模/同跳过规则）。
- **口径校验**（重要）：我们 `times.us()/ms()` 用 `QueryPerformanceCounter`（**墙钟**），
  Lua `os.clock()` 是 **CPU 时间** —— 但用**外部墙钟**测整个进程（1101 vs 636ms = 1.73x）
  与各自内部计时（1078 vs 601ms = 1.79x）**一致** ⇒ 口径差异不是原因。
  ⚠ Lua 的 `os.clock()` 在 Windows 分辨率 ≈1ms ⇒ **它的小规模数字被量化成 0/1000**
  （<5ms 的项目一律不可用；5~20ms 的量化误差可达 ±25%；**≥20ms 才 <5%**）。

**现状复核（2026-09-22 晚，i5-14400F，倍数 = Leno/Lua）**

真实负载（**只列 Lua 侧 ≥5ms 的档**；5 次取 min，两侧**交错跑**避免"整批先跑谁"的频率漂移）：

| 规模 / 算法 | Leno（⑤-p 前） | Leno（现在） | Lua | 现在比 |
|---|---|---|---|---|
| 400000 优化快排 | 183,185µs | 137,172µs | 106,000µs | **1.29x** |
| 400000 原地快排 | 215,825µs | 186,544µs | 148,000µs | **1.26x** |
| 100000 优化快排 | 39,995µs | 31,481µs | 23,000µs | 1.37x |
| 100000 原地快排 | 48,451µs | 41,946µs | 33,000µs | 1.27x |
| 20000 原地快排 | 8,508µs | 7,300µs | 5,000µs | 1.46x |
| 20000 优化快排 | — | 5,278µs | 4,000µs | 1.32x |
| **20000 原始快排（分配密集）** | 16,985µs | 16,208µs | 19,000µs | **0.85x（反超）** |
| 5000 冒泡 | 265,295µs | 255,543µs | 195,000µs | 1.31x |
| 5000 原始快排 | — | 3,343µs | 4,000µs | **0.84x（反超）** |

⇒ 由 2026-09-21 记录的"紧循环密集的三类排序慢 **1.8–1.95x**"，经 ⑤-p（写路径直取寄存器）
与 ⑤-q（`while` 回边融合）两步收窄到 **1.26–1.46x**；40 万档的绝对差从
**36.5ms / 76.2ms** 降到 **38.5ms / 31.2ms**（原地 / 优化）。
分配密集的原始快排由打平（0.99x）到反超（0.85x）。

**⚠ 与 A/B 的交叉印证（两套独立测量必须对得上）**：
- 400000 原地快排：本表 215,825 → 186,544 = **−13.6%**，而 ⑤-p 的 A/B 说该档 **−12.7%** ✓ 同量级；
- 400000 优化快排：本表 183,185 → 137,172 = **−25.1%**，而 ⑤-p（−6.6%）+ ⑤-q（−17.3%）累计
  ≈ **−22.8%** ✓ 同量级。
两套口径（跨轮比值 vs 同轮 A/B）能这样对上，说明这批数字可信；**对不上就说明有漂移，不能用**。

**⚠ 分辨率**：Lua 侧 `os.clock()` 在 Windows 上**粒度 1ms**（读数全是 1000µs 的整数倍）⇒
500 那档直接读成 `0`（不可用），**2 万档（Lua 4–5ms）的量化误差可达 ±25%**，
**只有 ≥20ms 的档位误差 <5%**。所以上表里 `1.46x`（2 万档原地）这类偏高的格子，
以 40 万档的 `1.26x` 为准 —— 差值大半是 Lua 侧的量化抖动，不是 Leno 在 2 万档真的更慢。

微基准（**仍慢于** Lua 的，按倍数降序）：

| 项目 | Leno | Lua | 比 |
|---|---|---|---|
| 空函数调用 | 158.6ms | 70ms | **2.27x** |
| 算术 `a=b+c+d` | 64.6ms | 39ms | 1.66x |
| `while` 三种形态（空 / i++ / #3） | 63.7–64.1ms | 39ms | 1.63–1.64x |
| 算术 `a=b+c` / `b*c` / `b-c` | 43.0–44.2ms | 27–28ms | 1.55–1.59x |
| 算术 `a=1+2` / `i++` / `a=1` / `i=i+1` / `a+=1` | 36.8–40.3ms | 24–27ms | 1.45–1.53x |
| try 无 / `for 空体` | 28.8–40.1ms | 21–28ms | 1.37–1.43x |
| `fib 经典递归 n=30` | 44.3ms | 33ms | 1.34x |
| `for to j` / `a=b` / `arr[index]` | 33.6–35.7ms | 26–28ms | 1.28–1.30x |

微基准（**快于** Lua 的）：有 try `sum++` **0.08x**、`arr.add()` **0.30x**、步长2 **0.31x**、
`for arr.add` **0.31x**、`while arr.add` **0.34x**、`while 步长2` **0.37x**。

**⚠ 以下几项的数字不能当差距用（口径不对等）**：
- `dict[k]=v` **4.00x**：Leno 跑的是 `d.set("key",1)`＝**native 方法调用**
  （`MOV+GET_METHOD+CALL`），Lua 跑的是 `d["key"]=1`（1 条）—— **两边不是一个东西**，
  而且正好绕开了 ⑤-k/⑤-l/⑤-m 新加的字典 IC；
- `arr[index]` **1.28x**：Leno 是 `arr[0]`（**结果丢弃**），Lua 是 `k = arr1[1]`（存下来）；
- `try 有` **0.08x**：Lua 每轮 `pcall(function() ... end)` 要**新建闭包**（天然极慢）；
- `arr.add` / 步长2：Lua 的 `table.insert` 是函数调用；
- fib 记忆化/迭代/尾递归/n=1000、嵌套 for/while：**绝对值 <8ms，Lua 侧被 1ms 量化** ⇒ 比值无意义。

**逐指令边际成本（2026-09-22 新算 —— 用来定位"基线"到底是什么）**

同一份微基准里的边际差（1e7 次，单位 ms；带「衍生」的是由相邻项相减得到）：

| 边际项 | Leno | Lua | 读法 |
|---|---|---|---|
| 循环指令本身（`for 空体`，1 条/轮） | 28.8 | 21.0 | 比值 1.37x |
| + 一条 `MOV`（`a=b` − `for 空体`） | +4.8 | +5.0 | **边际成本相同** ⇒ 普通指令的"增量"两边一样 |
| + 一条 `ADD_INT`（`a=b+c` − `for 空体`） | +14.2 | +6.0 | 差在**语义**：我们要判 bigint + 溢出 + int48 提升 |
| + 一条 `INC`（`for i++` − `for 空体`） | +11.2 | +6.0 | 同上 |
| `CMPJMP_LT`+`JMP` 这一对（衍生） | ≈52.5 | ≈33.0 | 1.59x；for 侧融合指令对是 1.48x |

⇒ **一处结论修正（重要）**：此前把 `while` 记成"比基线还差的一种形态"，**这个读法是错的**。
按**绝对边际成本**看，while 的每条目成本（63.7÷3 ≈ 2.12ns）与 for 的（40.0÷2 ≈ 2.00ns）
基本一致，且两边 VM 各自内部也一致（Lua 1.30 / 1.35ns）⇒ while 的比值看起来更大，
只是因为它**多一对"比较+跳回"**，而这一对的比值（1.59x）略高于融合指令对（1.48x），
量级落在噪声内 ⇒ **不是独立缺陷，并入下面的"基线"问题一起看**。

- **已排除（都做了实测，别再重复试）**：
  1. 编译优化等级：两边 `build.bat` 都是 `-O2` ✗；
  2. 每条指令的行号写回（`DISPATCH` 里的 `vm.current_line`）：消融只值 **7–13%**；
  3. 寄存器基址缓存（把 `frame->locals` 缓存成局部 `cur_regs`、175 处改帧统一换成
     `FRAME_RESYNC()`）：**完全中性**（探针 1.02/1.00/1.01/0.98x、排序 1.94→1.96x）⇒
     `-O2` 早已在寄存器里缓存了这一层 ⇒ **已回退**（中性收益不值得多一条不变量风险）；
  4. 测量口径（见上）；
  5. handler 序言里无条件算的 `GET_CURRENT_LINE()`（`frame->chunk->lines[…]`，3 次依赖加载）——
     消融（宏改常量 `(0)`）后 36 项全量 A/B **差异方向随测量顺序翻转** ⇒ 中性，详见 **⑤-n**；
  6. **回边的 GC 安全点检查**（`FOR_LOOP` 每轮一次的 `vm.gc_force_request` 全局加载 + 分支）——
     消融后 `for i++` 看似 −4.4%，但**对调 A/B 顺序翻成 +3.5%**，且完全不经过 `FOR_LOOP` 的
     `while` 三种形态也在同轮里动了 ±3.6~3.9% ⇒ 纯布局漂移，**无信号**。
     （该检查是 `LENO_GC_FORCE_EVERY` 测试钩子的落点，别删。）
  7. **`while true` 专精**（入口恒真测试 + 回边 `LOADTRUE + JMP_IF_TRUE` ⇒ 单条 `OP_JMP`）：
     每外圈派发 **26 → 19 条**（dump 实证）却**更慢 ~10%**（隔离探针 7/7 次、双向顺序一致），
     排序基准中性 ⇒ **已回退**，详见 **⑤-af**。教训：**「砍跳转」≠「砍指令」**，别去数跳转。
  8. **T9：抽巨型 handler 的冷路径成 `static` 函数（整条路线，含 `OP_CALL` / `OP_STRUCT_INIT` /
     `OP_GET_METHOD` / `OP_STRUCT_DEF` / `OP_INDEX` / `OP_INDEX_SET` 的**任何一个**）** ——
     **2026-09-24 已完整实施并回退**：`vm_run` 机器码确实 **120,256 B = 3.75× L1I(32KB)**（动机成立），
     抽出 5KB 冷代码后 `vm_run` 缩到 115,168 B（−4.23%）、断言 367/367，
     但 **空调用探针稳定 +8.5%**（164.99~169.36 → 177.85~184.27，各 6 次分布**完全不重叠**）、
     广覆盖探针合计 **+3.34%**、光线追踪 −7.3% 与 +2.2% **并存且互相抵消**。
     ⇒ **它不是杠杆，是"代码布局抽奖"**：抽代码把 `vm_run` 绝对地址挪了 ~11KB，
     I-cache 组相联的 set 映射全变，极短循环体对 ±10% 的单循环级抖动最敏感、**方向由运气决定**。
     **不要再试**（除非先做出"布局可控"的手段：独立段 / profile 重排 / 链接脚本）。
     详见 §T9 末节；补丁留档 `build\_t9_reverted.patch`。
- **⚠ 量测分辨率上限（本轮的新认识，比任何单项结论都重要）**：
  本机 A/B 的噪声底约 **±1ns/轮 ≈ ±4%**（判据：同轮里**与改动无关**的项目也会动 ±4%）。
  由此把三次消融排成一列就看得很清楚：

  | 消融对象 | 量级 | 结果 |
  |---|---|---|
  | 去掉一条 `LOADI`（**整条派发**） | ~0.9ns | **−29%**，清晰可辨 ✅ |
  | handler 序言的行号计算 | ~0.2ns | 中性（方向随顺序翻转）|
  | 缓存 `frame->locals`（175 处） | ~0.2ns | 中性 |
  | 回边 GC 检查 | ~0.2ns | 中性（方向随顺序翻转）|

  ⇒ **整条指令的削减（0.9~2.9ns）测得出来；几个 uop（0.1~0.3ns）测不出来。**
  继续在 handler 里抠 uop 等于闭着眼睛改 —— 要么先上**周期级探针**（rdtsc）把分辨率压下去，
  要么只做"整条指令"级别的改动。**别再交"中性"的微优化了。**
- **剩余嫌疑（按可操作性，已按上面证据修订）**：
  1. **代码密度 / I-cache**：`vm_run` 是**一个 4000+ 行的函数**，其中 `OP_INDEX` 单个 handler
     就有 256 行 ⇒ 热派发循环与巨型冷路径纠缠在一起（Lua 的 handler 大多 ~20 行）。
     对应 **T9**（把巨型 handler 的冷路径抽成 `static` 函数）—— 现在多了一个**性能**理由；
     ⚠ **2026-09-24 已实测证伪**：`vm_run` 机器码确实 **120,256 B = 3.75× L1I**（前提成立），
     抽出 5KB 冷代码后 `vm_run` 缩到 115,168 B，但**空调用探针稳定 +8.5%、广覆盖探针 +3.34%**，
     单循环级 ±10% 的抖动方向随机（B2 −7.3% 与 空调用 +8.5% 并存）⇒ **是地址对齐抽奖，不是杠杆**。
     详见 §T9 末节；**别再试**（除非先有布局可控的手段）；
  2. 各算子里比 Lua 多的检查（bigint/溢出/int48 范围/GC/类型派发）—— 由边际成本表算出：
     `FOR_LOOP` 比 Lua 重 **+7.8ms/1e7**、`INC` **+5.2ms**、`ADD_INT` **+8.2ms**，
     即**每条指令额外 0.5~0.8ns**。这是 Leno 数字语义（int48 提升 + bigint + null）的**固有代价**，
     不是 bug，抠不掉；且逐项都**低于上面的分辨率上限** ⇒ 不构成可攻目标；
     ⚠ **2026-09-25 补一条例外（别把这条推广成"所有多出来的检查都抠不掉"）**：⑤-ah 发现索引
     **写**路径里那块成本根本不是语义 —— 是**漏做静态类型特化**（读侧三个变体早就有，
     写侧一个都没有，还多绕一次 `value_to_double` 的 int→double→int 往返），补上后
     写密集基准 **−9~−12%**。判据：先分清"这个检查是**语义要求**（bigint/null/int48）"，还是
     "**编译器没把已知信息传下来**（该特化没特化）"——后者是可攻的，且量级远大于 0.1~0.3ns 那一档；
  3. ~~派发本身的固定开销（间接跳转 + `ADVANCE` + `frame->ip` 经结构体字段访问）~~
     —— **已证伪（2026-09-22）**：`MOV` 的边际成本两边**几乎相同**（+4.8 vs +5.0ms/1e7），
     说明"多一条派发指令"的代价并不比 Lua 贵 ⇒ 派发机制不是瓶颈。
     `frame->ip` 提局部变量 / 寄存器窗口这一族**不必再试**（与已排除项 3、5 同源，第三次同向否定）。
- **下一步建议（已改向）**：
  1. 唯一"量级够大"的剩余目标是**调用路径（微基准 2.27x）** —— 结构性：`CallFrame` ≈384B、
     每次调用写 14 个字段跨 6 条 cache line；Lua 的 `CallInfo` 只有几十字节。风险高，需先想清方案。
  2. 继续做**整条指令削减**（唯一被证明有效的杠杆：`a+=1` −28%、`arr[0]` −29%、空体返回 −7.8%）：
     方法是 dump **真实程序**的字节码数条数，找可以整条删掉的，而不是在 handler 里省 uop。
  3. 若仍要做微优化，**先解决量测精度**（周期级探针），否则做出来的结论不可信。

**判据**（每条独立验证、独立提交）：`leno --debug` 里能 dump 出新融合指令；
相关样例耗时不变差；断言套件 **366/366**；提交信息附 **同轮双向 A/B 数据**
（机器频率与**构建布局**都会漂移，跨轮比较不可信 —— F1 那次差点被误导；
⚠ 本轮补一条：**同一轮里不相关的项目也可能动 ±4%**，所以必须对调顺序双向验证）。

> 注：F1（`arr.add(x)` 融合 + `OP_ARRAY_APPEND` 补 GC 写屏障，`b209f07`）就是"反向差异"的第一条，
> 详见 `docs/寄存式与栈式的差异清单.md` 第九节。

---

**⑤-v 追 Lua 的第二轮（2026-09-23）：零拷贝原地帧 / 位运算裸位运算 / int48 越界修复**

> ⚠ **教训（本轮最大的收获，写给下一次的自己）**：动手消融前**先读本节的"已排除"清单**。
> 本轮我先试了 `GET_CURRENT_LINE()` 消融、`FOR_LOOP` 回边 GC 检查消融 —— 做完才发现
> 这两条**早就在下面第 5、6 项里记着"中性"**，白烧了两轮构建。

- **宽口径复核**（`全部测试.leno` vs `.lua` + `各种排序`）：差距**仍是弥散的 1.3–1.7x**，
  唯一量级够大的仍是**调用路径（2.27x → 本轮 2.51x）**。**没有新的单点肥肉** —— 与 ⑤-g 一致。
- 本轮拿下三笔**整段算子级别**（不是 uop 级）的改动：

**✅ 已完成（三笔，各自独立提交、独立验证）**

1. **零拷贝原地帧**（`0cab715`）—— 照 Lua `luaD_precall / prepCallInfo`：被调方的寄存器区
   **直接复用调用方实参那一块**（`locals = caller->locals + arg_base`）⇒ 实参零搬运、
   非参数槽位零 nil 填充、不另占内存。原先每次调用要「另取一块 locals 数组 + 逐个复制实参
   + 其余槽位全填 null」。
   关键第二刀：判定用**物理容量**（新增 `CallFrame.locals_capacity`）而**不是** `local_count`
   这个寄存器高水位 —— 自有 `inline_locals` 有 64 格，用高水位（往往一二十）会白白漏掉
   一大片本来可以安全别名的空间。
   实测 **PBKDF2 主耗时 11.43ms → 10.07ms（−12%）**，反超栈式（11.42ms）。
   ⚠ 踩过的坑：`call_reg_impl` 漏写 `locals_capacity` ⇒ alias 帧读到**帧槽复用的脏容量**，
   递归链上层层误判"装得下"直到数组越界（`fib(20)` 直接崩）⇒ **6 处建帧点都要写**。
   ⚠ 帧数组扩容时 `locals` 指针的搬迁也改了：原实现无条件写成自己的 `inline_locals`，
   会把原地帧指向调用方的指针改错 ⇒ 改成按地址差统一平移。

2. **位运算快路径改"裸位运算"**（`e91ed86`）—— 见 `04_compare_bit_cast.inc` 的 ★★ 注释。
   NaN 装箱下两个 int48 是 `T | p`（T = `QNAN|SIGN_BIT|TAG_INT` **两侧相同**，p 是 48 位补码
   载荷、只占 bit 0..47）⇒ **AND/OR 直接在原始 64 位上做就是精确的**，XOR 补回 T 即可
   （负数只是 48 位补码，照样成立）。省掉两次拆箱（其中符号扩展带**分支**）+ 一次装箱。
   实测 `bench_bitwise_loop`（2 亿轮 × 6 条）**2588ms → 1946ms（−25%）**，对 Lua **1.80x → 1.36x**。
   ⚠ 定位过程（方法论）：先反汇编确认**指令数完全相同**（Lua 的 `MMBINI/MMBINK` 在整数快路径上
   被 `pc++` **跳过**，实际每轮也是 6 条）⇒ 差距全在每指令成本；再两步消融：守卫 384ms、
   拆箱/装箱 **504ms** ⇒ **拆箱/装箱才是大头**，不是类型检查。

3. **int48 越界必须提升 bigint**（`27669a0`）—— 正确性修复（静默错值，比崩溃更难查）。
   · 左移：守卫只看**移位量**、看不到**左值大小** ⇒ `123456789012345 << 3` 静默截成
     -138245594743864（正确 987654312098760）。判据必须**先比阈值再移位**（先移位会撞
     int64 左移 UB，回绕后还会被误判成"在范围内"—— `hi << 1` 就是这么算成 -2 的）。
   · 除法：只有一个越界组合 `INT48_MIN / -1 == +2^47`。
   · 同族已核过、**无需改**：ADD/SUB（`VM_INT_RESULT`）、MUL（`LENO_MUL_OVERFLOW`）、
     NEG（范围检查）、USHR（`val_int_safe`）、MOD（结果绝对值恒小于除数）、BITNOT（`~x` 封闭）。
   · 新增 `assert/test_int48_overflow.leno` —— **这两个边界原先一个都没被覆盖**，
     套件全绿也漏（"全绿"不等于"覆盖到了"）。

**❌ 已回退（中性 —— 接在 ⑤-c"已排除"清单的第 6 项之后，别再试）**

7. **把 `frame->ip` / `frame->locals` / `frame->chunk->constants` 提成局部量**
   （207 处帧切换点统一 `RELOAD_FRAME()`）：改动正确（365/365）但**零收益甚至略差**
   （2622~2655ms vs 基线 2588ms）。与已排除项 3、5 同源 ⇒ **第四次同向否定**。
8. **K 版位运算守卫削减**（`_K` 的常量由 codegen 保证是 int48 ⇒ 那次 `val_is_int(cv)` 可证冗余）：
   **零收益**（1957 vs 1920，噪声内）⇒ 那 384ms 来自**守卫的分支本身**，不是比较次数。
9. **`FOR_LOOP` 非计数制回退路径（bigint/float）整体短路**：**中性**（33.2~35.6 vs 30.4ms）
   ⇒ "代码密度 / I-cache" 这个假设**在 `FOR_LOOP` 上不成立**（它在位运算族上成立）
   ⇒ 剩余嫌疑 1（T9）的**适用范围比原先设想的窄**：要挑 handler 做，不是普遍规律。
10. **帧内联数组池化 / 共享寄存器池**（`CallFrame` 664B → 152B）：结构性改造做完、断言 366/366、
   差分与基线逐文件一致，但**收益没量出来**（唯一确证：大帧不再 malloc，−4~8%），
   且协程帧要改成每次调用 malloc ⇒ 已整体回退。**详见 ⑤-x**（含三条判据：热路径别放扩池判定、
   池窗口要给被调方留白、A/B 必须双向交替）。

**✅ 本轮新做的一笔（整段级，非 uop 级）**

4. **`OP_FOR_LOOP` 计数器"原始值减一"**（Lua `chgivalue` 同口径）：end 槽本来就是 canonical
   的 int48 Value `T | count`，而 `count ∈ [1, 2^47-1]` ⇒ 减 1 **不会借位到标签位**，
   结果恰等于 `val_int(count-1)`。省掉「拆箱（含符号扩展分支）→ 减 → 装箱」三步。
   **A/B 分布不重叠**（各 5 轮，空体 for 1e7）：BASE 31.8 / 32.5 / 34.0 / 34.0 / 34.1ms
   vs NEW 30.7 / 28.9 / 29.2 / 30.3 / 28.8ms ⇒ **min −9.4%、中位数 −14.3%**。
   ⚠ **量测提醒**：这条**第一版 3 轮判成了"中性"** —— 正是 ⑤-c 末尾警告的那种情形。
   凡是要下"有/无收益"的结论，必须做到**分布不重叠**，否则不要下结论。

**⚠ 口径提醒（本轮复核，与 ⑤-c 结论一致）**
- `dict[k]=v` 在 `全部测试` 里 Leno 跑的是 `d.set("key",1)`＝**方法调用**（`GET_METHOD+CALL`），
  Lua 跑的是 `d["key"]=1`（1 条）⇒ 表里的 **4.10x 不是差距**。同一二进制内实测三形态：
  `d.set(k,v)` **157.6ms** / `d[k]=v` **69.5ms** / `d[k]` 读 **62.1ms**
  ⇒ **公平对照是 `d[k]=v` 对 Lua 的 41ms = 1.70x**（与基线一致）。
- 排序组（≥10 万档，Lua 量化误差 <5%）：原地/优化快排 **1.27–1.34x**，
  与 ⑤-c 记录的 1.26–1.37x 一致 ⇒ 那批数字仍然可信。

---

**⑤-w `SHL` 越界语义的连锁反应：一次"修对了却让 PBKDF2 慢 2.6 倍"的教训**

> **病症**：⑤-v 的第 3 笔（`27669a0`，给 SHL 补"越界提升 bigint"检查）之后，
> `examples/crypto/pbkdf2.leno` 从 **10.07ms 涨到 26.58ms**（+164%）。基准本身全 PASS、
> 断言全绿 —— **只有性能塌了**，是最难发现的那一类回归。

**根因（语义正确 ≠ 性能可接受）**

`pbkdf2.leno` 的 `right_rotate` 原写法是 `_int32((ux >> c) | (ux << (32 - c)))`，
其中 `ux = _uint32(x)` 可达 2^32 ⇒ `ux << 30` 可达 **2^62，远超 int48(2^47)**。
在"越界提升 bigint"的语义下，**每次旋转都分配一个 bigint**（SHA-256 每轮 6 次旋转）。

**栈式为什么没这个问题**：它的 `OP_SHL`（变量移位量版）是
`if (shift < 32) { push(val_int(a << shift)); }` —— **静默截断，漏了检查**。
只有 `OP_SHL_IMM`（字面量版）在 §8.123 里被修过（作者把"静默截断"标为 **✗ bug2**，
修法正是 `_a <= INT48_MAX >> imm`）。**同族 bug 只修了一半** —— 变量版是漏网。

⇒ 所以：**寄存式（带越界检查）是合法的，栈式变量版的截断是 bug**；
但"合法"不代表"这个写法能跑得快"。

**修法（不改 VM 语义，改调用方的写法）**

32 位旋转本来就存在**每步都不越界**的等价写法 —— 先拆位、再各移各的：

```leno
int hi = ux >> c                              // 右移，永不越界
int lo = (ux & ((1 << c) - 1)) << (32 - c)    // 只把低 c 位左移 ⇒ 结果 ≤ 2^32
return _int32(hi | lo)
```

数学上与原式逐位等价（左移溢出的高 (32-c) 位本来就会被 `_int32` 丢掉），
但全程落在 int48 内 ⇒ **一次 bigint 都不触发**。

**实测**：26.58ms → **10.83ms（−59%）**，恢复并略优于 ⑤-v 之后的 10.07ms 水平；
同轮栈式 16.13ms ⇒ **反超 33%**。向量三个 PASS、与栈式逐行一致、断言 **366/366**。

**可复用的判据（写进这里免得再踩）**
- 给"特化快路径"补越界检查时，**必须同时看热点调用方**是否依赖旧的截断行为 ——
  补检查 = 把静默错值变成 bigint 分配，**错值可能"恰好"被算法容忍（结果仍对），
  但分配一定慢**。`_int32(...)`/`_uint32(...)` 这类"取低 32 位"的写法是典型信号。
- 在"48 位整数 + 越界升 bigint"的语言里写 32 位定点运算，**要主动拆位**，
  别指望高位溢出的中间结果被后面的 mask 救回来（求值顺序摆在那）。
- 反过来说：**这类改写属于"调用方适配语义"，不该为了它把 VM 的越界检查撤掉** ——
  撤掉就是退回 §8.123 明令修掉的 bug2，两个后端又会不一致。

---

**⑤-x 调用路径第三步：把帧内联数组换成"共享寄存器池"（Lua 风格）—— ❌ 已整体回退**

> **动机**：⑤-g 量清了调用路径差 Lua 约 2.5x、且没有单点肥肉；唯一量级够大的结构性差距是
> **`CallFrame` 体型**：帧槽里内联 `Value inline_locals[64]`（512B）⇒ 帧 664B，活跃帧的元数据
> 按 664B 步长散在不同 cache line 上；Lua 的 `CallInfo` 只有几十字节、寄存器另放在一块连续栈里。
> 方案：照 Lua 做**共享寄存器池**（每 VM 一块连续 `Value` 数组，所有帧的 `locals` 窗口从池里切，
> 零拷贝原地帧 = 池里的相邻位置），目标是把 `CallFrame` 从 664B 压到 ~152B。

**做了什么（工作树已完成、验证通过、按作者决定整体回退）**

- `VM.reg_pool / reg_pool_capacity / reg_pool_top`；`CallFrame.pool_top_saved`（-1 = 本帧没动水位）。
- 池化改造后 `CallFrame` **664B → 152B**（目标达成），6 处建帧点 + 全部帧弹出点改走池；
  协程帧仍 `malloc`（yield 会把整帧 memcpy 到 `saved_frames`，池窗口搬不走）。
- 热路径判据**保持与改造前逐字一致**（容量够 ⇒ `locals = caller->locals + arg_base`）；
  容量不够时走慢路：池内调用方扩池后原地延伸（③-b），否则另切窗口（③-c，含 `POOL_RESERVE` 留白）。
- **正确性全过**：断言 **366/366**（当时那套与栈式的对照手段现已废止，不再作为回归口径）。

**为什么回退：收益没量出来（双向交替 A/B，各轮取 min，Δ = 池化相对基线）**

| 基准 | baseline | 池化 | Δ |
|---|---|---|---|
| 探针 big_calls（640 万次调用） | 68.66 | 68.26 | −0.6% |
| `examples/crypto/pbkdf2.leno` | 9.43 | 9.31 | −1.3% |
| 空调用与自增隔离微基准 | 178.84 | 183.84 | +2.8%（分布重叠 ⇒ 不成立） |
| 深递归 `经典斐波那契数列测试_int` | 60.39 | 63.70 | **+5.5%** |
| 光线追踪对象版 Phase C | 1721 | 1728 | 中性（分布重叠） |
| 探针 big_nocall（纯帧创建） | 38.8 | 37.0 | **−4~8% ← 唯一确证收益** |

⇒ 唯一确证的是"**大帧（>64 槽）不再 malloc**"这一小块；其余全在中性 ±5% 噪声里，
而 512B 的帧缩小**没有换来任何可测收益**（深递归、光追这些"应该受益"的场景反而不占优）。
代价却是实打实的：协程帧从"内联数组"变成**每次调用一次 malloc**（异步/协程密集代码要吃亏）、
动了 12 个文件、6 处建帧点 + 全部弹出点 + GC/异常/协程三套配套 ⇒ **不划算，按纪律回退。**

**可复用的判据（写进这里免得再踩）**

1. **热路径上不许出现"扩池/容量重判"这类账**。第一版把"算偏移 + 比水位 + 抬水位"塞进了别名判定：
   探针 640 万次调用每次多 **~1ns**、pbkdf2 掉 12%。改成"容量够就指针加偏移"后立刻回到基线水平。
   ⇒ 与 ⑤-c 已排除项（指针缓存、K 版守卫削减…）**第五次同向否定**：调用热路径只认"整条指令削减"。
2. **池窗口要给被调方留白**（`POOL_RESERVE`）。只按 `local_count` 精确切窗口时，被调方动不动就判
   "装不下"，退到慢路（扩池 + 填 null）：空调用微基准因此 **+7.7%**；留 16 格后回到 +2.8%（噪声内）。
   Lua 的栈同样在每个新帧之上留 `LUA_MINSTACK` 的余量。
3. **A/B 必须双向交替**（本轮最大教训）：固定"先 baseline 后新版"跑了 4 轮，得出"池化慢 12~15%"，
   改成双向交替后同一对二进制变成"−0.6% / −1.3%"。机理是机器在同一轮内会单向漂移（本轮 base
   自身在不同轮之间从 61ms 漂到 75ms），**后跑的那个被系统性坑**。与 ⑤-c 末尾"分布不重叠才算数"
   是同一条纪律的两个面：**既要分布不重叠，也要顺序不偏袒**。
4. 需要"大帧不再 malloc"这一条时**不必**重做池化：`INLINE_LOCALS_MAX` 32→64（⑤-o）已经把
   语料里 33~64 槽的函数收进内联数组，剩下的 >64 槽大帧是少数；真要动，就只动那一条慢路。

---

**⑤-y 返回路径专精（照 Lua 5.5 的 OP_RETURN0/1 抄）—— ✅ 已完成：空调用 −6.1%、pbkdf2 −2.1%**

> **动机（这轮把 Lua 源码逐行对着读了）**：⑤-g 量清"调用路径差 Lua 约 2.5x、无单点肥肉"，
> 而差距的**形状**在 Lua 源码里写得很清楚 —— 它把**编译期可证的信息塞进字节码**，
> 运行期不为"不可能发生的事"付代价（`D:\Lua\lua-5.5.0\src`）：
> · 建帧只写 5 个字段（`ldo.c` 的 `prepCallInfo`）；参数**不搬**（实参本就在栈上连续）；
>   只补缺的形参，**不整帧填 null**；`CallInfo` 只有 ~56B（`lstate.h`）。
> · 返回分 **OP_RETURN0 / OP_RETURN1 / OP_RETURN** 三条指令（`lvm.c`），前两条的非 hook 分支
>   只有 `L->ci = ci->previous` + 写结果 + `L->top.p`，然后 `goto ret` 刷新 `cl/k/pc/base`。
> · upvalue / hook 这类检查由**返回指令的 k 位**在编译期给出（`TESTARG_k(i)`），运行期零成本。
> 我们则是一条通用 `OP_RETURN` 扛全部：每次返回约 18 次访存（含 `is_ctor` 的 3 次依赖加载、
> `FRAME_TRY_VALID` 的 2 次加载、只有宿主才需要的 `last_return_*` 三连写、`open_upvalues` 全局加载）。

**三刀（每刀都单独编译 + 断言 + 双向交替 A/B）**

1. **C 字段承载"专精"标记**：codegen 在「当前函数无 try 且非构造」时发 `OP_RETURN` 的 `C=1`
   （两条都是编译期可证的：`ObjFunction.has_try` / `is_ctor`；`current_func` 为 NULL 时置 0 走通用），
   handler 据此整段跳过 is_ctor / finally 两组检查。探针：`call_ret` **−4.9%**、`call_void` **−4.4%**。
2. **`last_return_*` 只在宿主路径写**：寄存器式调用的结果由调用方直接从自己寄存器读走，
   这三个字段只有 `vm_call_value` 之后（`ffi.c` / `arrays.c` / 退出码）才读。
   累计 `call_ret` **−7.4%** / `call_void` **−7.1%**。
3. **`close_upvalues` 守卫改用帧上的 `has_captures`**（省掉 `vm.open_upvalues` 的全局加载）。

**⚠ 这一刀踩到两个坑（都靠断言套件当场抓到，记下来免得再踩）**

- **`frame_cnt > 0` 不能兜住宿主的写回**：主帧（`ret_frame = -1`）返回时 `frame_cnt` 已归零，
  而**进程退出码就是从 `last_return_value` 读的** ⇒ 4 个用例当场失败（`test_var_param` 等）。
  正解：三连写按 `ret_frame < 0` **无条件**执行，只有"结果压回栈"才需要 `frame_cnt > 0`。
  顺带发现**主帧从来没初始化过 `ret_frame`**（一直是脏值/0，只是过去无条件写 `last_return_value`
  才没暴露）⇒ 已在 `vm_init` 显式置 -1。
- **`CallFrame.has_captures` 的语义是错的**：它记的是"本闭包捕获了**父函数**的 upvalue"，
  不是"本帧有被捕获的局部量" ⇒ 拿它当守卫会**漏关 upvalue**
  （`test_closure` / `test_closure_upvalue_rw` / `test_function_closure` 当场失败）。
  这个坑栈式早踩过（`D:\CLeno\Leno\src\vm\vminc\op_call.inc:671` 就有注释）。
  正解：**运行期精确置位** —— 只有 `OP_CLOSURE` 走"非值捕获"分支（真在 `locals` 里留了指针）时才置 1
  （`02_loads.inc`）。该字段此前是"写了 6 处、**没有任何读取点**"的死字段，这轮才真正生效。

**实测（双向交替 A/B，各 5 轮取 min；baseline = `build\leno_base_ret1.exe`）**

| 基准 | baseline | 现在 | Δ |
|---|---|---|---|
| 探针 `call_ret`（1M 次调用） | 16.16 | 14.90 | **−7.8%** |
| 探针 `call_void`（无返回值） | 13.65 | 12.70 | **−7.0%** |
| 探针 `deep`（40 层递归 × 10 万轮） | 76.63 | 75.77 | −1.1%（噪声内） |
| 空调用与自增隔离微基准 | 168.55 | 158.34 | **−6.1%** |
| `examples/crypto/pbkdf2.leno` | 8.70 | 8.51 | −2.1% |

正确性：断言 **366/366**（当时的栈式对照已废止，不再跑）。

**下一刀的候选（还没做）**：调用侧仍是 **13 个字段写入**（Lua 5 个）+ `vm.sp` 双栈记账；
按本轮实测"每个访存 ≈ 0.2ns"估算，把调用侧压到 Lua 的形状还能省 **~1.5–2ns（约 10–12%）**。

---

**⑤-z 调用侧第一刀：方法调用零拷贝 —— ❌ 已回退（方法中性、构造 −5.5%）**

> **先把"调用形态"量清楚再动刀**（新探针 `build\_probe_call.leno`，10M 次/项，同机同轮）：

| 形态 | ns/次 |
|---|---|
| A 全局直呼（`OP_CALL_GLOBAL_FUNC_TYPED`，已核类型） | 23.6 |
| B 经局部变量（`OP_CALL`，逐参数转换路径） | 29.3 ~ 30.6 |
| C 绑定方法（`OBJ_BOUND_METHOD` 分支） | 36.4 ~ 38.2 |
| D 0 参数 / 3 参数（全局直呼） | 29.4 ~ 30.4 / 32.3 ~ 36.8 |
| E 结构体构造（`new P(x=1)`，含 has_self 的 ctor 调用） | 81 ~ 85 |

- **C 贵不是因为"没零拷贝"**：绑定方法那条路 `self_in_args` 判定成立时走的是 `has_self = 0`
  ⇒ **本来就已经零拷贝**了。它贵在**前戏**：receiver 去重（`expect_arity` 要 2~3 次依赖加载
  + 6~8 次值比较）+ 逐参数转换（B 的 +5.4ns 那份）+ `is_async` 的 2 次加载。
- 于是试着让 **has_self 也能零拷贝**：把 receiver 填进 `R[arg_base - 1]`（= callee 值那个槽，
  调用期间已经没用、返回时结果正好覆盖它），被调方窗口从那里起算 ⇒ 理论上一个字节都不用搬。
- **实测（双向交替 A/B，各 4 轮取 min）**：方法形态 **中性**（363.8 vs 364.7 —— 因为它本来
  就已经零拷贝，改动对它无效），结构体构造 **−5.5%**（base 814~853ms vs new 859~875ms，
  分布不重叠）⇒ **按"收益不确证不落"的纪律整体回退**。

**⚠ 副产物：一条必须记住的约束（这刀踩出来的）**

**`OP_DTOR_LOCAL` 不是编译期调用点** —— 它在**作用域退出**时发出，调用方的活寄存器
（典型的：已经算好、等着 `return` 的临时量）可能就紧贴在对象寄存器**之上**；别名会把它们覆盖掉。
实测 `assert\test_dtor_inline.leno`：`var h = create()` 拿到 **0** 而不是 42（`create()` 内联后
返回值临时量正好被析构帧的寄存器区踩掉）。回退后该路径回到"另开窗口"，结论留在这里：
**将来再给 has_self 做零拷贝，必须给析构单独留一个 no_alias 入口**（这轮已经实现过
`call_reg_no_alias`，验证通过但不划算，随刀一起撤了）。

**下一刀候选（已重新归因 —— 见 ⑤-aa，别按上表的"C 绑定方法"数字去估收益）**：
~~绑定方法的前戏（~8ns/次）~~ —— ⑤-aa 用 `LENO_GC_TRACE` 证明：上表 C 行那条形态
（`b.get(s)`，接收者静态类型是 struct）codegen 发的是**融合指令** `OP_INVOKE_METHOD_TYPED`，
**根本不建绑定对象**（10M 次 0 次 GC）。真正走 `OBJ_BOUND_METHOD` 分支的形态（接收者静态
类型是 **face**，或绑定对象先存进变量）实测前戏只有 **~2~5ns**：
① `expect_arity` 的 2~3 次依赖加载 ⇒ 已试过缓存进 `ObjBoundMethod`，**收益落在地板下、已回退**（⑤-aa）；
② 剩下那 6~8 次值比较理论上能用"arity 先定调"（`nargs == arity` ⇒ 含 receiver；`nargs+1 == arity`
⇒ 不含）省掉，但那是**判定优先级反转**（现状是值比较优先、arity 只用来纠正自引用误判）⇒
动它之前必须先把语义安全性论证清楚（可变参数函数、`a.add(a)` 这类自引用都要覆盖）；
③ **真正的开销既不在前戏也不在查找**（查找有 IC、命中 ~100%），而在**每轮分配一个
ObjBoundMethod**（64B 池化对象 + GC 压力）⇒ **已由 ⑤-ab 修掉：face 路径 95.7ns → 38ns 量级，
实测 −47.4%**。

---

**⑤-aa 把 `arity` / `is_async` 缓存进 `ObjBoundMethod` —— ❌ 已回退（收益 < 测量地板）**

> **做了什么**：给 `ObjBoundMethod` 加 `cached_arity` / `cached_is_async` 两个字段，在两个
> 构造器（`bound_method_new` / `bound_closure_method_new`）里建对象时一次算好，`06_call.inc`
> 的绑定方法分支直接读 —— 省掉 `bound->closure->function->arity`（2~3 次指针链加载）与
> `->is_async`（2 次）。顺带把第三个手写创建点（cstruct 实例方法，`12_fields_object.inc`）
> 收敛到公共构造器，免得漏填缓存。断言 **366/366** 通过；**收益不确证 ⇒ 整体回退**
> （原留档 `build\_aa_patch.txt` 已被 build 目录的清理带走；改点就是上面这四处）。

**⚠ 本轮真正的收获：纠正了 ⑤-z 的一个归因错误（比这刀本身值钱）**

判别法（零改动、可复用）：**跑 `LENO_GC_TRACE=1` 数 GC 次数** —— 真走绑定对象的话，每轮要
分配一个 64B 的 `ObjBoundMethod`（本仓库 `gc_alloc` 对其按 64B 池化）⇒ 一定会触发 minor GC。

| 形态（10M 次，同机同轮） | ns/次 | GC 次数 | 说明 |
|---|---|---|---|
| `b.get(s)`（接收者静态类型 **struct**） | ~37 | **0** | **融合指令 `OP_INVOKE_METHOD_TYPED`**，不建绑定对象 ⇒ ⑤-z 表里那一行不是绑定方法 |
| `sp.get(s)`（接收者静态类型 **face**，每轮查找 + 分配） | ~85 ~ 92 | 66 | 真·绑定分支；贵在**每轮重查方法 + 分配**，不在调用前戏 |
| `var g = sp.get; g(s)`（绑定对象**预创建**，纯调用侧） | **~38.9** | 0 | 本刀的目标形状（探针 `build\_probe_facebound.leno`） |
| 对照：普通 `OP_CALL`（经局部变量，⑤-z 表的 B 行） | ~30 | 0 | —— |

⇒ 绑定分支的"前戏"实测量级是 **~2~5ns**（38.9 含 `_int()` 转换与 `for` 开销），**不是 8ns**；
这刀逻辑上只省 2~4 条依赖加载（≈0.5~1ns，占 ~39ns 的 1~2%），**低于本仓库的噪声地板**。

**⚠ 顺手把"地板"标定出来了（新证据，以后判刀先看这条）**

把改动**整体回退后重建**（源码与基线逐字节等价、产物同为 1354.40KB，仅哈希不同 —— 构建
非比特可复现，但布局等价），再按同一 A/B 协议跑同一个探针：

| 对比（`_probe_facebound`，双向交替取 min） | baseline | 新 exe | Δ |
|---|---|---|---|
| 改动版（⑤-aa 改完） | 388.9 | 396.0 | +1.8%（看着像"变慢"） |
| **空改动版（回退后重建）** | 386.9 | 392.2 | **+1.4%（同量级、同方向）** |

⇒ **新写盘的 exe 在这种协议下系统性略慢**，**<2% 的差异不可归因**。凡预期收益 <2% 的刀，
先跑一遍"空改动 A/B"当对照，否则会把自己的改动误判成回归、也会把噪声当成收益。

**⚠ 另一个坑（真 bug，已登记 T16）：被调函数里的"空体 for"会踩掉调用方的活寄存器**

写探针时撞到，**2026-09-23 复核时把条件钉准了**（我第一版记的条件是错的，更正如下）：
**调用方 for 循环之前多一个活着的局部量** + **被调函数体内有一个空体 `for n {}`**
⇒ 调用方的循环**永不终止**（100% CPU 空转，`N=10` 就挂）。最小复现：

```leno
import times
func f0():int { for 1 {} return 7 }     // ← 体内的空体 for 是关键：填上循环体就正常
main() {
    int N = 10
    var s = 0
    var t1 = 7                          // ← 循环前多一个活着的局部量是关键：去掉这行就正常
    for N { s = f0() }                  // ← 挂在这里：这个循环不终止
    print("after s=" + s + " t1=" + t1)
}
```

逐条实测（都在当前头上复跑）：① 去掉 `var t1` ⇒ 正常；② `print` 前置、`times.ns()`、float/int
**都不是**必要条件（各变体照样挂，我第一版记的"前置 print"是误判）；③ 被调方的 for 体填成
`{ q = q + 1 }` ⇒ 不挂，**但它把调用方的 `t1` 打印成 1（应为 7）** ⇒ **铁证：调用方的寄存器被被调方踩了**；
④ `--debug` 两版字节码结构正常（FOR_PREP/FOR_LOOP 只是整体移了一位）⇒ 问题在 **VM 的帧窗口/别名**，
不在 codegen。⇒ 与 ⑤-z 记的 `OP_DTOR_LOCAL` 别名教训**同类**（调用方活寄存器落在被调方窗口里）。
**归属 T16**，根因待查：下一步从 `OP_CALL_GLOBAL_FUNC` 的 `dst_safe` / 原地帧（`locals = caller->locals + arg_base`）
的窗口起算判据查起。判据：上面 9 行 1 秒内打印 `after s=7 t1=7`。
（临时探针里，凡"调用方循环前有活着的局部量 + 被调函数体内有空体 `for {}`"的形状都会挂，别用。）

---

**⑤-ab `OP_GET_METHOD`（struct 分支）的调用窥孔 —— ✅ 已完成：face 路径 −47.4%**

> **动机**：face / 静态类型未知的接收者走的是 `OP_GET_METHOD` + `OP_CALL` **两指令**形状
> （codegen 只为「接收者静态类型是 struct 且方法非 async」发融合指令 `OP_INVOKE_METHOD_TYPED`）。
> 探针实测 **95.7ns/次**，是融合路径（38.2ns）的 2.5 倍。
> **先用临时插桩（env 开关的指令计数，验证后已全拆）把两条路的指令计数打出来**，才看清钱花在哪：
> · face 探针：`OP_GET_METHOD`(struct 分支)=10M、**IC 命中 9,999,999**、`OP_CALL` 绑定分支=10M、`OP_INDEX`=0
>   ⇒ **查找有 IC 且几乎全命中**（⑤-aa 里"每轮重查方法"那句是错的），也**不是**调用前戏（~2~5ns）
>   —— 贵的是**每轮分配一个 `ObjBoundMethod`**（64B 池化对象 + 它带来的 GC：10M 次触发 66 次 minor GC）；
> · 只做"取方法 + 分配"、不调用（`var g = sp.get`）单这一半就是 **83.5ns/次**。

**改法**（只动 `12_fields_object.inc` 的 `OP_GET_METHOD` struct 分支）：加**窥孔**，条件与上文
"内置类型方法"那条窥孔**逐字一致** —— `a <= 255`（无 EXTEND 前缀）、下一条就是 `OP_CALL`、
同一基址 `a`、`C == 2`（单返回值）；命中则**不物化绑定对象**，直接按 `OP_INVOKE_METHOD_TYPED`
那套做（receiver 去重 + `call_reg`），结果写回 `R[a]`。**async 方法不走窥孔**（它要"调用即建协程"）
⇒ 原样退回"分配绑定对象 → OP_CALL 绑定分支走 `vm_start_async_call`"。
顺带把该分支的"取方法"收敛成「先拿到 `cl` 再统一收尾」，IC 命中与查表两条支共用同一段尾码。

**实测（双向交替 A/B，各 4 轮取 min；baseline = `build\leno_base_bound1.exe`）**

| 用例（10M 次调用） | baseline | 现在 | Δ |
|---|---|---|---|
| 探针 face 直调（本刀目标形态） | 847.15 | **445.72** | **−47.4%**（分布不重叠：847~972 vs 446~488） |
| 对照：探针 struct 融合路径（没动这条） | 364.73 | 360.35 | 中性（噪声内） |
| 对照：`空调用与自增隔离微基准` | 39.36 | 39.28 | 中性 |

正确性：断言 **366/366**；另把 `examples\struct`、`examples\泛型系统`、`examples\测试`、`examples\func`、
`examples\array`、`examples\dict` 六个目录共 **296 个样例**用新旧 exe 各跑一遍逐文件比输出（单文件上限 20s）
⇒ **无真实差异**（18 个不逐个一致：17 个是"同一个 exe 连跑两遍就不一致"的时间/地址类输出，
1 个 `test_args_debug.leno` 打印的是 exe 自身路径）；`face_test.leno`、`struct 方法 + async.leno`、
`test_dtor_inline.leno`、`test_generic_use_face.leno` 等逐个与基线输出一致。

**⚠ 本刀没覆盖的形态（下一刀候选，别当已修）**：**语句位置**的调用（丢弃返回值）在 `OP_CALL` 里
是 `C != 2`，窥孔**不命中** ⇒ 仍每轮分配绑定对象。`examples\struct\face\face 调用耗时.leno`
全是这种形态（`s.speak()` 作语句）⇒ 它**没变快**（21603200 vs 21600500 ns，中性），**不能拿它验收本刀**。
要修得先确认 `emit_call` 对 `nresults=0` 的 C 编码（`emit_call` 写的是 `nresults + 1` ⇒ 丢弃时 `C == 1`），
再决定窥孔是否收 `C == 1`（收的话还要把 `call_reg` 的 `ret_want` 传 0，与绑定路径逐字同口径）。

---

**⑤-ac 修 T16：`dst_safe` 的"实参区空闲"判据不健全 —— 被调方窗口会踩调用方的活寄存器**

> **病症**：调用方 `for` 循环前**多一个活着的局部量** + 被调函数体内**有空体 `for n {}`**
> ⇒ 调用方循环**永不终止**（100% CPU 空转，`N=10` 就挂）；被调方循环体非空时不挂，
> 但那个局部量被踩成错值（`t1` 7 → 1）。新增的 `assert\test_for_call_alias.leno` 在修复前
> **立刻失败**（`assert(cnt == N)` ⇒ 迭代次数被踩坏），不会挂 ⇒ 能当回归网。

**根因（三层，逐层实测确认）**

1. **循环状态就在调用方寄存器里**：`for` 的 start/end/step/var 都是寄存器槽，`OP_FOR_PREP`
   把"剩余迭代次数"写进 **end 槽**，`OP_FOR_LOOP` **每轮回边**都要读它（减一/判零/加步长）。
2. **零拷贝原地帧的窗口比实参区长**：`call_reg_impl` 的 `locals = caller->locals + arg_base`
   把被调方 R0.. 直接映射到调用方 `R[arg_base..]`，窗口长度是**被调方 local_count** ⇒
   会伸到实参区**之上**，踩掉调用方在那里还活着的值（循环计数槽、局部量）。
3. **`dst_safe` 不健全（真正的入口）**：全局函数直呼那条原先判
   `dst + 1 == next_reg || dst >= next_reg || regs_available(gen, dst+1, dst+1+expected)`，
   而 `regs_available` 只校验**实参区**空闲 —— 0 参调用时那个区间是**空的**（空区间恒真）
   ⇒ 直接拿 `dst`（活变量槽）当基址；就算有实参，窗口也会伸到实参区之上踩活寄存器。
   （`for` 那个空体循环只是决定"被踩成什么值"：踩成 0 ⇒ 少跑几轮；踩成 1 ⇒ 永真回边。）

**修法**（只动 `codegen_expr.c` 全局函数直呼那条，原注释保留、只换判据）：
改成「**dst 之上没有活寄存器**」，两条**健全**形态 ——
① `dst+1 == next_reg` 或 `dst >= next_reg`（dst+1 起全是死槽）；
② `dst+1+expected >= next_reg` 且实参区 `regs_available`（实参区顶到高水位 ⇒ `[dst+1, next_reg)`
整段都在实参区里，本来就会被调用方写的实参盖掉；`next_reg` 之上未分配）。
`regs_available` 对不在空闲栈里的槽位一律当占用（保守方向）⇒ 判据健全。
同类判据另两处已核查：方法调用那条本来就只有①的两式；**原生调用不建帧、没有寄存器窗口**（实参拷到 vm.stack）⇒ 不构成同类风险。

**先写的严格版（"dst+1 到 next_reg-1 全空闲"）正确性没问题但付了代价**：`空调用微基准`
**+12%**（39.35 → 44.22ms，调用点多一条结果 MOV）⇒ 用上面的②放宽后回到中性。

**实测（双向交替 A/B，各 4 轮取 min；baseline = `build\leno_peek1.exe` = 修前的 ⑤-ab 构建）**

| 用例 | 修前 | 修后 | Δ |
|---|---|---|---|
| 最小复现（9 行，原本死循环） | **挂住**（100% CPU 空转） | `after s=7 t1=7`（1 秒内退出） | **修好** |
| `assert\test_for_call_alias.leno` | 断言失败（`cnt != N`） | ALL PASSED | **修好** |
| 空调用与自增隔离微基准 | 39.35 | 39.45 | 中性（严格版是 +12%） |
| 调用开销微基准 | 232.0 | 230.5 | 中性 |
| 探针 face 直调（⑤-ab 的目标形态） | 444.40 | 443.61 | 中性（⑤-ab 的 −47% 保留） |
| 探针 struct 融合路径 | 368.07 | 365.31 | 中性 |
| `examples/crypto/pbkdf2.leno` | 0.913 | 0.903 | 中性 |

正确性：断言 **367/367**（含新增 `test_for_call_alias`）；`examples` 八个目录共 **514 个样例**
用修前/修后两个 exe 逐文件比输出（单文件上限 20s）⇒ **无真实差异**（27 个不一致里 26 个是
"同一 exe 连跑两遍就不一致"的时间/地址类输出，1 个 `test_args_debug.leno` 打印 exe 自身路径）。

---

**⑤-ad 「比较+跳转融合」放宽到非变量操作数（`arr[j] <= pivot` 这类）—— ✅ 已完成：原地快排 −10.7%**

> **动机**：T10-① 的融合原先只认「左操作数是普通局部变量/参数」，于是分区函数内层
> `while a[i] < pivot` / `while a[i] > pivot` 的左操作数是**数组下标**，仍编成
> `INDEX_ARRAY_INT + LT_INT + JMP_IF_FALSE`（**4 条/轮**），而 Lua 的 `GETTABLE + LE + JMP` 是 3 条
> —— 排序基准里最大的单点指令数差就在这里。

**做法**（只动 `codegen_expr.c` 的 `try_emit_cmpjmp`，原注释保留、只加分支）：
判据 ② 由「左操作数必须是 `SYM_LOCAL`/`SYM_PARAM`」放宽为「**两侧操作数都落进一个寄存器**」——
普通局部变量/参数直接用它自己的寄存器（既有快路，一个寄存器都不分配），其余形态
（下标 `arr[j]`、字段访问、`right-left` 这类算术子表达式、upvalue/全局）**按原路径 `gen_expr`
求值到临时寄存器**，再让融合指令读它（用完即 `reg_free`）。该有的求值指令**一条不少**，
省掉的正是一条 `OP_JMP_IF_FALSE` 的派发 —— 与 ⑤-n/⑤-c 的结论一致：有效杠杆只有"整条指令削减"。

**为什么合法**：判据 ①（两侧静态类型都是 int）保证原路径一定会发 `*_INT[_IMM]`，
而 `OP_CMPJMP_*` 与它的运行期语义**逐条等价**（都只排除 null、都不做类型检查），
求值顺序也仍是"先左后右"（未动）。顺带修掉一处小回退：**超出 int8 的整数字面量**
（`size <= 2000`）原先回退成 `LOADI + LE_INT + JMP_IF_FALSE` 三条 ⇒ 现在 `LOADI + CMPJMP_LE` 两条。

**dump 实证**（`build\_dump_sort_new.txt` 对照旧 dump）：

| 位置 | 修前 | 修后 |
|---|---|---|
| `partition` 内层 `if arr[j] <= pivot` | `INDEX_ARRAY_INT + LE_INT + JMP_IF_FALSE` | `INDEX_ARRAY_INT + CMPJMP_LE A=14 B=pivot` |
| `partition_opt` 内层 `while a[i] < pivot`（含回边） | 4 条/轮 | **3 条/轮**（回边用极性位 C=0x40 "真则跳"） |
| `partition_opt` 中位数三比较 `arr[x] > arr[y]` | 3 条 | 2 条 |
| `quick_sort_opt` 的 `right-left < 20` | `SUB_INT + LT_INT_IMM + JMP_IF_FALSE` | `SUB_INT + CMPJMP_LT(imm)` |
| `main` 的 `size <= 2000`（超 int8 立即数） | `LOADI + LE_INT + JMP_IF_FALSE` | `LOADI + CMPJMP_LE` |

**实测（双向交替 A/B 取 min；baseline = `build\leno_base_cmpjmp1.exe`）**

微基准 `build\_micro_idx.leno` ③「比较+分支」（`if a[i] <= m`，正是本轮目标形态），4 轮：

| | 各轮 | min | Δ |
|---|---|---|---|
| A（修前） | 10.51 / 9.92 / 10.13 / 9.77 | **9.77** ns/轮 | — |
| B（修后） | 8.99 / 8.98 / 9.03 / 8.88 | **8.88** ns/轮 | **−9.1%**（分布不重叠） |

非目标探针（同文件）：①读4次/轮 min 20.83 → 20.75、②写1次/轮 min 9.11 → 9.12 ⇒ **中性**（符合预期）。

`examples/排序测试/各种排序.leno`（3 轮取 min；单位微秒）：

| 规模 | 原地快速排序 A → B | Δ | 优化快速排序 A → B | Δ |
|---|---|---|---|---|
| 2000 | 638 → 574 | **−10.0%** | 490 → 491 | 0 |
| 5000 | 1771 → 1771 | 0 | 1334 → 1343 | 0 |
| 20000 | 8183 → 7310 | **−10.7%** | 6195 → 6122 | −1.2% |
| 100000 | 45633 → 42246 | **−7.4%** | 36337 → 34428 | −5.3% |
| 400000 | 204698 → 190539 | **−6.9%** | 155248 → 155708 | 0 |

（"优化快速排序"收益小/为 0 是**能解释的**：小区间走 `insertion_sort_range`，它的 `and` 链比较
**本刀没覆盖**（见下），所以省下的派发只占它总指令数的一小部分。）

**正确性**：断言 **367/367**；`examples` 11 个目录（if/for/短路/逻辑/三元/切片/范围检查/自增自减/
作用域/array/struct/switch/while）共 **110 个样例**用修前/修后两个 exe 逐文件比输出 ⇒
**102 个逐字节一致**，8 个不一致的（`face 调用耗时`、`test_random_face`、`test_field_perf`、
`test_index_vs_field`、`test_method_fib_performance`×2、`benchmark_if_vs_switch`、`benchmark_switch_types`）
**同一 exe 连跑两遍也不一致** ⇒ 计时/随机类噪声，非回归。

**遗留（下一刀的入口，别重复试本刀）**
- **`and`/`or` 链里的比较没被覆盖**：`while j >= left and a[j] > key`（插入排序内层）走的是
  `gen_binop` 的**短路生成**（每侧 `gen_expr` + `JMP_IF_FALSE`）⇒ 每侧仍是 2 条。
  要吃掉它得在短路点**逐侧**调 `try_emit_cmpjmp`（短路语义与融合同向，极性按 `need_true` 传）。
  插入排序那对 `GE_INT + JMP_IF_FALSE` / `GT_INT + JMP_IF_FALSE` 就是它。
  ⇒ **`and` 链已由 ⑤-ae 完成**（优化快速排序 −8~−10%）；`or` 链仍未做，见 ⑤-ae 的遗留。
- **`arr.len() <= 1` 不融合**：`.len()` 的静态类型不是 int（整条走通用 `OP_LE`，连 int 特化都没有）
  ⇒ 得先给 `.len()`/`OP_LEN` 补 int 静态类型。那是**类型推断**的事，不是融合的判据问题。

---

**⑤-ae 「and 链」条件的逐侧融合（`while j >= left and arr[j] > key` 这类）—— ✅ 已完成：优化快速排序 −8~−10%**

> **动机**（⑤-ad 遗留的第 1 条）：条件位置上的 `and` 链走的是 `gen_binop` 的**短路生成** ——
> 整条链被当作**值表达式**算进一个 bool 临时寄存器（每侧一条自己的 `比较 + JMP_IF_FALSE`），
> 链尾再跟一条 `JMP_IF_FALSE` 决定分支。2 侧的链一共 **3 条跳转**，其中 2 条是白付的；
> 而 ⑤-ad 只覆盖"条件是单条比较"的形态，链上的每侧都没被融合。

**做法**（`codegen_expr.c` 新增 `gen_and_cond_jumps`，`codegen_stmt.c` 的 `if`/`while`
三处条件点接线，原注释保留、只加分支）：

- 先把 and 链**摊平**成求值顺序的侧列表（纯 AST 遍历，`(a and b) and c` 与 `a and (b and c)`
  都给 a,b,c），再**逐侧**发跳转：每侧的比较直接融进它自己的跳转（复用 ⑤-ad 放宽后的
  `try_emit_cmpjmp` ⇒ 下标/算术操作数也能融合）。
- **假标签就是链尾那条跳转的目标** ⇒ 每侧那条 JMP 与链尾那条 JMP 一起消失。
  `if`/`while 入口`：各侧全部"假则跳"；`while 回边`：非末侧"假则跳出"、**末侧**"真则跳回"。
- 侧数上限 `MAX_COND_JUMPS = 8`（超长链**先摊平、后发指令**，返回 0 时一条指令都还没发 ⇒
  调用方安全回退；否则会重复发出整条链）。
- `if x is T => name`（`need_bind`）**不走这条**：绑定必须插在"第一侧成立之后、第二侧求值
  之前"，仍由原来那个专门分支处理。
- 侧条件不是"两侧静态 int 的有序比较"（如 `p and q` 真值判断）时，该侧退回
  `gen_expr + JMP_IF_FALSE`，指令数与原来**相同**（不亏）。

**dump 实证**（`build\_dump_sort_and.txt`，`insertion_sort_range` 内层）：

| | 修前（⑤-ad 构建） | 修后（⑤-ae） |
|---|---|---|
| 入口 | `GE_INT + JMP_IF_FALSE + INDEX_ARRAY_INT + GT_INT + JMP_IF_FALSE`（5 条） | `CMPJMP_GE(→出口) + INDEX_ARRAY_INT + CMPJMP_GT(→出口)`（**3 条**） |
| 回边 | `GE_INT + JMP + INDEX + GT_INT + JMP_IF_TRUE`（5 条） | `CMPJMP_GE(→出口) + INDEX + CMPJMP_GT(C=0x40 真则跳回)`（**3 条**） |

**实测（双向交替 A/B 取 min；baseline = `build\leno_base_and1.exe` = ⑤-ad 构建）**

- 新增探针 ④（`build\_micro_idx.leno`：`while i < lim and a[i] > k`，插入排序内层形状，
  每轮 6 次链求值）4 轮：A ∈ [54.85, 56.17] → B ∈ [43.43, 47.40]，min **54.85 → 43.43 ns/轮 = −20.8%**（分布不重叠）；
- 同文件非目标探针：①读4次 20.14 → 20.45、②写1次 9.33 → 8.99、③比较+分支 8.74 → 8.85 ⇒ **中性**；
- `examples/排序测试/各种排序.leno`（3 轮取 min，微秒）：**优化快速排序**（含 and 链的插入排序）
  2000 483→442（−8.5%）、5000 1327→1208（−9.0%）、20000 6151→5533（−10.0%）、
  100000 34571→31606（−8.6%）、400000 152873→140978（−7.8%）；
  **原地快速排序**（无 and 链 ⇒ 对照组）中性（20000 7080→7122、100000 41962→41435、400000 183518→180791，±1.5% 内）。

**正确性**：断言 **367/367**；`test_cmpjmp` 新增第 9 组 —— 第一侧为假时**后续侧不得求值**
（副作用计数器）、三侧链假在中段、`while` 回边、可融合/不可融合混合、以及
**"第二侧的下标不得越界求值"**（`while i9 < 3 and c9[i9] > 0`：第一侧为假时若还去读
`c9[3]` 就直接报越界 —— 正是插入排序内层的形状）；融合开与 `LENO_NO_CMPJMP=1` 两种模式都过。
`examples` 11 个目录 **110 个样例**逐文件比输出（修前/修后两个 exe）⇒ 8 个"同一 exe 两跑也不一致"
的计时/随机噪声 + 2 个**缓存态**差异（见下），**无真实差异**（`简单的链表` / `linkedlist_oo`
两 exe 首跑输出逐字节一致）。

> ⚠ **样例扫描的噪声类型又多一类（缓存态）**：入口缓存的 deps 清单里记了**运行中 exe 的指纹**
> （§8.112）⇒ 两个 exe 各自编译、**A/B 不会被 on-disk 缓存串味**（好消息）；但同一个 exe
> **首跑会打印编译警告、命中缓存后的第二跑不打印** ⇒ 逐文件比输出时"发现 N 个警告"那几行
> 会随机出现/消失。判据仍是"同一 exe 连跑两遍是否一致"，别把它当成回归。

**与 Lua 的差距（同轮三方对比，2026-09-24 补测）**：`lua 各种排序.lua` / `leno_base_cmpjmp1.exe`（⑤-ad 前）/
  当前 exe 在同一轮里各跑一遍，微秒（第 1 轮）：

| 用例 | Lua | ⑤-ad 前 | 现在 | 差距变化 |
|---|---|---|---|---|
| 原地_5000 | 1250 | 1941（1.55x） | 1707（1.37x） | −0.18x |
| 原地_20000 | 5500 | 8296（1.51x） | 7574（1.38x） | −0.13x |
| 原地_100000 | 34000 | 48581（1.43x） | 43342（1.27x） | −0.16x |
| 原地_400000 | 145000 | 211940（1.46x） | 189680（1.31x） | −0.15x |
| 优化_20000 | 4000 | 6563（1.64x） | 6173（1.54x） | −0.10x |
| 优化_100000 | 25000 | 36475（1.46x） | 31919（1.28x） | −0.18x |
| 优化_400000 | 109000 | 158587（1.45x） | 141447（1.30x） | −0.15x |

  第 2 轮同向（原地 20000 1.39→1.29、100000 1.42→1.30、400000 1.42→1.27；优化 400000 1.41→1.27）
  ⇒ 大中规模的差距 **~1.42~1.46x → ~1.27~1.31x**。
  ⚠ **2000 档不可用**：Lua 的 `os.clock()` 粒度 1ms、小规模取平均后仍在 350~500 之间跳（两轮差 30%）。

**遗留（留给下一刀）**：`or` 链没做（短路方向相反：`a or b` 为假要"两侧都假" ⇒ 各侧发
"真则跳真标签"、末侧"假则跳假标签"，形态与 and 对称，但要新加一条回填约定）；
`for` 计数循环的条件由 `FOR_PREP/FOR_LOOP` 自己管，不经过这里。

---

**⑤-af `while true` 专精（入口恒真测试 + 回边）—— ⛔ 已回退（中性 / 隔离探针下反而更慢）**

> **动机**：`while true {`（分区函数就是这样写的）入口发 `LOADTRUE + JMP_IF_FALSE`，那条跳转
> **恒不跳**（常量真永远进体，循环只能从 `break` 出去）⇒ 两条纯白付；回边也发
> `LOADTRUE + JMP_IF_TRUE`。编译期能确定的何必留到运行期。

**做了什么**（`gen_while`：常量真条件不发入口测试、回边改单条 24 位 `OP_JMP`；开关 `LENO_NO_WHILETRUE=1`）：

| | 修前 | 修后 |
|---|---|---|
| `partition_opt` 入口 | `LOADTRUE + JMP_IF_FALSE` | **0 条**（`break` 直接回填循环出口） |
| `partition_opt` 回边 | `LOADTRUE + JMP_IF_TRUE` | **1 条** `OP_JMP sJ=-100` |
| `partition_opt` 函数体积 | 288 字节 | 276 字节 |

**实测（先隔离、后真实基准；`--no-cache` 两侧都加 —— 原因见下面的教训 a）**

1. **隔离探针** `build\_micro_whiletrue.leno`（`while true { i=i+1; if i>=N {break} }`，每外圈 5 圈；
   实测每外圈派发 **26 条 → 19 条**）：**7/7 次都是"指令更少的那侧更慢、约 +10%**，
   且**对调顺序不翻转**（A 先：A ∈ [37.17, 40.79] / B ∈ [40.81, 45.93]；B 先：B ∈ [39.66, 41.38] / A ∈ [35.76, 36.98]）。
2. **排序基准**（3 轮，微秒，min）：原地快排 5 档里 4 档 B 略快（0.9~3.0%）、1 档慢 3.4%；
   优化快排 5 档里 3 档 B 略慢（1~3%）。⇒ **中性**（全在 ±3% 噪声内、方向混乱）。

⇒ 没有收益证据，**整体回退**（代码已还原，断言 367/367）。

> ⚠ **教训 a（比这条结论重要得多）：同二进制 env 开关做 A/B 必须带 `--no-cache`。**
> 入口缓存的 deps 清单只记「源码哈希 + **运行 exe 的指纹**」（§8.112），**不含环境变量** ⇒
> 第二次运行会直接吃**第一次编译出来的字节码**（开关对它无效）。实测铁证：不加 `--no-cache` 时
> on/off 两次计时**一模一样**（35.70 vs 35.72 ns/轮，而两版字节码差 7 条指令）；加了才分得开。
> ⇒ **本文档历史上凡是用 `LENO_NO_CMPJMP` 这类开关做 A/B 的数据都要打问号**（除非当时带了
> `--no-cache`）。以后一律：同二进制开关 A/B 用 `--no-cache`；跨 exe A/B 天然安全（指纹不同）。
>
> ⚠ **教训 b：「砍跳转」不等于「砍指令」。** 这条探针每外圈少了 7 条**跳转/常量装载**，耗时反而 +10%；
> 而 ⑤-n 那条"整条指令削减有效"的杠杆（去掉 `LOADI` → −29%）砍的是**计算/装载类**指令。
> ⇒ 分支的代价由跳转本身（预测 / 目标 / 布局）决定，与**条数**无关 —— **以后找窥孔别去数跳转**，
> 要数就数 LOADI / GETTABLE / MOV 这类。

---

**⑤-ag 一元运算直取变量寄存器（`-x` 不再白搬一条 MOV）—— ✅ 已完成：隔离探针 −13.4%**

> **动机**（扫光线追踪字节码时发现）：`hit` / `hitSingle` 里的 `var t = -b - sq` 被编成
> **3 条**：`MOV r15,b + NEG r14,r15 + SUB_F r14,r14,r13`，而 Lua 的 `UNM + SUB` 只要 **2 条**。
> 根因在 `gen_unary`：它无条件 `gen_expr(operand)` —— 普通局部量也会被先搬进一个临时寄存器。
> `hit`/`hitSingle` 各有 **2 处**（两个函数是全样例最热的：Phase C 里 `hit` 被调 1800 万次）。

**做法**（只动 `codegen_expr.c` 的 `gen_unary`，原注释保留、只加分支）：
判据与 `gen_binop` 的 `rl_direct` **完全同一手法** —— 操作数是普通局部变量/参数
（`SYM_LOCAL`/`SYM_PARAM`）时直接用它的寄存器；一元指令都是 iABC「A=目标、B=源」，
**B 可以直接是变量自己的寄存器**（`dst != src` 是允许的，handler 也是"先读 R[B] 再写 R[A]"），
所以那条 MOV 是纯多余的。全局/upvalue 仍走 `gen_expr`（它们需要真正的取值指令）。
`x = -x` 这类 dst 恰好是变量自己也安全（先读后写）；只有"借来的临时寄存器"才 `reg_free`。

**dump 实证**：`hitSingle` 188 → **180 字节**，`MOV + NEG` → 单条 `OP_NEG A=14 B=10`。

**实测（双向交替 A/B；baseline = `build\leno_base_neg1.exe`；两侧都带 `--no-cache`，见 ⑤-af 教训 a）**

| 探针 | 前 | 后 | Δ |
|---|---|---|---|
| 隔离探针 `build\_micro_neg.leno`（`t = t + (-b - sq)`，4 轮） | 8.04~8.27 | **6.95~7.12** ns/轮 | **−13.4%**（分布不重叠） |
| 同文件对照 `t = t + (b - sq)`（**无一元负号**） | 5.15~5.22 | 5.11~5.28 | 中性 ✓ |
| 光线追踪对象版 Phase C（`hit` 调 1800 万次） | 1763.1 / 1757.8 / 1781.9 | 1756.4 / 1739.4 / 1761.5 | −0.38 / −1.05 / **−1.15%**（3/3 同向） |
| 光线追踪对象版 Phase B / B2 | — | — | −1.4~−5.9% / −2.0~+1.3%（混在噪声里） |
| 光线追踪标量版 Phase C | — | — | −0.02~+2.7%（**噪声**，同轮 Phase A 无一元负号也动了 ±8%） |

⇒ **机制确证**（隔离探针分布不重叠、对照中性 ⇒ 每条被删的 `OP_MOV` ≈ **1.1ns**，与本文档
"整条指令 0.9~2.9ns" 一致）；**真实基准只有 ~1%** —— 因为那些循环体约 50 条指令、
且被浮点运算/缓存占满，**不是派发受限**，删 2 条派发自然被稀释。改动是**严格减指令、零语义风险**，
故保留。

**正确性**：断言 **367/367**；`examples\性能测试` 36 个样例 + 另外 10 个目录 24 个样例，
用修前/修后两个 exe 逐文件比输出（**屏蔽计时数字**后比较 —— 这批文件输出里全是实测毫秒，
不屏蔽必然 30/36 不一致）⇒ **0 处差异**。

**扫字节码剩下的候选（都没做，留档）**

| 候选 | 位置 | 量级 | 为什么没做 |
|---|---|---|---|
| 方法调用实参块末尾的**多余 `OP_LOADNIL`** | `benchHit`/`trace` 每次调用 1 条 | 每次调用 1 条（Phase C ≈1800 万） | 口径太绕：`expected` 取的是 `pcnt`（含隐式 self），而 self 是走 `base` 传的 ⇒ 末位多填一个 nil。但实测 `s.f(1)` 的默认参数语义**是对的**（257/227/223），说明 args 里到底含不含 self 在两种调用形态下不一致 —— **没弄清前不动**（怕改坏默认参数补齐） |
| `s.r * s.r` 把**同一个字段读两次** | `hit` 第 54 行（`r*r`） | 每次调用 1 条（1800 万） | 要**字段级 CSE**（两个 AST 节点结构相同）⇒ 属于另一个量级的工程 |
| `tmin = h.t` 紧跟 `h.t < tmin` ⇒ **重复读字段** | `trace` 内层 | 每轮 1 条 | 同上（要"复用刚算出的临时值"，是 CSE/活跃性分析） |
| `if si >= NS` 里 **`NS` 是模块级变量 ⇒ 每轮 GETGLOBAL** | Phase A/B/B2 各每轮 1 条 | 每轮 1 条（900 万） | 要"模块级 `int X = 字面量` 且全程未被赋值 ⇒ 当编译期常量"，属**语义层常量传播**，风险与 ⑤-u 的 `is_null_value` 同级；且是本样例的写法特征，不是通用窥孔 |
| `OP_CAST_FLOAT` 冗余（`tmin = h.t`，两侧都是 float） | `trace` 内层 | 每轮 1 条 | 得先查清 `h.t` 为何推断成非 float（收窄/any）⇒ 属类型推断，不是窥孔 |

**结论**：这两个样例里**已经没有"整条指令级"的纯窥孔了** —— 剩下的 MOV 全是调用约定
（实参块连续、结果搬回 dst）与赋值本身；再往下要么是 CSE/常量传播（语义层工程），
要么是调用路径的结构性成本（帧 384B、每调用 10 条调用前戏），见 ⑤-c/⑤-g。

---

**⑤-ah 索引写的静态类型特化（`OP_INDEX_SET_ARRAY_INT`）—— ✅ 已完成：写密集的排序 −9~−12%**

> **动机**（对着 Lua 5.5 逐行读热路径时发现）：`arr[i] = v` 的**写**路径从来没有做过静态类型
> 特化，而**读**路径早就有了三个特化变体。dump `partition` 的内循环（8 条）看得很清楚：

```
0056 OP_INDEX_ARRAY_INT   arr[j]              ← 特化读
0060 OP_CMPJMP_LE         arr[j] <= pivot     ← 已融合（⑤-ad）
0068 OP_INDEX_ARRAY_INT   temp = arr[i]       ← 特化读
0072 OP_INDEX_ARRAY_INT   arr[j]              ← 特化读
0076 OP_INDEX_SET         arr[i] = arr[j]     ← 通用写（本条要修的）
0080 OP_INDEX_SET         arr[j] = temp       ← 通用写
0084 OP_INC / 0088 OP_FOR_LOOP
```

> **不对称在哪**（`07_collections.inc` 通用 `OP_INDEX_SET` 的数组分支 vs 特化读）：
> 通用写要 `val_is_obj(obj)` → `->type` 分流 → 再 `val_is_num(idx) ? (int)value_to_double(idx) : -1`
> —— 对 int48 下标是「符号扩展 → `cvtsi2sd` → `cvttsd2si`」**两次浮点往返**；
> 特化读只做 `val_as_obj`（不判型）+ `(int)val_as_int(idx)`（无往返）。
> Lua 的数组写快路径 `luaH_fastseti`（`ltable.h:57`）同样是**整数原样当下标**，没有这层往返。
> ⚠ 这与 ⑤-c 剩余嫌疑第 2 条（"多出来的检查是数字语义的固有代价、抠不掉"）**不同源**：
> 那批是语义（bigint/null/int48），这条是**纯漏做特化**，语义零变化。

**做法**

- 新增 opcode `OP_INDEX_SET_ARRAY_INT`，**追加在枚举末尾**（既有编号不动）；
  handler 与 `OP_INDEX_ARRAY_INT` 同形：`val_as_obj` 直卸 + `val_as_int` 解下标 +
  `(uint64_t)i2 >= count` 越界检查 + 同一个 `gc_write_barrier`。
  越界**报错文本与行号来源逐字一致**（"数组索引越界: 索引 %d, 长度 %d"）。
- codegen 侧新增 `index_set_op_for()`（`codegen_stmt.c`），判据与读路径的 `arr_spec`
  **同源**：接收者静态类型 `Array[int]` **且**下标静态类型 `int`。两个发射点
  （`gen_index_assign` 与多目标赋值里的索引分支）都走它。

**A/B（双向交替 5 轮、取 min；baseline = `build\leno_base_setspec.exe`；两侧 `--no-cache`）**

先把两份 .lenb 隔离到"只差字节码"：长度同为 6821 B、**逐字节比对只有 22 个字节不同**（就是那 22 处 opcode）。

| 项目（写密集） | 前 | 后 | Δ |
|---|---|---|---|
| 400000 原地快排 | 183550 | **161446** | **−12.0%**（分布不重叠 ✅） |
| 100000 原地快排 | 40127 | **36868** | −8.1%（分布几乎不重叠） |
| 20000 原地快排 | 7241 | 6523 | −9.9% |
| 5000 原地快排 | 1502 | 1361 | −9.4% |
| 2000 原地快排 | 552 | 501 | −9.2% |
| 5000 冒泡 | 255513 | **227579** | **−10.9%**（分布不重叠 ✅） |
| 2000 冒泡 | 40374 | **36197** | −10.3%（分布不重叠 ✅） |
| 400000 优化快排 | 133148 | 126209 | −5.2%（有重叠，仅旁证） |
| 100000 优化快排 | 30126 | 29242 | −2.9%（有重叠，仅旁证） |
| **对照：5000 原始快排**（无索引写） | 3856 | 3811 | −1.2% ✓ 中性 |
| **对照：20000 原始快排**（无索引写） | 15704 | 16022 | +2.0% ✓ 中性 |

两个**对照组纹丝不动**、处理组 −5~−12% ⇒ 机制确证，不是布局漂移。
回归检查（另两个排序样例）：`更多排序` −2.4%、`基数排序测试` −7%（分布不重叠）。

**同轮 Leno/Lua（交错跑 5 轮取 min，Lua 侧只取 ≥20ms 的档，量化误差 <5%）**

| 档位 | 现在 | ⑤-c 表里的旧值 |
|---|---|---|
| 400000 原地 | **1.13×** | 1.26× |
| 400000 优化 | **1.17×** | 1.29× |
| 100000 原地 | **1.19×** | 1.27× |
| 100000 优化 | **1.12×** | 1.37× |
| 5000 冒泡 | **1.17×** | 1.31× |

分配密集的原始快排仍反超（0.80× / 0.93× / 0.82×，与 ⑤-c 的 0.84~0.85× 同量级）。
⇒ 排序这一族与 Lua 的差距由 **1.26~1.37× 收窄到 1.12~1.19×**。

**覆盖率提醒（"全绿 ≠ 覆盖到了"）**：`assert\test_array_bounds.leno` 原先只测越界**读**，
新 opcode 的越界**写**分支一条都没走到 ⇒ 补了 3 组：正下标越界（239 次捕获 + 报错文本含
"数组索引越界"）、负下标越界、**特化路径与通用路径的报错文本逐字一致**
（后者是特意用元素类型非 int 的 `Array[any]` 触发通用版）。
⚠ 补测时踩了个坑：两个数组**长度必须相同**才比得对（报错文本里带"长度 N"），
第一版拿长度 1 和长度 3 的数组比，两个编译器都"失败"—— 是测试写错了，不是代码。

**版本与验收**

- **bump `LENO_BIN_VERSION` v3.1.0 → v3.2.0、`LENO_MODCACHE_VERSION` v16 → v17**：
  虽然按惯例追加在末尾（旧 .lenb 在新 VM 上照旧可跑），但**新字节码含旧构建不认识的 opcode**
  ⇒ 必须升（判据与先例 v2.6.0「新增 OP_CMPJMP_LI_INT，追加在末尾仍 bump」一字不差）。
  实测：旧 v3.1.0 的 .lenb 被新 VM 以「反序列化失败: 5」**fail-closed 挡下** ✅。
- ⚠ **VM 必须同批重建**（本次已跑 `build_vm.bat`）：这正是打包握手的意义 —— 不升版本号时，
  旧 VM + 新编译器的组合会"握手通过但运行期跳转发散"。
- 断言 **373/0**（默认 + `LENO_NO_CACHE=1` 强制全量重编译各一轮）；`-c` 编译后跑 `.lenb`
  输出与直跑 `.leno` 逐字一致（屏蔽耗时）；基准输出两次逐字一致、15 行 `正确性: true`。

**留档（没做）**：`Array[float]` 的写路径**未特化**（名字叫 `_INT`，就只发 int 元素；
handler 本身与元素类型无关）—— **这一条已由 ⑤-ai 解决（放宽判据，元素类型不限）**。
立即数下标版（对齐读侧 `OP_INDEX_ARRAY_IMM`）也没做 —— 写侧的 `arr[0] = v` 本来就只有
「LOADI + 写」两条，且 ⑤-p 已让值/接收者直取寄存器，收益面比读侧小。

---

**⑤-ai 把索引特化的判据放宽到「任意元素类型」（⑤-ah 的后半）—— ✅ 已完成：结构体/字符串数组 −12.6~−35%**

> **起因**：⑤-ah 之后问"要不要再给 `Array[float]` / `Array[string]` 各做一套特化"。先把
> 判据想清楚就发现**不该做三套指令** —— 特化省掉的那几样成本**与元素类型无关**：

| 被特化省掉的开销 | 真正取决于什么 |
|---|---|
| `val_is_obj` + `->type` 分流（array/dict/struct/cstruct/…） | 只取决于"**接收者是数组**" |
| 下标 `val_is_num ? (int)value_to_double(idx) : -1`（int→double→int 往返） | 只取决于"**下标是 int**" |
| 无条件 `GET_CURRENT_LINE()`（通用版在 handler 顶部就付；特化版挪进报错分支） | 与两者都无关，只是"通用版要报多种错"才需要 |

> 读侧 `OP_INDEX_ARRAY_INT` / `_FLOAT` 的 handler **逐字相同**（连注释都写"语义同上"）就是
> 证据：那个后缀说的是**下标类型**，不是元素类型。所以正确做法是**一句判据放宽**，
> 一套指令覆盖 struct / string / 嵌套数组 / bigint 等全部元素类型。

**第一步：先量站点（`LENO_IDXPROBE` 探针，量完已删、`git diff` 确认为空）**

探针按"接收者是 Array + 下标静态 int"分类，编译期打点、打印所在函数名：

| | 读（现覆盖 / 放宽后新增） | 写（现覆盖 / 放宽后新增） |
|---|---|---|
| pvz | 477 / **585** | 25 / **73** |
| hello_window | 436 / **559** | 23 / **73** |

⇒ **放宽能新增覆盖的站点数超过现有覆盖**。更关键的是落点（pvz 按函数聚合，widen 数）：
`run` **75**、`render` **48**、`process` **37**、`_process_ml` **24**、`_step_anim`/**`_swap_rows`**/
`_swap_cols` 各 **16**、`_find_r` 15、`loadAnim` 14 —— **全是每帧/每次更新的热函数**
（`_swap_rows`/`_swap_cols` 就是游戏棋盘）。元素类型不是 int：pvz 里是
`Array[Plant]` / `Array[Zombie]` / `Array[Pea]` / `Array[Sun]` / `Array[Mower]` / `Array[Particle]` 这类结构体数组。

**第二步：改（两处判据各删一支）**

- `codegen_expr.c` 的 `arr_spec`：去掉 `元素 ∈ {int,float}` 这一支。
- `codegen_stmt.c` 的 `index_set_op_for`：同上。
- 读侧指令选择由 `elem==INT ? INT : FLOAT` 改成 `elem==FLOAT ? FLOAT : INT`
  —— 既有的 int/float 产物**字节码一字不变**，只是新增覆盖（最小 diff、零回归面）。

**★ 这次改动最值钱的性质：零新 opcode ⇒ 不升版本号、不重建 VM、不作废任何缓存。**
handler 本就与元素类型无关，所以**旧 VM（v3.2.0）执行放宽后的新产物完全正确** —— opcode 集合没变。
这是唯一一类"不用让用户重编译任何东西"的性能改动（⑤-ah 那种新增 opcode 就必须 bump）。

**实证（pvz，`--debug -c` 计数；⚠ GUI 程序一律带 `-c`，裸跑会弹窗挂住终端）**

| | base | new |
|---|---|---|
| 读特化（`OP_INDEX_ARRAY_INT`） | 134 | **708** |
| 通用 `OP_INDEX` | 2053 | **1468** |
| 写特化（`OP_INDEX_SET_ARRAY_INT`） | 0 | **98** |
| 通用 `OP_INDEX_SET` | 3134 | **3036** |

与探针预测的 585 / 73 逐项对上（读侧另有 11 个立即数版顺带覆盖：`OP_INDEX_ARRAY_IMM` 204 → 215）。

**A/B（无头隔离探针 `build\_micro_idxstruct.leno`；双向交替 5 轮取 min；两侧 `--no-cache`）**

探针三个热循环：`Array[struct]` 读（`ps[i].x`）、`Array[struct]` 相邻交换（每轮 2 读 2 写）、
`Array[string]` 读。dump 实证：base 是 5 通用读 + 2 通用写，new 是 4 特化读 + 2 特化写
（剩那 1 条通用 `OP_INDEX` 是 `ps[i].x` 里的**字段读**，另一条路径，不在本次范围）。

| 探针 | base | new | Δ |
|---|---|---|---|
| A 结构体数组_读 | 169,896 | **148,448** | **−12.6%**（分布不重叠 ✅）|
| B 结构体数组_写 | 230,583 | **149,962** | **−35.0%**（分布不重叠 ✅）|
| C 字符串数组_读 | 121,488 | **105,528** | **−13.1%**（分布不重叠 ✅）|

⇒ 折算：每轮 4 次被转换的访问省 6.5ns ⇒ **单次 ≈1.6ns**，与"整条指令 0.9~2.9ns"同一量级。
写侧幅度最大（−35%），因为通用写的数组分支比通用读多一次 `val_is_num` + 往返 + 报错分派。

**验收**：断言 **373/0**（默认 + `LENO_NO_CACHE=1` 强制全量重编译各一轮）；
跨 10 个目录 156 个样例差分对拍 **0 处真实差异**（3 处差异全是输出自身不确定 —— 计时/随机/
链表打印，两侧各自重复跑都不一致）。

**⚠ 一处契约要写明（不是新风险）**：通用 `OP_INDEX` 的数组分支有一条"数组索引必须是数字"的
检查与专属报错，特化版没有（静态类型保证下标是 int）⇒ 下标在运行期意外不是数字时，
报错文本会从"数组索引必须是数字"变成"数组索引越界"。这与既有 `Array[int]`/`Array[float]`
特化是**同一契约**，放宽只是把该契约延伸到更多数组类型。

**可复用判据（写进来免得下次又想歪）**：⑤-c 剩余嫌疑第 2 条把"比 Lua 多的检查"整体记成
"数字语义的固有代价、抠不掉" —— **别这么推广**。动手前先分清：
① **语义要求**（bigint 提升 / null 报错 / int48 范围）⇒ 真抠不掉；
② **编译器没把已知信息传下来**（该特化的没特化、编译期可证的没证）⇒ 可以整片消掉，
而且量级远大于 0.1~0.3ns 那一档。

**仍未做（留档）**：`arr_spec` 要求 `element_type` 非空 ⇒ pvz 里 7 个"推断不出元素类型"的站点
仍走通用（保守，不动）；`Array[any]` 同理。

---

### ~~T6 / T7（顺手）~~ ⇒ **已取消**（2026-09-23）

> 这两条都是在维护"与栈式的差异清单"。既然不再与栈式对比，整条作废；
> `docs/寄存式与栈式的差异清单.md` 降级为**历史归档**（里面的 C 类条目讲的"两侧一起错"
> 仍是寄存器式自身该修的 bug，但归属改记在本文档的待办里，不再靠那张清单驱动）。

---

### T17（P2）.lenb 体积继续瘦身：构成已实测，三个候选方案待选

> **已拍板：暂不做，先记账**（2026-09-24）。行号表 RLE 已落地（`4c14ab1`）：
> minilang 34225 → 17829 B（−47.9%）、pvz 1972545 → 1254165 B（−36.4%）。
> 下面是"**还能压什么**"的实测构成与候选。

> **2026-09-25 收工（裁剪侧到顶）**：方法级 DCE + 两处结构去重落地后，
> `hello_window` **432725 B**、`pvz` **505432 B**（自 1.8 MB / 1.25 MB 下来，−50% / −47%）。
> 结论：**不再有死代码可删**，剩余体积只能来自"压缩"（见 ③-补 的复测：当前产物 deflate 仍是 2.8×）。
> 继续剪只有靠放松 fail-safe（ctor/dtor 全留、匿名闭包保活、间接调用按名字通配），
> 而**剪错的症状是"静默返回 null"**（不崩、不报错）⇒ 收益小、风险大，**本条收工，不再动裁剪**。

**① 实测构成（分段跳过法：`LENO_SZSKIP` 位掩码只跳一段、看输出体积差；探针量完已删）**

| 段 | minilang（17829 B） | pvz（1254165 B） |
|---|---|---|
| 字节码 code | 8776（**49.2%**） | 382332（30.5%） |
| 字符串载荷 | 4110（23.1%） | 413796（**33.0%**） |
| 行号表（RLE 后） | 1173（6.6%） | 50464（4.0%） |
| 其余（u32 计数 / 常量标签 / scope / 函数与块元数据） | 3770（21.1%） | 407573（32.5%） |

**② 字符串去重潜力（what-if：重复载荷不写第二遍，哈希表实测）**

- minilang：重复 2250/4110 = **54.7%** ⇒ 17829 → 15579（**−12.6%**）
- pvz：重复 349532/413796 = **84.5%** ⇒ 1254165 → **904633**（**−27.9%**；扣掉索引引用开销 ≈ −25%）

**③ 整包 deflate 上限**（衡量 LZ 类方案；PowerShell `GZipStream` Optimal）

- minilang 17829 → 7548（42.3%，2.4×）；pvz 1254165 → **327923（26.1%，3.8×）**
- 自研 LZSS（无 Huffman）一般比 deflate 差 15~30% ⇒ pvz 预期 ~40–45 万 B

**③-补 复测（2026-09-25，DCE 落地后的当前产物；同一台机 `GZipStream` Optimal）**

| 产物 | 当前 .lenb | deflate | 比率 |
|---|---|---|---|
| `pvz.lenb` | 505432 B | **181610 B** | 35.9%（**2.8×**） |
| `hello_window.lenb` | 432725 B | **151542 B** | 35.0%（**2.9×**） |

DCE 把重复函数体删掉后压缩率自然变差（pvz 3.8× → 2.8×），但**绝对量仍是当前的一半以下**
⇒ 裁剪到顶之后，LZ 是唯一剩余的体积路径（§④ 方案②）。

**④ 三个候选（按性价比排序）**

| 方案 | 预估收益（pvz / minilang） | 成本 | 风险 | 要点 |
|---|---|---|---|---|
| ① 字符串 interning | −25~28% / −13% | 中 | 低–中 | 写侧 hash 去重表 + 读侧 id→串表；重复串只写一次载荷、后续写索引（现有 u32 长度字段可借高位做标志）；**bump `LENO_BIN_VERSION` + `LENO_MODCACHE_VERSION`**（布局变） |
| ② 整包 LZ（自研 LZSS） | −70%+ / −50%+ | 高 | 中 | 加载时一次性解压到内存 ⇒ 运行时零成本；三处格式（`.lenb` / `.lenomc` / 打包 exe 内嵌段）同改 + 损坏时 fail-closed |
| ③ 元数据 u32→varint | −5~8% | 低 | 低 | 每 chunk 12 B（`local_count`/`const_cnt`/`code_len`）+ 每函数 ~28 B 计数字段，多数 < 128；纯机械、读写对称 |

**⑤ 判据（下次开工时的验收线；裁剪侧已于 2026-09-25 收工 —— 见开头"收工"块）**

- ①：pvz ≤ 95 万 B、minilang ≤ 1.56 万 B；②：pvz ≤ **18.2 万 B / hello_window ≤ 15.2 万 B**（2026-09-25 复测线，见 ③-补；原 45 万 B 线已过时）；③：**先补一次构成测量再定线**
- 三条共同要求：断言 **373 / 0 failed**；`-c` 编译 → 跑 `.lenb` 输出与直跑 `.leno` 一致；
  旧格式被版本号挡下重编译（fail-closed）；按 §七登记表写明"为什么升/为什么不升"

**⑥ 复测跑法（构成怎么量出来的）**

把探针按同样方式临时加回 `src/serialize/serialize.c` 的写入点（`LENO_SZSKIP`：
1=不写 code、2=不写字符串载荷、4=不写行号表、8=不写 scope、16=字符串去重 what-if），
`.\build.bat` 后逐档跑：

```powershell
$env:LENO_SZSKIP='1'; .\build\leno.exe --no-cache -c examples\minilang\minilang.leno
(Get-Item examples\minilang\minilang.lenb).Length     # 与基准 17829 的差 = code 段字节数
```

各段写入互不影响 ⇒ 差值是**精确值**。量完记得把探针删干净（本次已 `git diff` 确认为空）。

**⑦ 引用图实测 + 结构冗余去重（2026-09-24，`LENO_DCEPROBE` 探针量完已删）**

量法：编译期引用图（def 点 = 槽位↔函数；use 点 = 全局直呼 / 取函数值 / 跨模块 exports 名 /
方法调用点 / 类型引用），序列化时按"独占字节"（栈式扣除嵌套函数）记账，`--no-cache -c` 跑：

| DCE 规则（从保守到激进） | hello_window | pvz |
|---|---|---|
| 保守：方法按**名字并集**（任一处调用某名 ⇒ 各模块同名方法全留） | 118764 B（10.5%） | 132166 B（10.5%） |
| 类型级：类型被引用 ⇒ 该方法全留 | 183089 B（16.1%） | 200016 B（15.9%） |
| 方法级：按调用点（接收者静态类型已知 ⇒ (类型,名) 精确；未知 ⇒ 名字通配且只作用于已引用类型） | 387824 B（34.1%） | 410366 B（32.7%） |

按存储点（函数体字节，hello/pvz）：`exports{}` 169392/184938、`init_chunk` 常量 727996/756491、
`globals[]` 77102/89571、入口 5432/47844；非函数字节 156397/175321（13.8%，DCE 动不到）。

**已落地两处结构去重（`module_compiler.c`，合成一次改动）**

1. **删掉 exports 里的 `"类型::方法"` 空壳原型**（原第 5.1 段）：1434 个方法各有一个
   chunk 为空的 proto 被塞进 exports，运行期从不被读（struct 方法分派只走 `struct_method_lookup`，
   VM 里没有任何地方按 `"::"` 查 exports）。
2. **顶层函数不再预填 `globals[]`/`exports{}`**：第 4 步与第 6 步各编译一份完整体，
   `globals[]` 里那份 76525 B + `exports{}` 里对它的重复 64595 B 全是 `init_chunk` 副本的冗余；
   现在 exports 只留 **null 占位**，运行期 init_chunk 跑完后由 `export_mappings` 的
   **FUNC_DEF** 条目（新增）从 `globals[slot]` 补填真身。

实测：hello_window 1136319 → **864587（−23.9%）**、pvz 1254165 → **954289（−24.0%）**。
验收：断言 373/0；7 个跨模块用例 `.leno` 直跑 vs `-c` 后跑 `.lenb` 输出逐字一致。
⚠ 已知行为差异（已扫过全仓，无此写法）：模块顶层在函数定义**之前**引用该函数值 ⇒ 那一刻读到 null。

**剩余候选（按性价比）**

| 候选 | 预估收益 | 成本 | 要点 |
|---|---|---|---|
| ① 方法级 DCE（引用图落地） | **已落地（2026-09-25）**：`hello_window` 862048 → **432725 B（−49.8%）**、`pvz` 951415 → **505432 B（−46.9%）** | 高 | 见下方"① 落地" |
| ② 第 4 步不再编译函数体 | **已落地（2026-09-25）**：codegen 时间**无可测变化**；.lenb 再小 2539 / 2874 B | 低 | 见下方"更正" |
| ③ 字符串 interning / 整包 LZ | LZ 已复测（2026-09-25，当前产物 deflate **2.8×**，见 §③-补）；interning 的数字**仍是旧产物测的，要用当前产物重测** | 中/高 | interning 要 bump `LENO_BIN_VERSION` + `LENO_MODCACHE_VERSION`（布局变）；LZ 要改三处格式 |
| ④ 类型级 / 保守级 DCE 档位 | 不需要了 | — | 原表里另两档（类型级 16% / 保守档 10.5%）是"方法级做不了时的退路"；方法级已落地且更激进，退路无需再实现 |

**① 落地（2026-09-25，`src/dce.c` + `src/include/leno_dce.h`）**

做法与原方案不同（**成本核算后改路线**）：原计划"未达函数写空壳保索引"，实测空壳 ≈76 B/个、
hello 要 2462 个 ⇒ 空壳自身 ≈187 KB、净收益只剩 ~4%，**放弃**。改成**写入期判定**：
`serialize_constant` 的 OBJ_FUNCTION 分支遇到不可达函数只写 **1 字节标签**
`CONST_TAG_DEAD_FUNCTION(0x14)`（零载荷）；读侧就地合成"空体 + 一条 `OP_RETURN`"的最小函数
（真被调到时返回 null、**不崩**）。因此**不需要保索引、不需要重排常量池**。

引用图（编译期收集，与 codegen 边生成边记）：
- **def 点**：`SYM_GLOBAL_FUNC` / `SYM_MODULE` 的（槽位 ← 函数）；方法由 `gen_struct_def` 带类型名注册
- **use 点**：全局/模块槽位引用（含 typed 直呼、`OP_GET_MODULE_VAR`）、跨模块名（`gen_module_call_prep` /
  `gen_module_access`）、方法引用（`gen_method_call` / `gen_call_multi` / `?.` 三处）、类型引用
  （类型名当值、`new T()`）、**匿名闭包创建点**（`dce_note_func_value`）
- **根**：各编译单元的模块级/顶层代码引用（这些一定执行）+ `main` 槽位
- 不动点收敛：名字/类型是间接引用 ⇒ 名字只命中顶层函数；方法由 (类型,名) 精确命中，
  未命中则**名字通配**（再由"该类型是否被引用"过滤）

三条 fail-safe（剪错的表现是**静默返回 null**，所以宁可少剪）：
① 未登记 ⇒ 保活；② 有模块来自 `.lenomc` 缓存 ⇒ 图不完整 ⇒ 整轮不剪；③ **ctor/dtor 无条件全留**
（VM 按 `ctor_index`/`dtor_index` 隐式调用，静态**没有任何**引用点 —— 这条不花体积，实测开/关同尺寸）。

顺带把入口编译（`-c` / `-p`）改成**不读模块缓存**：命中缓存的模块不走 codegen ⇒ 图缺一半。
**这是有代价的**（见下方"⑤ 缓存实测"）：读模块缓存本来快得多（hello_window `-c`：
读缓存 168 ms vs 全量重编译 531 ms）⇒ 每次分发编译多付约 **360 ms**，换产物体积 −50%。

实测（同二进制 `LENO_NO_DCE=1` 开关，`--no-cache`，3 轮取 min）：

| 产物 | DCE 关 | DCE 开 | 变化 |
|---|---|---|---|
| `hello_window.lenb` | 862048 B | **432725 B** | **−49.8%** |
| `pvz.lenb` | 951415 B | **505432 B** | **−46.9%** |
| `minilang.lenb` | 17829 B | 17829 B | 0（16 个函数全可达，符合预期） |

可达/可剪（`LENO_DCE_VERBOSE=1`）：hello 1775 个函数（顶层 332 / 方法 1434 / 局部 9）⇒ 可达 510、剪 1265；
pvz 1934 个 ⇒ 可达 601、剪 1333。
代价：`hello_window` 一次全量编译 549 ms → 582 ms（**+33 ms，+6%**，图收集 + 收敛）。

格式与版本：新增常量标签 ⇒ **`LENO_BIN_VERSION` v3.0.2 → v3.1.0、`LENO_MODCACHE_VERSION` v15 → v16**
（理由见 `docs/待办_单一事实来源与重复实现收敛.md` 第七节"实例十一"）。VM 也须同批重建。

**验收**：断言 **373 / 0**（含 `LENO_NO_CACHE=1` 强制全量重编译一轮）；
**373 个用例全量差分对拍**（`.leno` 直跑 vs `.lenb`）——用 `leno.exe` 跑差集 22 条、
用 `leno_vm.exe`（真正的分发路径）跑差集 47 条，**与 HEAD 基线逐条一致（集合差异 0）**，
且差异全是既有噪声（编译期 warning 只在一侧、内存/耗时/随机数）。关掉 DCE 后
`test_struct_iterator` 同样失败 ⇒ 属既有问题（`.lenb` 路径的 `p.len()`，与本次无关）。
`hello_window.lenb` / `pvz.lenb` 通过 `leno_vm.exe --check-bin`；两者 GUI 短时启动均未崩溃；
12 个 SDL3 例子 `-c` 全通过；`-p` 打包产物跑通（exit 0）。

**更正（2026-09-25，候选②）**：原估"省 codegen 时间 ≈ 一半"**实测不成立**。跨 exe A/B
（两版二进制来回交替、冷缓存 `--no-cache`、各 5 轮取 min）：`hello_window` 有第 4 步
**399 ms** → 去掉后 **398 ms**；`pvz` **408 ms** → **401 ms**（两组分布重叠，无有效差异）。
原因：第 4 步只重复编译了**顶层小包装函数**（实测 ≈330 个、均 232 B 的函数体），
1434 个方法体它只建"空壳原型"—— 大头从来没有被编译两遍。
**该改动仍保留**（真实收益在别处）：删掉 ~90 行重复编译 + 一个 dead API
（`codegen_set_func_dict`）、每次模块编译少建 ~1750 个函数对象与 1434 个 `"类型::方法"`
驻留字符串（内存）、`.lenb` 再小 2539 / 2874 B（hello/pvz）。
**教训**：估"某段代码占多少时间"必须实测（同 §⑥ 的口径）—— 这次错在把"两遍编译"
直接当成"时间减半"，而没先确认那两遍编译的**工作量是否相当**。

**⑤ 缓存实测（2026-09-25）—— 顺带挖出并修掉一个"白烧 0.6 秒"的 bug**

起因：DCE 落地后我在注释里写"入口编译不读模块缓存**没有代价**（读缓存并不更快：
775 vs 560 ms）"。用户追问"读缓存更慢！那缓存还有必要?"，重测后发现：那个数字**是真的**，
但**原因是 bug**，不是"缓存没用"。

量法（同二进制、交替 5 轮取 min）：

| 路径 | 配置 | 修 bug 前 | 修 bug 后 |
|---|---|---|---|
| `-c` 编译 | 全量重编译 43 模块 | 536 ms | 531 ms |
| `-c` 编译 | 读模块缓存 | **763 ms（慢 227）** | **168 ms（快 363）** |
| 运行到窗口 | 无任何缓存（`LENO_NO_CACHE=1`） | 未测（pvz 644） | 623 ms |
| 运行到窗口 | 模块 + 入口缓存（稳态） | 未测（pvz 92） | **95 ms** |
| 运行到窗口 | 只模块缓存（改了主文件） | 908 ms | **263 ms** |
| 运行到窗口 | 两者都删（都写入口缓存） | 869 ms | 663 ms |

⚠ 上表两行"未测"：hello_window 的 **A（无任何缓存）** 与 **B（稳态）** 是修完之后才补测的，
修前只测过 pvz 的同名配置（列在括号里，那两格同口径可比：pvz A 644 / B 92）。
**不要把"两者都删"（869）当成"无任何缓存"** —— 两者**都写入口缓存**，是这个 bug 的"
共同成本"，修后 869 → 663 那 206 ms 就来自它。

**根因**：`cache_runtime_binary_fingerprint()`（serialize.c）**没有记忆化** —— 它要把
**整个 exe（1.4 MB）读进来逐字节 FNV**（≈1 ms/次），而调用点是每个模块的 `.lenomc` /
`.lenosymc` 缓存的**读与写**各一次 ⇒ 43~52 个模块就是**上百次** ⇒ 白烧 ≈0.6 s。
函数原注释写"只在入口缓存校验时调用一次"，早已不成立（§8.112 之后模块缓存/符号表缓存
也要它）。**修法**：把成功结果记忆化（运行中的 exe 在进程生命周期内不可能变；0 是
"取不到"的哨兵、可能瞬时占用 ⇒ 只记忆非 0）。一处 4 行改动。

**结论（回答"缓存还有必要?"）**：两个缓存都**很有必要** ——
入口缓存让稳态启动 **6.6×**（623 → 95 ms，跳过 parse+semantic+codegen+serialize 全部）；
模块缓存在"改了主文件、入口缓存失效"这个最常见场景下省 **400 ms（2.5×）**（663 → 263 ms）。
之前量到的"负收益"完全是上述 bug 的假象。

**方法论教训**：两个测量（`-c` 上 +227 ms）与（运行路径上 +39 ms）对**同一件事**给出
差 5 倍的幅度 —— 这个"不一致"本身就是信号：说明有一个**共同成本**被两条路径都付了，
所以它的**差值**被掩盖了。顺着它去查才挖出未记忆化的指纹。
**"两次测量互相矛盾"要当成线索，不要当成噪声。**

---

## 四、常用命令

```powershell
# 编译
.\build.bat

# 断言套件（当前 373 passed / 0 failed）
.\build\leno.exe assert\run_tests.leno build\leno.exe assert

# 单条断言 / 单条样例
.\build\leno.exe assert\test_async.leno
.\build\leno.exe --no-cache "examples/async await/struct 方法 + async.leno"
```

> 没有差分/对照这一步了（对照脚本已删）。要看"某个改动会不会炸样例"，按 T2 的计划做输出快照。

---

## 五、维护约定

1. **做完一项**：在 §三 补一段（现象 / 最小复现 / 根因 / 修法 / **实测数字**），这里把状态改成 ✅ 并写 commit。
2. **新发现的问题**：先判断是不是已有条目的同类 —— 是就挂到那条下面（或扩成子项），
   别新开条目，否则这份清单会碎片化。
3. **每项必须有判据**：没有"怎么算做完"的条目不算条目。
4. **改了语义或热路径** ⇒ 必跑断言套件 + 按纪律做双向交替 A/B（预期收益 <2% 的先做空改动对照），
   结论写进 §三；**不与任何第二实现做对照**。
5. **动手前先查 §T5 的「已排除（都做了实测，别再重复试）」清单**（当前 8 条）—— 尤其是
   **第 8 条：T9 / 抽 handler 冷路径整条路线已实测证伪（是"代码布局抽奖"，不是杠杆）**。
6. **A/B 的三条硬规矩**（都是踩过的坑）：
   - **同二进制 env 开关**（`LENO_NO_CMPJMP` 之类）**必须带 `--no-cache`** —— 入口缓存只认
     "源码哈希 + exe 指纹"、**不含环境变量**，否则第二次运行直接吃第一次编译的字节码（见 ⑤-af）；
   - **跨 exe 对比天然安全**（指纹不同），但**每次 A/B 都要对调顺序**再下结论；
   - **"改动让某项变慢"必须做一次回退复测**（还原后回到原区间才叫归因干净）—— 见 §T9 的 ⑤。
