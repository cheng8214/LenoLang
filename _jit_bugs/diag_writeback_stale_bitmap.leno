// §8.128 诊断：写回路径的"类型位图陈旧" ⇒ 裸 int48 被当 float 原样写回 frame->locals
//
// 触发条件（两个都要）：
//   ① 某局部量**进循环时非 int**（如 float 1.0）⇒ 序言把它的位图位置 1；
//   ② 循环体内被赋成 **int**（如 `z = 7`）⇒ 位图**没有**被更新（写点不维护位图，§8.127 已证）。
// 循环退出写回时：`BT RBX,i` 命中 bit=1 ⇒ 走"原样存回"分支 ⇒ 把**裸 int48（无 NaN-box 标签）**
// 写进 `frame->locals[z]`。解释器随后按 double 解释这个位型 ⇒ **静默错值**（不是类型标签差异，
// 是数值直接变成次正规垃圾）。
//
// 期望（JIT 必须与 LENO_NO_JIT=1 一致）：z=[7]
main() {
    var z = 1.0            // ① 入口 float ⇒ 位图 bit=1
    int i = 0
    while i < 300 {
        z = 7              // ② 循环内改写成 int ⇒ 位图陈旧
        i = i + 1
    }
    print("z=[" + z + "]\n")
}
