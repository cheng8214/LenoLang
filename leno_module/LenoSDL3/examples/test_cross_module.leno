// test_cross_module.leno — 跨模块匿名函数
import "mylib.leno" as mylib
use mylib.Vec2
main() {
    // 不用 as，直接用模块名访问
    print("--- forEachVec ---")
    var sum = 0.0
    mylib.forEachVec(5, func(Vec2 v) {
        sum = sum + v.x + v.y
    })
    print("sum = " + sum)

   
}
