// mod.leno — export struct 包含跨模块 cstruct 数组操作
import "bug_repro_core.leno" as core
use core.Vertex

export struct Renderer {
    func test() {
        Vertex v = Vertex.malloc_array(3)
        v[0].x = 1.0
        v.free()
    }
}
